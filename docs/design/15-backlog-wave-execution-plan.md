# Backlog wave-execution plan (design-sweep output, 2026-06-22)

Generated by the `backlog-wave-plan` ultracode workflow (14 design agents + synthesis). Full per-slice specs (exact API, RED-GREEN test plans) are in the workflow transcript; this is the deconflicted execution index. **Serial landing** — 10 slices edit both `src/validation_status.jl` + `test/runtests.jl` so they land one at a time. Nothing here is a covered-promotion or blocked; all land as `partial` (experimental/validation-scale).

## Waves (serial landing order)

- **Wave 1**: H1, C5, I9, I10
  - H1 lands first to establish the dispersion-parameter/Fisher-weight convention H2 reuses; padded with low-risk in-place/doc-only work (C5 widens V1-HERIT-CI for genomic, I9 read-only tracker, I10 pure-docs gate predicates) so only ONE heavy (H1) and the validation_status.jl/runtests.jl edits stay serial.
- **Wave 2**: H2, I1, L1
  - H2 (beta-binomial) now that H1 set the two-parameter convention; one heavy (H2), with I1 (sire fixture, light) and L1 (Makie drawing stub, runtests.jl-only) as deconflicted fillers — all serialize cleanly on the shared row-count pin.
- **Wave 3**: C2, C10
  - C2 is the heavy cross-lane MV-REML genetic-correlation-interval bridge (appends, not restructures, the freshly-landed V4-MV-REML row); C10 (medium LRT helper) adds a new row — one heavy, both serialize on validation_status.jl+runtests.jl.
- **Wave 4**: C6
  - C6 (parametric-bootstrap VC interval) is heavy and appends to V1-HERIT-CI — held apart from C5's Wave-1 edit of the same row to avoid clobber; lands solo as the wave's single heavy.
- **Wave 5**: H3
  - H3 (probit/threshold family) is heavy and bumps the V6 row count (+V6-PROBIT); landed solo to rebase cleanly on the running count and keep one heavy per wave.
- **Wave 6**: H6
  - H6 (non-Gaussian interval coverage) is heavy, edits V6-FIT in place, and its BLAS-heavy coverage run must not coincide with another heavy slice; lands solo.
- **Wave 7**: H7
  - H7 (latent/observation-scale heritability transform) is heavy and adds V6-NS-H2; final wave, solo, so the covered-count guard (runtests.jl:185) and row-count pin rebase last with no contention.
- **Unscheduled (slot into a later wave)**: J1

needs_signoff: [] | blocked: []

## Slice index

| id | lane | cpu | eff | funnel files | one-line change |
| --- | --- | --- | --- | --- | --- |
| C10 | JL | medium | M | 3 | Extract a fit-agnostic, reusable LRT helper from `covariance_structure_lrt` and add a real boundary correction.  NEW (internal, in src/multi… |
| C2 | bridge | heavy | L | 5 | ENGINE (src/multivariate.jl), one new exported function with two methods:  `genetic_correlation_interval(fit, Y, X, Z, Ainv; level=0.95, met… |
| C5 | JL | light | S | 4 | No new exported symbol and NO new positional/keyword argument. `variance_component_interval(fit::AnimalModelFit; level=0.95, method=:profile… |
| C6 | JL | heavy | M | 6 | New exported Julia function in src/likelihood.jl (univariate Gaussian REML only, the validated lane):      bootstrap_variance_component_inte… |
| H1 | JL | heavy | M | 5 | Add a NEW internal family to src/nongaussian.jl and a 2-D-overdispersion branch to the EXISTING exported `fit_laplace_reml`. NO export chang… |
| H2 | JL | heavy | M | 4 | Add a `BetaBinomialResponse <: ResponseFamily` to src/nongaussian.jl and wire it into the conditional (Laplace) kernel path only.  NEW TYPE … |
| H3 | JL | heavy | M | 4 | Add a probit (threshold / liability-scale) family to the existing non-Gaussian engine in src/nongaussian.jl, mirroring how BernoulliResponse… |
| H6 | JL | heavy | M | 3 | SCOPE CORRECTION (ground-truth): the headline "extend laplace_reml_interval to Poisson/Bernoulli/Binomial uniformly" is ALREADY DONE. `src/n… |
| H7 | JL | heavy | M | 5 | Add the Nakagawa–Schielzeth / de Villemereuil (QGglmm) latent- and observation-scale heritability transform to src/nongaussian.jl. NEW EXPOR… |
| I1 | JL | light | M | 5 | No new src/ numerics. The Mrode Ch.3 (animal-model) fitted target already exists at test/fixtures/animal_model_fitted_target/ (#46); I1 adds… |
| I10 | bridge | light | M | 3 | DESIGN-ONLY, DOC slice — no Julia/R code, no exports, no signature change. Add a new canonical design doc `HSquared.jl/docs/design/15-promot… |
| I9 | JL | light | S | 3 | NEW self-contained module `sim/summarize_validation_debt.jl` named `module ValidationDebtTracker`, mirroring the existing `sim/summarize_rec… |
| J1 | JL | light | M | 4 | Add a new dense, validation-scale haplodiploid additive-relationship kernel to `src/pedigree.jl`, mirroring the EXISTING sibling kernels `cl… |
| L1 | JL | light | M | 2 | Drawing-only: extend the EXISTING `HSquaredMakieExt` weak-dep extension with new `kind`s for figure kinds whose backend-free `*_plot_data` p… |

## Per-slice validation-row text (honest, partial)

- **C10** (medium): New row id `C10-LRT` (capability "reusable nested-model LRT helper (df + single-boundary chi-bar correction)", Phase "Phase 4 / inference", status "partial"). Evidence: "`nested_lrt(loglik_constrained, loglik_full; df, boundary_df)` is a fit-agnostic likelihood-ratio helper extracted from `covarianc
- **C2** (heavy): EXTEND the engine V4-MV-REML row evidence (src/validation_status.jl:259-271) with a new sentence (do NOT add a new row; do NOT touch the row status, which stays "covered" for the REML POINT ESTIMATE only): "`genetic_correlation_interval(fit, Y, X, Z, Ainv; method = :delta | :profile)` adds an experi
- **C5** (light): Extend the EXISTING V1-HERIT-CI row (validation_status.jl:142 / validation-debt-register.md:31 / capability-status.md:31) in place — do NOT add a row (count pinned at 41, runtests.jl:174). Append to the V1-HERIT-CI evidence: "The additive-variance profile-LRT interval `variance_component_interval(fi
- **C6** (heavy): V1-HERIT-CI (status stays `partial`): "... A parametric-bootstrap cross-check is now provided: `bootstrap_variance_component_interval(fit)` simulates Gaussian responses at the fitted (beta, sigma_a2, sigma_e2) over the supplied relationship (a* = chol(inv(Ainv)).L * randn .* sqrt(sigma_a2)), refits 
- **H1** (heavy): NEW row V6-NBINOM (status: partial), inserted adjacent to V6-BINOMIAL in src/validation_status.jl and docs/design/validation-debt-register.md:  "V6-NBINOM | Non-Gaussian negative-binomial (NB2, log link) family + overdispersion (Phase 6, H1) | partial | `NegativeBinomialResponse(theta)` (internal) e
- **H2** (heavy): V6-BETABINOMIAL | Non-Gaussian beta-binomial / overdispersed-logit family (Phase 6) | partial | `BetaBinomialResponse(n_trials, rho)` adds an OVERdispersed binomial family (logit link): the success probability is Beta-distributed across records with intra-class correlation ρ∈(0,1), marginalised in c
- **H3** (heavy): V6-PROBIT | Non-Gaussian probit / threshold (liability-scale) family (Phase 6) | partial | `BernoulliProbitResponse` (probit link, p = Phi(eta)) extends the non-Gaussian engine to a BINARY threshold (liability-scale) animal model for BOTH the Laplace marginal and the VA ELBO, reusing the existing pe
- **H6** (heavy): V6-FIT (UPDATE the existing `partial` row; do NOT add a new row and do NOT promote): append to the interval clause — "The single-component `sigma_a2` profile-LRT interval covers `:poisson`/`:bernoulli`/`:binomial` UNIFORMLY through one shared `_resolve_single_family` + `target`/`_profile_root` path 
- **H7** (heavy): New register/capability rows land as PARTIAL (experimental, validation-scale, opt-in; NOT the public default, NOT covered). Proposed validation-debt-register row id V6-NS-H2:  "| V6-NS-H2 | Non-Gaussian latent- and observation-scale heritability (Nakagawa–Schielzeth / de Villemereuil) | partial | `n
- **I1** (light): V1-SIRE-FIT | fitted sire-model native target (Mrode Ch.4) | partial | A Julia-native FITTED sire-model REML target (`test/fixtures/sire_model_fitted_target/`, #16): the engine fits its OWN sire model — an ordinary `AnimalModelSpec` with a sire-incidence `Z` and a sires-only `pedigree_inverse(Ainv)`
- **I10** (light): No new `validation_status()` row and no status change (this slice adds a meta-doc, not a capability). The engine `validation-debt-register.md` gets one top-of-file cross-reference line, not a new capability row: "Each capability's exact `partial -> covered` predicate is recorded in `docs/design/15-p
- **I9** (light): | V0-DEBT-TRACKER | Validation-debt burn-down tracker | covered | `ValidationDebtTracker` (`sim/summarize_validation_debt.jl`) auto-derives status counts (covered/covered_external/partial/planned), per-phase breakdown, and the verbatim open-debt list from the live `validation_status()` rows; determi
- **J1** (light): Two markdown rows, both PARTIAL/experimental, mirroring the V3-DOMINANCE/V3-CLONAL wording exactly (construction-only, dense, validation-scale, no fitting claim, no R model-spec, external comparator as retained debt):  validation-debt-register.md — SPLIT the existing `V7-INHERIT` row (currently "Oth
- **L1** (light): Update the `V-PLOT-DRAW` row in `docs/design/validation-debt-register.md` (and the §8 table + the prose 'four kinds / 5 stub assertions' counts in `13-plotting-layer.md`) from four to NINE figure kinds: '...renders the honest-status behaviors ON the figure (... the genetic-correlation heatmap gated 
