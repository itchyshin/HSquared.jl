# Capability Status

| Capability | Status | Evidence |
| --- | --- | --- |
| Package loads | implemented | `test/runtests.jl` |
| Backend marker types | implemented | `CPUBackend`, `ThreadsBackend`, `CUDABackend`, `AMDGPUBackend`, `MetalBackend`, `OneAPIBackend`, and `AutoBackend` tests; marker types only, no backend dispatch |
| `HSControl` validation | implemented | validates shared R/Julia planned backend and accelerator vocabulary in `test/runtests.jl`; no backend execution claim |
| `backend_info()` status diagnostics | implemented | typed rows for `cpu`, `threads`, `cuda`, `amdgpu`, `metal`, and `oneapi`; all selectable, all `execution_available == false`, all `status == :planned`; no runtime probing |
| Backend and algorithm roadmap documentation | implemented | Documenter page and design note mirror R head `2c18b30`; roadmap only, no backend execution, GPU, APY, AI-REML, Takahashi selected inversion, GLLVM fitting, or performance claim |
| `formula_status()` grammar diagnostics | implemented | 20 typed rows matching the R twin's parsed, reserved, and planned grammar status categories; diagnostic only |
| `validation_status()` validation diagnostics | implemented | typed validation rows for covered, external, partial, and planned validation evidence; diagnostic only |
| Planned genomic/QTL model-term vocabulary | implemented | `planned_genomic_qtl_terms()`, `genomic()`, `single_step()`, `markers()`, `marker_scan()`, and `qtl_scan()` tests; functions throw planned-not-implemented errors and do not construct model specs |
| Planned standard quantitative-genetic model-term vocabulary | implemented | `planned_quantgen_terms()`, `permanent()`, `common_env()`, `maternal_genetic()`, `maternal_env()`, `paternal_genetic()`, `paternal_env()`, `cytoplasmic()`, `imprinting()`, `dominance()`, `epistasis()`, `relmat()`, and `HSquared.precision()` tests; functions throw planned-not-implemented errors and do not construct model specs |
| `HSData` input container | implemented | in-memory phenotype/pedigree/genotype/expression ID-map tests plus genotype-status diagnostics, marker-map metadata, genotype-marker alignment, pedigree-status diagnostics, expression-status diagnostics, annotation-feature diagnostics, environment-key diagnostics, and `data_status()` diagnostic tests; no file-backed storage, automatic genotype-to-relationship construction, automatic expression/annotation/environment joins, environmental model terms, genotype parsing, marker scanning, eQTL/omics fitting, genomic relationship construction, Ainv construction from raw data containers, or modelling claim |
| `hsquared()` fitting | planned | Phase 0 placeholder only |
| `fit_animal_model(spec)` dense fitting | experimental | dispatches to `fit_variance_components()` for validated `AnimalModelSpec`; all other signatures remain placeholders |
| Pedigree validation | implemented | `normalize_pedigree()` valid, malformed, duplicate, missing-parent, self-parent, same-parent, and cycle tests |
| Sparse `Ainv` | implemented | `pedigree_inverse()` hand-checked tiny pedigrees and dense inverse comparison; shared calf/sire/dam fixture mirrored by R head `fe7e346`; optional R-side `nadiv::Mrode9` / `nadiv::makeAinv()` comparator at R head `369d14a`; bounded relationship cache, no huge-scale claim |
| Mendelian sampling variances `d` (public accessor) | implemented | `mendelian_sampling_variances(pedigree)` returns the per-individual within-family variances `d_i` (the diagonal of `D` in `A = T·D·Tᵀ`): `1` founder, `0.75 − 0.25 F` one parent, `0.5 − 0.25(F_s+F_d)` both. Validated: matches the internal per-record helper, the `det(A) = ∏ d_i` identity (atol 1e-10) on a mixed and a selfing-chain pedigree, the selfing recurrence `d_k = ½(1−F_{k−1})`, and the `(ids,sire,dam)` convenience (`test/runtests.jl`); the `1/d_i` are the Henderson `pedigree_inverse` scales |
| Dense additive relationship `A` (public accessor) | implemented | `additive_relationship(pedigree)` exposes the dense numerator relationship `A = inv(Ainv)` (`A[i,i] = 1 + F_i`) — the additive companion of the exported dominance / cytoplasmic / clonal matrices. Validated: equals the internal recursion, `A == inv(pedigree_inverse)`, full sibs `0.5`, parent–offspring `0.5`, inbred diagonal `1.25` at `F = 0.25`, and the cache guard (`test/runtests.jl`); validation-scale and dense (bounded cache), production uses the sparse inverse |
| Sparse CSC bridge marshalling | implemented | `sparse_csc_matrix()` zero-based R-slot, one-based Julia-slot, malformed-slot, and direct payload integration tests |
| Animal model spec validation | implemented | `animal_model_spec()` dimension, ID, family, and method tests |
| Gaussian ML/REML likelihood evaluation | experimental | `gaussian_loglik()` hand-calculated tiny tests plus a Mrode9-shaped supplied-variance fixture with pinned ML/REML likelihood values; dense evaluator only, supplied variance components only |
| Sparse REML likelihood identity | experimental | `sparse_reml_loglik()` matches dense REML on tiny fixtures and a Mrode9-shaped supplied-variance fixture using the Henderson MME determinant identity; supplied variance components only, no optimizer or AI-REML |
| Sparse REML validation optimization | experimental | `fit_sparse_reml()` and `fit_animal_model(...; target = :sparse_reml)` optimize the sparse REML objective on tiny fixtures and improve over supplied starting variances; REML-only, no AI-REML, no fitted Mrode/comparator evidence, no production sparse fitting claim |
| Dense variance-component optimization | experimental | `fit_variance_components()` tiny improvement and dispatch tests; dense path only |
| Dense validation size guard | implemented | `max_dense_cells` tests for likelihood, dense optimizer, spec dispatch, and direct payload dispatch; guard only, not a sparse solver |
| Supplied-variance Henderson MME solve | experimental | `henderson_mme()` sparse solve tested against the shared R/Julia supplied-variance fixture for Ainv, fixed effects, EBVs, fitted values, and `h2`; R head `ca8bce1` also compares Julia against an independent R MME reference when available; a Mrode9-shaped supplied-variance fixture pins Ainv, fixed effects, EBVs, fitted values, PEV, reliability, derived accuracy, and `h2`; does not estimate variance components or claim production fitting |
| Iterative (PCG) MME solver | experimental | `solve_animal_model_pcg(spec, σ²a, σ²e; preconditioner = :jacobi \| :none)` solves the supplied-variance animal-model MME by preconditioned conjugate gradient on the IDENTICAL sparse SPD system `henderson_mme` factorizes directly (`_sparse_mme_system`), never forming a Cholesky factor. Validated to MATCH the direct solve — PCG β and EBVs equal `henderson_mme` to atol 1e-7/1e-8 on the tiny 3-animal, Mrode9-shaped 8-animal, and a larger 110-animal 4-generation pedigree (nfixed = 2); plain CG reaches the same solution and Jacobi takes no more iterations; the reported `relative_residual` is the true `‖rhs−Cx‖/‖rhs‖` at the returned solution and `≤ tol` at convergence; a starved `maxiter` reports `converged = false`; non-PD-curvature + σ/tol/maxiter/preconditioner guards (`test/runtests.jl`); exported (`V1-PCG`). A `matrix_free = true` path applies `C·v` directly from the sparse `X`/`Z`/`Ainv` matvecs (+ a matrix-free Jacobi diagonal) without ever assembling `C`, validated by `C·eᵢ == C[:,i]` exactly + matrix-free solve == assembled == direct. A CORRECTNESS primitive only — NOT the default fit path and NOT a performance/large-pedigree scaling claim (NO benchmark recorded); the iterative-solver foundation for the future production sparse path. Deferred: a recorded large-pedigree benchmark, advanced preconditioners, REML/fit-path wiring, and external comparator |
| MME-backed EBVs/BLUPs and fitted values | experimental | `breeding_values(fit)`, `EBV(fit)`, `BLUP(fit)`, and `fitted_values(fit)` now use the Henderson MME solve at the fit's variance components; hand-checked identity-relationship, Henderson MME, and Mrode9-shaped supplied-variance fixture tests cover values and MME equivalence; no production sparse fitting or external fitted-model comparator yet |
| Fitted univariate animal-model target fixture | experimental | `test/fixtures/animal_model_fitted_target/` serializes a Julia-native FITTED single-trait animal model (20-animal multi-generation pedigree, `Intercept + x`, interior REML optimum): the engine fits its OWN model (`generate.jl`) and serializes `sigma_a2`/`sigma_e2`/β/EBVs/PEV/reliability/`h²`/`loglik` — no textbook EBVs typed from memory. A committed CI test rebuilds `Ainv` from the pedigree and checks SELF-CONSISTENCY (the Henderson MME at the stored variance components reproduces the stored β/EBVs/PEV/reliability/loglik). An opt-in JWAS.jl comparator scaffold (`comparator/`, env-gated `HSQUARED_RUN_JWAS=true`, separate `Project.toml` — JWAS is NOT a package dependency) confronts the target with a Bayesian/MCMC estimator, reporting AGREEMENT honestly (not parity). Self-consistency only — NOT external validation; the R-lane (`nadiv`/`pedigreemm`) + JWAS confrontations are opt-in/cross-lane, no comparator EVIDENCE recorded yet, nothing promoted to covered |
| Dense heritability | experimental | `heritability()` hand-checked simple univariate variance-ratio test |
| Validation-scale supplied-variance outputs | experimental | `variance_components()`, `heritability()`, `prediction_error_variance()`, `reliability()`, and checked `accuracy()` tested for validation-scale objects, including a Mrode9-shaped supplied-variance fixture; PEV/reliability use dense Henderson MME inverse blocks; `accuracy()` derives from reliability and rejects invalid ranges; no sparse production solve or comparator yet |
| Fit diagnostics metadata | experimental | `fit_diagnostics()` tests cover `AnimalModelFit` and supplied-variance `HendersonMMEResult` metadata fields; metadata only, no new fitting, no optimizer run, no backend probing, and no `result_payload()` widening |
| Direct payload fit targets | experimental | `fit_animal_model(y, X, Z, Ainv; ...)` parity tests against validated-spec dispatch; default dense optimizer path plus explicit `target = :henderson_mme` supplied-variance solve; no production sparse fitting |
| R result payload shape | experimental | `result_payload()` field-name/value tests aligned to R `hsquared_fit` extractor contract; R head `c837f2d` has an internal JuliaCall smoke over these fields; R head `8235289` enriches the opt-in tiny/local R bridge from exported Julia PEV/reliability extractors when available; R head `d7e8914` also enriches supplied-variance `target = "henderson_mme"` bridge results from `prediction_error_variance(mme)` and `reliability(mme)` when applicable; R head `060988d` adds R-side `fit_diagnostics()` and Julia mirrors it as a metadata-only helper; the base `result_payload(::AnimalModelFit)` now includes `prediction_error_variance` and `reliability` as standard `(ids, values)` fields — PEV computed through the `O(nnz(L))` (sparse-scalable) Takahashi selected inverse (`method = :selinv`, machine-precision-equal to the dense MME diagonal for well-conditioned validation-scale fits — `V1-SELINV-PEV`) and reused by `reliability` (no second factorization), which the R twin unpacks top-level via `hs_julia_id_values()` (the coordinated `hsquared#21` landing; the prior opportunistic per-extractor enrichment is now redundant); other `fit_diagnostics()` extras stay out of the base payload. Validation-scale, not a production large-pedigree reliability claim (the `reliability` denominator still forms the dense `inv(Ainv)`) |
| R v0.1 formula parser and payload preview | external implemented | `hsquared` head `b57b48e`; R parser builds the narrow bridge payload; head `9eabf0d` adds opt-in experimental `hs_control(engine = "julia")` for tiny/local use; head `398e019` records sparse `Z` marshalling through Julia `sparse_csc_matrix()`; head `bacef9c` adds exported `model_spec()` preview without fitting or Julia execution; head `36efbf3` lets the same parser consume `hs_data()` while keeping the bridge payload shape unchanged; heads `74eef82` and `39ca990` add R-side `animal(1 \| id)` shorthand when `hs_data()` supplies pedigree, still without a Julia API or payload change |
| R `hs_data()` data container | external implemented | `hsquared` head `644c75e`; head `36efbf3` connects `hs_data()` to the v0.1 R parser; head `f067cd9` adds genotype-status diagnostics; head `06cdf59` adds expression-status diagnostics; head `e7fbb31` adds environment-key diagnostics; head `87888d9` adds annotation-feature diagnostics; Julia mirror exists as `HSData`, but live Julia object marshalling is not implemented |
| Opt-in R-to-Julia bridge execution | external experimental | `hsquared` head `398e019` consumes Julia `sparse_csc_matrix()` for sparse `Z`; head `00b9e33` adds an explicit opt-in `target = "henderson_mme"` path with supplied variance components and green R checks; still no variance-component estimation, AI-REML, production sparse fitting, Mrode fitted-output validation, large-data readiness, or relationship-object marshalling beyond `Z` |
| File-backed phenotype/genotype storage | planned | no Arrow/Parquet/PLINK/VCF/HDF5/Zarr implementation yet |
| Sparse production fitting / AI-REML | experimental | `fit_ai_reml` estimates the two variance components by average-information REML on the sparse Henderson MME (score from the Takahashi selected inverse; AI matrix from working-variate re-solves); validated to recover the same optimum as the dense/sparse NelderMead optimizers, and its AI matrix matches an independent finite-difference REML Hessian (~8%, see `V1-HERIT-CI`); known-truth recovery (R-lane DGP study) and the published gryphon anchor (Wilson 2010, h²=0.470) + sommer comparator agreement are validated externally via the bridge; REML-only, 2-component, Gaussian-only; Julia-native recovery/fitted fixtures, large-pedigree, and boundary hardening pending; not the public default |
| Variance-component covariance + heritability interval | experimental | `variance_component_covariance` / `variance_component_standard_errors`, `heritability_standard_error`, and `heritability_interval` (logit-transform delta interval, always in (0,1)) from the REML AI matrix; an opt-in profile-likelihood interval (`heritability_interval(fit; method = :profile)`) inverts the REML likelihood-ratio statistic over the total-variance-profiled h² and clamps to (0,1) where the surface is too flat to reach the threshold (default stays `:delta`); the AI matrix matches an independent finite-difference REML Hessian (~8%) and the interval nests by level; asymptotic, REML-only, wide and unreliable at small n, not coverage-calibrated |
| Repeatability / permanent-environment model (supplied variance) | experimental | `repeatability_mme` solves the two-random-effect animal model (additive `a` + permanent-environment `pe`) at supplied variance components via a block-diagonal relationship precision; matches an independent marginal-GLS BLUP (~1e-9) and reduces to the animal model as `σ²pe → 0`; supplied-variance only, no R-facing model-spec, engine-internal |
| Repeatability REML (variance-component estimation) | experimental | `fit_repeatability_reml` estimates (σ²a, σ²pe, σ²e) by maximizing the dense two-random-effect REML log-likelihood, returning the repeatability `t` and `h²`; the loglik reduces to the animal-model REML when σ²pe=0, its BLUPs match `repeatability_mme`, the optimum beats a coarse grid, and a seeded n=70 simulation recovers the VCs (one-off); a committed opt-in recovery harness (`sim/phase3_qg_recovery.jl`, outside CI) recovers the repeatability `t` on 5/5 predeclared seeds (max rel 0.254), with the σ²a/σ²pe split (`h²`) under-identified at this validation scale (reported, not gated); `repeatability_interval` gives an asymptotic delta-method CI for the well-identified `t` (logit-scale, observed-information from a finite-difference REML Hessian; throws on a non-PD/flat information), validated for bracketing/(0,1)/level-nesting/point-estimate-match on a seeded fixture; dense/validation-scale, REML-only, no `h²` interval (the split is ill-conditioned), no external comparator, no R-facing model-spec |
| General two-random-effect MME (common environment / maternal) | experimental | `two_effect_mme` solves a model with two independent random effects (each with its own incidence, relationship inverse, and variance); validated on a common-environment fixture vs an independent marginal-GLS BLUP (~1e-9); `repeatability_mme` is the `Z2=Z1, A2=I` special case; supplied-variance, two independent effects only — no correlated direct–maternal genetic (2×2 G), no estimation, no R-facing model-spec |
| Two-effect REML (common-environment / maternal estimation) | experimental | `fit_two_effect_reml` estimates (σ1, σ2, σe²) of the general two-effect model by dense REML (ratios `ratio1`/`ratio2`, e.g. h²/c²); equals `fit_repeatability_reml` in the `Z2=Z1, A2=I` reduction, loglik reduces to the animal-model REML at σ2=0, common-env fit converges; dense/validation-scale, REML-only, no committed recovery test, no intervals, no correlated maternal genetic, no R-facing model-spec |
| Multivariate (multi-trait) animal model (supplied covariance) | experimental | `multivariate_mme` solves the multi-trait animal model at supplied genetic/residual covariances `G0`, `R0` (Kronecker MME: genetic precision `Ainv⊗G0⁻¹`, block-diagonal residual precision; trait-fastest ordering), returning per-trait fixed effects and EBVs plus `genetic_correlation`, `variance_components`, `fixed_effects`, and `breeding_values`/`EBV`/`BLUP` extractor coverage with copy-return tests; β/EBVs match an independent loop-built MME, an independent marginal-GLS BLUP, the univariate animal model at `t=1`, and `t` independent single-trait fits when `G0`/`R0` are diagonal (committed 1e-10, observed machine precision); **unbalanced / missing-trait records** (`missing`/`NaN`) handled via per-individual `inv(R0[S_i,S_i])`, validated vs loop-built MME + marginal-GLS on a missing fixture (committed 1e-9); supplied-covariance with a shared design across traits — no R-facing multivariate model-spec or bridge payload change |
| Multivariate REML (estimate G0/R0) | experimental | `fit_multivariate_reml` estimates the multi-trait `G0`, `R0` by dense REML (log-Cholesky-parameterized NelderMead on `V = Z(A⊗G0)Z' + block-diag R`; handles missing records), returning the covariances, correlations, per-trait `h²`, and EBVs at the estimate; validated by the `t=1` reduction to the univariate REML (the loglik EQUALS `sparse_reml_loglik` at `t=1` — full REML loglik incl. the `(N−p')·log(2π)` constant, on the package scale), grid-beating, EBV consistency with `multivariate_mme`, and copy-returning Julia accessors (`variance_components`, `fixed_effects`, `heritability`, `breeding_values`/`EBV`/`BLUP`) over existing fields; `test/fixtures/phase4_multitrait_parity/` serializes a deterministic two-trait Julia target fixture for R-lane comparator work, now with README + decision-note comparator protocol, and CI checks target-covariance self-consistency; opt-in script `sim/phase4_multivariate_reml_recovery.jl` records seeded two-trait known-truth recovery on a repeated-record half-sib design outside CI and now accepts `--seed` or explicit `--seeds` lists with summaries (seed 20260616: G relative error 0.174500, R relative error 0.131056, thresholds 0.25/0.20); `docs/dev-log/decisions/2026-06-14-multivariate-recovery-calibration-protocol.md` defines the seed-count, run-plan, and reporting gate required before any broad calibration claim, and the predeclared run did not pass (unstructured 6/10, all fits converged; failure-mode triage found 3 G-only failures plus 1 G+R failure); a 7-lens adversarial review (Julia-verified) ran — confirmed robustness findings (non-finite/empty-trait guards, loglik constant) fixed + regression-tested; **`multivariate_covariance_standard_errors`** returns asymptotic observed-information + delta-method SEs (`G0`/`R0`, genetic/residual correlations, per-trait `h²`) for the unstructured fit — validated against an independent raw-variance FD-Hessian SE at `t=1`, with a documented non-PD-information guard (the n=8 single-record optimum is on the `rg→±1` boundary, so SEs need an interior optimum from repeated records); **`covariance_structure_lrt`** gives a boundary-aware nested-structure LRT (dependency-free χ²`df` survival validated vs textbook critical values); experimental, dense/validation-scale, asymptotic/not-coverage-calibrated — recovery is not broadly multi-seed calibrated, **no external-comparator (sommer/ASReml/JWAS) parity yet**, no structured-fit SEs (rotation-nonidentified), no R-facing model-spec, no `result_payload()` widening |
| Multivariate result bridge payload (diagonal/unstructured) | experimental | `multivariate_result_payload(result)` returns the "boring" bridge `NamedTuple` (`engine`/`target`/`genetic_structure`/`n_traits`/`traits`/`G0`/`genetic_variances`/`R0`/correlations/`heritability`/`fixed_effects`/`breeding_values`/`loglik`/`n_genetic_params`/`converged`) for a `fit_multivariate_reml` result, mirroring the univariate `result_payload`; exposed only for the rotation-free `:unstructured` and `:diagonal` structures (`:lowrank`/`:factor_analytic` rejected — loadings rotation-nonidentified). `n_genetic_params` (`:diagonal` = t, `:unstructured` = t(t+1)/2) makes the diagonal-vs-unstructured LRT `df` a difference (interior null). `test/fixtures/structured_covariance_parity/` serializes a deterministic two-trait `:diagonal` target with CI self-consistency. Unblocks the R-lane diagonal LRT (#42/#47); no loadings surfaced, no external comparator parity, R-side activation cross-lane |
| Production sparse reliability / PEV | experimental | `prediction_error_variance`/`reliability` accept `method = :selinv`, a Takahashi selected inverse (kernel adapted from DRM.jl, MIT) of the sparse Henderson MME coefficient matrix; the selected-inverse diagonal matches the dense MME inverse diagonal to machine precision on tiny, Mrode9-shaped, and a larger 110-animal 4-generation pedigree (nfixed = 2) fixtures; `O(nnz(L))`, exact at the `L+Lᵀ` pattern (diagonal/PEV exact); the default extractor path stays dense and `result_payload()` is unchanged; the AI-REML score trace `tr(A⁻¹C^uu)` in `fit_ai_reml` is now computed by the fused `selinv_trace_against` (accumulates over `Ainv`'s nonzeros, `O(nnz(L))`, no output matrix), numerically identical to the prior `sum(Ainv .* takahashi_selinv(...))` (rtol 1e-10, `test/runtests.jl`) — internal performance refactor, claim unchanged; no large-pedigree or external-comparator validation yet |
| Public multivariate G-matrix model-spec / syntax | planned | Julia has experimental multivariate supplied-covariance and dense REML engine utilities, plus structured `G0` builders; the public R-facing multivariate model-spec, covariance-structure syntax, long-format interface, and comparator-backed public claim surface remain planned |
| Factor-analytic / structured multivariate G matrices | experimental | `diagonal_covariance`, `lowrank_covariance`, and `factor_analytic_covariance` build `diag(σ²)`, `ΛΛ'`, and `ΛΛ' + Ψ` trait covariance matrices with guards; `fit_multivariate_reml(...; genetic_structure = :diagonal \| :lowrank \| :factor_analytic, rank = K)` estimates constrained genetic covariance structures while keeping residual `R0` unstructured. Deterministic checks cover constructor identities/guards, structure metadata, copy-returning `genetic_structure`/`genetic_loadings`/`genetic_uniqueness` accessors, returned loglik equality to the existing multivariate REML evaluator, PSD/PD covariance properties, constrained loglik not exceeding the unstructured fit, and deterministic sign-canonicalization of returned loading columns. The rotation/interpretation convention is now DECIDED (`docs/dev-log/decisions/2026-06-19-fa-rotation-convention.md`): bridge/inference uses rotation-INVARIANT functionals of `G` (eigenstructure via `genetic_pca`/`g_max`, evolvability, `Ψ`), never raw loadings; the sign-only metadata convention remains for returned loadings. Opt-in script `sim/phase4b_structured_covariance_recovery.jl` records seeded low-rank and factor-analytic recovery on a repeated-record half-sib design, accepts explicit `--seeds` lists, and prints per-case summaries outside CI. `docs/dev-log/decisions/2026-06-14-multivariate-recovery-calibration-protocol.md` defines the seed-count, run-plan, and reporting gate required before any broad calibration claim, and the predeclared structured run did not pass (factor-analytic 8/10 with G-only/G+R failures, low-rank 9/10 with 1 R-only failure, all fits converged). Experimental dense/validation-scale only — no R-facing covariance-structure syntax, no bridge/result-payload change (the decided rotation-invariant exposure is gated on R-lane ratification, #42), no production sparse FA solver, no broad multi-seed calibration, and no external comparator evidence |
| Evolvability / G-matrix geometry (Phase 4B) | experimental | `evolvability`, `conditional_evolvability`, `respondability`, `autonomy`, `variance_along_gradient`, `genetic_pca`, `g_max`, and `mean_evolvability` compute Hansen & Houle (2008) directional metrics and the genetic principal axes on a genetic covariance `G` (a matrix, or a multivariate result's `genetic_covariance`). Validated by hand-checked identities (`test/runtests.jl`): diagonal `G` (e = direction-weighted variance, c = harmonic form, autonomy = 1 along axes), isotropic `G = cI` (e = c = r = eigenvalue, autonomy = 1 in every direction), the `[3 1; 1 3]` eigenstructure (e/c/r = eigenvalue along eigenvectors; `genetic_pca`/`g_max` recover the descending, sign-canonicalized eigenpairs), `mean_evolvability = tr(G)/t`, `c ≤ e`, and EXPLICIT rotation-invariance `evolvability(ΛΛ') = evolvability((ΛQ)(ΛQ)')` (so reduced-rank/FA loadings' rotation ambiguity does not affect these G-functions). PSD-safe metrics (`evolvability`/`respondability`/`genetic_pca`/`g_max`) accept a rank-deficient `G`; the inverse-using `conditional_evolvability`/`autonomy` require positive-definite `G` and throw on a singular one. Descriptive geometry only — NOT a selection-response prediction or a fitting/estimation claim; metrics on an estimated `G` inherit `fit_multivariate_reml`'s caveats; no external (evolqg / Hansen worked-example) comparator yet |
| Random-regression covariance-function descriptors (supplied K_g) (Phase 3) | experimental | `legendre_basis(t, order)` (normalized Legendre `φ_n = sqrt((2n+1)/2)·P_n`, orthonormal on `[-1,1]`), `standardize_covariate(a)` (affine map to `[-1,1]`), and supplied-`K_g` descriptors `rr_genetic_variance` (`v_g(t) = φ(t)ᵀ K_g φ(t)`), `rr_genetic_covariance_surface` (`Φ K_g Φᵀ`), `rr_genetic_correlation_surface` (reuses `genetic_correlation`), and `rr_heritability` (`h²(t)`, supplied scalar or per-point residual) for a random-regression / reaction-norm coefficient genetic covariance `K_g`. Validated by hand-checked closed forms (`φ` at `t = -1,0,1`), trapezoid orthonormality, the documented Kirkpatrick/Meyer fixture numbers (`v_g`, `ρ_g(-1,+1)`, `h²`), `v_g == diag(surface)`, PSD inheritance, correlation diagonal = 1, and guards (`test/runtests.jl`). DESCRIPTIVE, supplied-covariance only — `K_g` is NOT estimated, NO mixed-model equations, NO selection-response claim, NO R model-spec or bridge payload; the supplied-covariance RR MME solve is now `random_regression_mme` and REML estimation of `K_g`/residual is now `fit_random_regression_reml` (the two rows below); the eigen-function (covariance-function) decomposition is now `rr_eigenfunctions(K_g, ts)` — Kirkpatrick (Lofsvold & Bulmer 1990) eigenfunctions `ψ_j(t)=φ(t)ᵀv_j` of a supplied `K_g` via `genetic_pca` (descending eigenvalues, sign-canonicalized eigen-coefficients, evaluated eigenfunctions, variance explained), validated by the spectral reconstruction `ΦK_gΦᵀ = Σ_j λ_j ψ_jψ_jᵀ` (atol 1e-10), eigenfunction orthonormality on `[-1,1]` (trapezoid `∫ψ_iψ_j = δ_ij`, atol 1e-3), the diagonal-`K_g` eigenvalue identity, and the rank-1 variance-concentration reduction (`test/runtests.jl`) — rotation-invariant and descriptive; curve-valued EBV-trajectory PEV and WOMBAT/ASReml/JWAS comparator are deferred (later slices) |
| Supplied-covariance random-regression MME (Phase 3) | experimental | `random_regression_mme(y, X, Phi, Z, Ainv, K_g, sigma_e2)` solves the Henderson MME for the polynomial random-regression animal model with a homogeneous scalar residual: random design `W = face-splitting(Z, Phi)` (record `r` scatters `Phi[r,:]` into animal `a(r)`'s `k`-column block — NOT `kron(Z, I_k)`), genetic precision `Ainv ⊗ inv(K_g)` (animal-outer, coefficient-fastest), returning per-animal `q×k` coefficient matrices. `legendre_design(ts, order)` builds `Φ`. Validated against an INDEPENDENT dense marginal-GLS oracle (`V = W(A⊗K_g)Wᵀ + σ²e I`, β + all coefficients to 1e-8 with an unequal-diagonal PD `K_g` pinning the coefficient ordering) and the degree-0 reduction to `henderson_mme` (scalar animal model) (`test/runtests.jl`). EXPERIMENTAL, dense/validation-scale, SUPPLIED-covariance — `K_g`/`σ²e` NOT estimated (RR REML is now the next row), homogeneous residual only, no R model-spec or bridge payload, no comparator evidence |
| Random-regression REML (estimated K_g + residual) (Phase 3) | experimental | `fit_random_regression_reml(y, X, Phi, Z, Ainv)` ESTIMATES the random-regression coefficient genetic covariance `K_g` (`k×k`) and the homogeneous residual variance `σ²e` of the polynomial reaction-norm animal model by dense REML (log-Cholesky-parameterized NelderMead on the marginal `V = W(A⊗K_g)Wᵀ + σ²e I`, `W = face-splitting(Z, Phi)`); coefficient BLUPs / β come from the GLS BLUP form at the estimate. Validated by deterministic self-consistency (`test/runtests.jl`): the reported REML log-likelihood (full `(n−p)·log(2π)` constant, package-wide scale) matches an INDEPENDENT dense marginal oracle at the estimate (~1e-6) and beats deliberately off-optimum points; the degree-0 (`k=1`) reduction recovers the univariate `fit_sparse_reml` optimum via `K_g[1,1] = 2σ²a` (equal `σ²e` rtol 1e-3, equal log-likelihood ~1e-8 at an interior-σ²a fixture); the fitted coefficient BLUPs / β equal `random_regression_mme` at the estimate (~1e-7). EXPERIMENTAL, dense/validation-scale, REML-only, Gaussian, homogeneous residual, NO permanent-environment term — `K_g` known-truth recovery NOT exercised, no curve-valued EBV-trajectory PEV/reliability, no R `rr()` model-spec or bridge payload, no WOMBAT/ASReml/JWAS comparator evidence |
| Plot-data preparers (plotting layer) | experimental | `rr_eigenfunctions_plot_data` / `rr_genetic_variance_plot_data(...; residual)` / `rr_covariance_surface_plot_data(...; correlation)` re-shape the existing supplied-`K_g` RR descriptors (`rr_eigenfunctions`, `rr_genetic_variance`, `rr_genetic_covariance_surface`, `rr_genetic_correlation_surface`) into the `marker_*_data` `*_plot_data` NamedTuple convention for the R `autoplot.R` ggplot2 + planned `HSquaredMakieExt` drawing layers, carrying honest-status flags (`supplied`, `rotation_invariant`). Validated (`test/runtests.jl`, 18 assertions): delegation-equality to the underlying descriptors, the `propertynames` shape, the honest-status flags, the residual→`h²` path, and delegated non-PSD-`K_g` / `\|t\|>1` guards. Plot-DATA only — NO drawing backend, NO estimation; the first slice of the plotting layer (`docs/design/13-plotting-layer.md`), which keeps `/src` dependency-free with R ggplot2 + a Julia Makie weak-dep extension drawing on top. The G-geometry set (set C) adds `genetic_pca_plot_data(G; n_axes)` and `genetic_correlation_plot_data(G; traits, heritabilities)` — rotation-INVARIANT eigenstructure (eigenvalues + sign-canonicalized principal axes + biplot `loadings_scaled = eigenvectors·√λ`) and the `D⁻¹GD⁻¹` correlation-heatmap data — with a HARD CONTRACT that raw factor-analytic loadings are NEVER accepted or returned (the FA rotation convention); validated (`test/runtests.jl`) by delegation-equality to `genetic_pca`/`genetic_correlation`, the rotation-invariance gate (low-rank `G=ΛΛᵀ` and rotated `ΛQ` give identical eigenvalues), the `:loadings`-absent shape, and `n_axes`/label/PSD guards. Set B adds `variance_components_plot_data(fit; level)` — tidy parallel vectors `(term, estimate, lo, hi, panel, level, interval_method, interval_status, supplied = false)` shaped to drop directly into R's `hs_gg_forest` (variance-component rows carry asymptotic `estimate ± z·SE`, NOT clamped; the `h2` row carries the logit-delta `heritability_interval`, in `(0,1)`; `lo`/`hi` are `NaN` where the interval is unavailable — no fabricated whiskers; `interval_status = "experimental_asymptotic"` (NOT coverage-calibrated) when present; `supplied = false` is the honest-status hinge — these are ESTIMATED). Validated (`test/runtests.jl`): the tidy shape + `propertynames`, estimate-equality to `variance_components`/`heritability`, the REML interval-consistency vs `heritability_interval` (h² row in (0,1)) AND the VC-row normal-Wald whiskers (`estimate ± z·SE`, unclamped), and the ML graceful-degrade (REML-only SE machinery → points-only, no whiskers) |
| Genomic relationship matrix (VanRaden G) | experimental | `genomic_relationship_matrix(markers; method = :vanraden1 \| :vanraden2)` builds VanRaden `G`: method 1 (default) `G = ZZ'/(2Σp(1-p))` and method 2 (standardized markers, equal weight) `G = ZₛZₛ'/m` with `Zₛ[:,j] = (M-2p)/√(2p(1-p))`; method 1 validated on a tiny hand-computed fixture (symmetric, PSD, pinned entries), method 2 against its explicit standardized construction (symmetric, PSD, differs from method 1) with a monomorphic-marker guard; optional per-marker `weights` (method 1, for weighted GBLUP) give `G = Z·diag(w)·Z'/Σw_j·2p_j(1-p_j)`, validated to reduce exactly to method 1 at equal weights, be scale-invariant, symmetric/PSD, and match the explicit weighted construction, with length/positivity/method-2 guards; `G`-construction only — its inverse is `genomic_relationship_inverse`, and the downstream GBLUP / SNP-BLUP fitting that consume it are `fit_gblup`/`fit_gblup_reml` / `fit_snp_blup`/`fit_snp_blup_reml` |
| Regularized genomic inverse (Ginv) | experimental | `genomic_relationship_inverse(G; ridge)` returns the ridge-regularized dense `inv(G + ridge·I)`; tested for the `(G + ridge·I)·Ginv ≈ I` identity, symmetry, a pinned `ridge = 0` hand inverse, a rank-deficient marker-`G` round-trip, and square/PD/negative-ridge guards; it is the `Ginv` consumed by the experimental supplied-variance `fit_gblup`; single-step (`H`-matrix) blending is now exported as `single_step_inverse` / `fit_single_step` |
| Genomic prediction (GBLUP), supplied variance components | experimental | `fit_gblup` wires `genomic_relationship_inverse` into the existing Henderson MME (`Ginv` in the `Ainv` slot); reproduces an independent dense MME solve to ~1e-15 and reproduces pedigree BLUP exactly when `G = A`; the convenience `fit_gblup_reml` additionally ESTIMATES `(σ²_g, σ²_e)` by REML on the `Ginv` spec (pinned equal to generic `fit_ai_reml`); dense `Ginv` (no sparsity benefit), no single-step in this path, no external comparator; reliability/PEV/accuracy reuse the existing extractors with the regularized genomic self-relationship diag(inv(Ginv)) = diag(G)+ridge as the denominator (the ridge perturbs reported reliability), and selinv PEV matches the dense diagonal |
| SNP-BLUP / RR-BLUP (marker effects), supplied variance components | experimental | `fit_snp_blup` (with `centered_markers`) fits marker effects via the existing Henderson MME (`Z = W`, `Ainv = I_m`, `σ²_marker = σ²_g/k`, `k = 2Σp(1−p)`); GEBV `= W·â` matches GBLUP (via the marginal `V`) to ~1e-15 (GBLUP↔SNP-BLUP equivalence); the convenience `fit_snp_blup_reml` additionally ESTIMATES `σ²_g` (= σ̂²_marker·k) and σ²_e by REML and reproduces supplied-variance `fit_snp_blup` at the estimate; VanRaden method-1 / single identity prior only; no external comparator |
| Fixed-effect single-marker scan | experimental | `single_marker_scan` residualizes `y` and centered marker dosages against `X`, then returns marker effects, supplied-variance standard errors, Wald z-scores, chi-square statistics, approximate two-sided Gaussian/Wald p-values, Bonferroni-adjusted p-values, Benjamini-Hochberg q-values, fixed-effect known-variance LOD-equivalent scores, denominators, marker IDs, allele frequencies, and VanRaden scale; `marker_scan_table` prepares row-aligned scan tables in original scan order with allele variances, marker-variance contributions, optional total-variance proportions, optional scan variance components / marker groups when present, and optional `HSMarkerMapSpec` / `HSData` chromosome-position metadata alignment by exact marker ID; `gwas_table`, `qtl_table`, and `eqtl_table` label those already-computed direct scan tables as GWAS/QTL/eQTL output and optionally carry trait or expression-feature metadata; `marker_region_data` prepares one-chromosome or coordinate-window regional scan data from the same row-aligned fields, preserving scan indices, optional variance proportions, and position order; `marker_significance_summary` reports nominal returned-marker-set raw, Bonferroni, and BH significance flags/counts plus top-marker provenance from the same scan fields; `marker_effects` prepares sorted top-marker effect summaries with the same metadata alignment; `marker_variance_explained` prepares sorted marker-level variance-contribution summaries as `2p(1-p) * effect^2`, with optional total-variance proportions and the same marker metadata alignment; `marker_manhattan_data` prepares plot-ready marker IDs, chromosomes, positions, cumulative plot positions, raw p-values, and `-log10(p)` values, with overloads for already-validated marker metadata; `marker_qq_data` prepares QQ plot data with sorted observed p-values, expected uniform order-statistic p-values, sorted marker IDs, observed/expected `-log10` values, and p-floor display handling; `marker_genomic_inflation` computes a lambda_GC diagnostic from returned chi-square values; tiny deterministic tests pin hand-computed effects, p-values, Bonferroni/BH adjustments, LOD-score identity, scan-table order/variance/proportion/metadata/optional-field behavior, GWAS/QTL/eQTL wrapper labels and metadata, regional chromosome-window filtering/order/flank/p-floor behavior, marker-significance raw/Bonferroni/BH flags/counts/top-marker provenance, marker-effect summary ordering/top-N/metadata alignment, marker-variance summary identities/top-N/proportions/metadata alignment, Manhattan defaults, chromosome-offset ordering, marker-map-backed metadata alignment, Manhattan/QQ p-floor handling, QQ order/expected p-values, lambda_GC identities, and covariate-adjusted residualization; opt-in `sim/phase5_marker_scan_recovery.jl` records default-seed (`20260614`) recovery smoke outside CI with top causal marker `m08` and fixed/mixed/LOCO effect relative errors `0.008513` / `0.000349` / `0.019075`; fixed-effect Gaussian screening plus row-aligned scan-table, GWAS/QTL/eQTL labelled table wrappers, marker-effect, marker-variance-contribution, Manhattan/regional-window/nominal-significance/QQ/inflation diagnostics only -- no formula-driven GWAS/QTL/eQTL workflow, no `regional_plot()` or fine-mapping activation, no relatedness/population-structure correction in the fixed-effect path, no expression-wide eQTL scan, no interval-mapping or mixed-model LOD workflow, no plotting backend, no calibrated PVE/model R² claim, no calibrated/correlated-marker genome-wide threshold claim, no calibrated/correlated-marker multiple-testing workflow, no R `marker_scan()` syntax, no bridge payload, and no comparator evidence |
| Supplied-variance mixed-model marker scan | experimental | `mixed_model_marker_scan` forms the dense validation-scale marginal covariance `V = σ²a Z A Z' + σ²e I` from supplied variance components and a supplied relationship precision, then runs marker-by-marker GLS Wald tests conditional on `X`; returns marker effects, standard errors, z-scores, chi-square statistics, approximate two-sided Gaussian/Wald p-values, Bonferroni/BH adjustments, LOD-equivalent scores, GLS denominators, marker IDs, allele frequencies, VanRaden scale, supplied variance components, and `target = :mixed_model_marker_scan`; a post-fit `mixed_model_marker_scan(fit::AnimalModelFit, markers)` convenience (and the fixed-effect `single_marker_scan(fit, markers)`) runs the same scan at a fitted model's `y`/`X`/`Z`, its relationship precision `Ainv`, and its fitted `(σ²a, σ²e)` — pinned to reduce EXACTLY to the explicit-argument calls (#45); deterministic tests pin the reduction to `single_marker_scan` when the random-effect design contributes zero covariance, agreement with an independent GLS calculation on a pedigree-covariance fixture, compatibility with marker-scan-table, marker-effect, and marker-variance summaries, Manhattan/QQ/inflation diagnostic helpers, and input/collinearity guards; opt-in `sim/phase5_marker_scan_recovery.jl` records default-seed (`20260614`) recovery smoke outside CI on a half-sib simulated random-effect design; dense supplied-variance Julia utility only — no marker-scan variance-component estimation, public LOCO workflow, sparse production scan, calibrated p-values, calibrated PVE/model R² claim, plotting backend, R `marker_scan()` syntax, bridge payload, or comparator evidence |
| Supplied LOCO marker scan construction and selection | experimental | `loco_relationship_precisions` constructs dense leave-one-group-out VanRaden-plus-ridge relationship precisions by marker group, and `loco_mixed_model_marker_scan` selects a relationship precision by marker group before running the same dense validation-scale GLS Wald scan as `mixed_model_marker_scan`; deterministic tests pin explicit LOCO precision identities, marker-wise agreement with separate `mixed_model_marker_scan` calls using the corresponding constructed or supplied precision, preserve marker-scan-table, marker-effect, and marker-variance summaries, Manhattan/QQ/inflation diagnostic helper compatibility, and cover missing-group, length-mismatch, empty-map, invalid-dimension, invalid-construction-input, and collinearity guards; opt-in `sim/phase5_marker_scan_recovery.jl` records default-seed (`20260614`) recovery smoke outside CI using constructed VanRaden-plus-ridge LOCO precisions; dense construction and supplied-matrix selection only — no public LOCO defaults, marker-scan variance-component estimation, sparse production scan, calibrated p-values, calibrated PVE/model R² claim, plotting backend, R `marker_scan()` syntax, bridge payload, or comparator evidence |
| Genome-wide significance threshold (permutation null) | experimental | `genome_wide_threshold_from_null(null_max_statistics; alpha, statistic)` turns a SUPPLIED empirical null distribution of per-scan maximum statistics into the `(1 - alpha)` empirical-quantile genome-wide threshold, and `genome_wide_pvalue(observed, null)` gives the add-one empirical genome-wide p-value `(1 + #{null ≥ observed})/(n_null + 1)`; the per-scan maximum comes from `_scan_max_statistic` (max chi-square / max -log10 p) and the quantile from the in-package type-7 `_empirical_upper_quantile` (no Statistics dependency). Using the distribution of the MAXIMUM over jointly-scanned markers makes the threshold correlation/LD-aware (less conservative than Bonferroni under LD). Deterministic CI tests pin the max-statistic extraction, the empirical-quantile interpolation vs hand values, threshold shape + alpha-monotonicity, the add-one p-value identities (never zero), the threshold↔add-one-p relationship (asymptotic agreement only — the type-7 quantile threshold is mildly anti-conservative at small `n_null` and `genome_wide_pvalue` is the exact/conservative rule), non-finite-input guards, and the other guards (`test/runtests.jl`). The RNG-heavy null generation is the opt-in `sim/phase5_threshold_calibration.jl` harness (outside CI): residual-permutation-conditional-on-`X` (a no-op under the committed intercept-only `X`; only approximately exchangeable under a non-trivial design, where Freedman–Lane/ter Braak are exact) re-runs the scan to build the null, draws fresh markers per type-I replicate, contrasts the permutation threshold with Bonferroni, and records a loose empirical type-I smoke. Deterministic machinery + add-one p only — NOT a production genome-wide-significance claim (the #48 gate; the R `gwas()` significance wording stays held until a realistic-LD/design calibration lands), depends on #45 for the post-fit scan, and has no external (PLINK max(T)/GenABEL) comparator |
| Single-step (`H`-matrix) construction + fitting | experimental | `single_step_inverse(Ainv, A, G, genotyped_rows; tau, omega, blend_weight, ridge)` (public wrapper over the validated internal constructor) assembles `H⁻¹ = A⁻¹ + scatter(τGʷ⁻¹ − ωA₂₂⁻¹)` on the genotyped rows, and `fit_single_step` / `fit_single_step_reml` wire it into the Henderson MME (supplied-variance and REML-estimated). Property-validated (reduces to `A⁻¹` when `G = A₂₂`, locality, symmetry, scattered rows, the `A₂₂⁻¹ ≠ (A⁻¹)[g,g]` regression guard, the singular-`G` guard) and the G=A₂₂ reductions reproduce the pedigree animal model exactly (supplied-variance BLUP atol 1e-9) and the pedigree-REML optimum (loglik rtol 1e-6); exported. Blending/τ/ω/ridge are validation-only knobs; dense, validation-scale; no comparator-validated blending defaults, no Mrode Ch.11 fixture, no sparse/APY scaling |
| Genomic REML (variance-component estimation) | experimental | the existing REML optimizers (`fit_ai_reml`, `fit_sparse_reml`, `fit_animal_model(...; target = :ai_reml)`) estimate the genomic variance components on a `Ginv` spec; AI and NelderMead reach the same optimum and `fit_gblup` at the estimate reproduces the REML breeding values; a seeded n=400 simulation recovers σ²g (1.0→0.997) and h² (0.40→0.42); experimental, no external comparator, no production sparse-`G` scaling |
| Mixed-model marker scans and QTL/eQTL | planned | formula vocabulary reserved only; `single_marker_scan`, `mixed_model_marker_scan`, `loco_relationship_precisions`, `loco_mixed_model_marker_scan`, `marker_scan_table`, `gwas_table`, `qtl_table`, and `eqtl_table` are direct Julia utilities, not the public formula-driven `marker_scan()` / `qtl_scan()` path |
| Standard QG effects and custom kernels | planned | vocabulary reserved only; no implementation yet |
| Non-standard inheritance — cytoplasmic / maternal-lineage relationship (Phase 3) | experimental | `maternal_lineage(pedigree)` traces each individual to its maternal founder (single forward pass over the topologically sorted pedigree), and `cytoplasmic_relationship(pedigree)` builds the dense 0/1 same-maternal-line indicator `C` (rank = number of maternal lineages; singular, so it is the relationship for an i.i.d. cytoplasmic random effect / grouping, not a matrix to invert). Validated by a hand-checked multi-lineage fixture (exhaustive same-lineage indicator check, founder self-labelling, all-founder → identity) and a `(ids, sire, dam)` convenience method (`test/runtests.jl`); exported. Construction only — no cytoplasmic-variance fitting claim, no R model-spec; the other non-standard inheritance systems (selfing, clonal, haplodiploid, polyploid) remain planned |
| Non-standard inheritance — self-fertilization (Phase 3) | experimental | `normalize_pedigree(...; allow_selfing = true)` permits `sire == dam`; the existing additive-relationship recursion and Henderson `Ainv` direct rules are already self-correct (a self contributes `A[i,j] = A[P,j]`, `A_ii = 1 + ½(1+F_P)`, Mendelian sampling variance `½(1−F_P)`). Validated against the canonical repeated-selfing inbreeding series (`F = 0, ½, ¾`, `A_ii = 3/2, 7/4`), `A[i,P] = 1`, `pedigree_inverse == inv(A)` for pure-selfing and mixed sexual/selfed pedigrees (`test/runtests.jl`); rejected by default. No public model-spec; selfing-specific variance partitioning not yet a separate claim |
| Non-standard inheritance — clonal / asexual (Phase 3) | experimental | `clonal_relationship(pedigree, clone_of)` builds the dense numerator relationship for a pedigree with clonal ramets: each ramet inherits its genet's whole row/column (`C[i,j] = A[rep(i), rep(j)]`, clone links followed transitively), so clonemates are genetically identical (no Mendelian sampling). Validated on a hand fixture (genet G = P×Q, ramets r1/r2): clonemates and ramet↔genet = 1, ramet inherits the genet's `0.5` to each parent, non-clones unchanged, no-clones → numerator relationship, transitive clone-of-clone chains collapse to one genet, and guards on length / unknown genet / clone cycles / self-clones (`test/runtests.jl`); exported. `C` is rank-deficient (identical rows) — a relationship to use directly, not invert. No public model-spec |
| Dominance relationship matrix (Phase 3) | experimental | `dominance_relationship(pedigree)` builds the dense Cockerham dominance relationship `D[x,y] = ¼(A[sx,sy]A[dx,dy] + A[sx,dy]A[dx,sy])` (unit diagonal, `0` whenever either animal has an unknown parent). Validated on a full-sib / half-sib fixture: full sibs `= 1/4`, half sibs and parent–offspring `= 0`, symmetry, exhaustive agreement with the explicit Cockerham formula against `A`, and a `(ids,sire,dam)` convenience method (`test/runtests.jl`); exported. Off-diagonal is general; the unit diagonal / no dominance-inbreeding correction assumes non-inbred parents (standard textbook case). `D` is the relationship for a dominance random effect (validation-scale, dense); no public model-spec, no dominance-variance fitting claim yet |
| Epistatic relationship matrices (Phase 3) | experimental | `epistatic_relationship(pedigree; kind)` builds Henderson (1985) Hadamard-product epistatic relationship matrices: `:additive_additive` → `A∘A`, `:additive_dominance` → `A∘D`, `:dominance_dominance` → `D∘D`, from the additive and dominance relationship matrices. Validated on the full-sib/half-sib fixture: full sibs `A∘A = 1/4`, `A∘D = 1/8`, `D∘D = 1/16`; half sibs `A∘A = 1/16`, `A∘D = D∘D = 0`; matches the explicit Hadamard products of `A` and `D`; unit diagonal; `(ids,sire,dam)` convenience; invalid `kind` rejected (`test/runtests.jl`); exported. Inherits the dominance non-inbred-parent assumption; validation-scale, dense; no public model-spec, no epistatic-variance fitting claim |
| Supplied-Γ metafounder relationship `A^Γ` and inverses (Phase 1, #53) | experimental | `metafounder_relationship`, `metafounder_inverse`, `metafounder_relationship_inverse`, and `metafounder_inbreeding` (Legarra et al. 2015) build the metafounder-augmented relationship `A^Γ` from a USER-SUPPLIED `m×m` metafounder covariance `Γ` and an `id→founder-group` assignment (`group_of`, aligned to `pedigree.ids` like `clone_of`). `A^Γ` is the existing tabular recursion with the leading `Γ` block seeded and unknown parents remapped to metafounder columns; `metafounder_inverse` is the combined `[metafounders; animals]` Henderson inverse (`inv(Γ)` block + the existing `[1,-½,-½]/d_k` outer products), `metafounder_relationship_inverse` the SEPARATE descriptive animal-only `inv(A^Γ)`. Deterministic checks (`test/runtests.jl`): reduction to `additive_relationship`/`pedigree_inverse`/`inbreeding_coefficients` at `Γ=0`, an independent dense tabular oracle (two-group `Γ`), `A_combined·metafounder_inverse=I` round-trip, shared-metafounder relatedness (off-diag `γ`, diag `1+γ/2`), two-inverse distinctness, `Γ` symmetry/PSD/PD + group-label + remap guards, positive `d_k` (may exceed ½; metafounder `F=γ−1` may be negative). `Γ` is SUPPLIED, NOT estimated (`V1-METAFOUNDER`). Validation-scale and dense (bounded by `max_relationship_cache`); experimental — no Γ estimation, no external comparator (AGHmatrix/nadiv lack metafounder Γ; opt-in BLUPF90 deferred), no R-facing metafounder model-spec or bridge payload, no single-step `H^Γ`; now WIRED into the supplied-variance Henderson MME via `metafounder_animal_model(y, X, Z, pedigree, group_of, Γ, σ²a, σ²e)` (builds `inv(A^Γ)` and solves `henderson_mme`) — validated by the `Γ=0` reduction to the classical animal model (β/EBVs equal `henderson_mme` with `pedigree_inverse`, atol 1e-9), the faithful-wrapper identity, and `Γ≠0` EBV-sensitivity (`test/runtests.jl`); supplied-variance/supplied-Γ only (no Γ/variance estimation), nothing promoted to covered |
| Non-standard inheritance — other systems | planned | haplodiploid, polyploid, sire models, dominance-inbreeding corrections, unknown-parent groups not implemented yet |
| Non-Gaussian Laplace marginal (Phase 6 foundation) | experimental | `laplace_marginal_loglik` computes the Laplace-approximate marginal log-likelihood of the animal model for a `ResponseFamily` (`GaussianResponse`, `PoissonResponse`), integrating `[β` flat`; u ~ N(0, Aσ²a)]` by penalized-IRLS mode-finding + a Gaussian integral at the mode; validated by EXACT reduction to `sparse_reml_loglik` for the Gaussian family (rtol 1e-8, mode == Henderson MME solution), the Poisson mode solving the penalized score equation (‖∇‖<1e-8), and per-family score/weight matching central finite differences; dense/validation-scale, NOT exported, NOT wired into fitting, no VA, no variance-component estimation, no R model-spec, no external comparator |
| Non-Gaussian variational (VA) marginal (Phase 6 foundation) | experimental | `variational_marginal_loglik` maximises the ELBO over a Gaussian variational posterior `q(u)=N(m,S)` (β integrated under a flat prior), with `covariance = :full` profiling `S` to the closed-form `(ZᵀW̃Z + Ainv/σ²a)⁻¹` (keeps pedigree relatedness; a diagonal/mean-field `S` would not be REML-exact); closed-form expected-loglik kernels for Gaussian/Poisson (Poisson via the log-normal MGF); validated by EXACT reduction to `sparse_reml_loglik` for the Gaussian family (rtol 1e-8; ELBO tight, mode == BLUP, `S` == Henderson `H_uu⁻¹`), Poisson ELBO stationarity (‖∇‖<1e-8), and closed-form kernel checks; returns `elbo` (a lower bound on `log p(y)`, tight for Gaussian); dense/validation-scale, NOT exported, NOT wired into fitting, no Poisson marginal-value comparator yet, no R model-spec |
| Fitted non-Gaussian (Laplace/VA REML) (Phase 6) | experimental | `fit_laplace_reml` estimates the variance component(s) by maximising the Laplace marginal or the VA ELBO over `(sigma_a2[, sigma_e2])` (NelderMead / Brent); for the Gaussian family the objective is the EXACT REML log-likelihood, so it recovers `fit_sparse_reml` (marginal loglik rtol 1e-6, VCs rtol 1e-2 on an interior 8-animal fixture, for both the `:laplace` and full-covariance `:variational` objectives); the Poisson fit estimates a positive `sigma_a2` and converges; it also returns the fitted breeding values (the posterior mode, == the BLUP at the fitted variance components for Gaussian); an opt-in harness (`sim/phase6_poisson_recovery.jl`) recovers the Poisson `sigma_a2` on 5/5 predeclared seeds (rel ≤ 0.323, mild Laplace downward bias) with breeding-value recovery correlation 0.81–0.88; a profile-LRT interval (`laplace_reml_interval`) for the Poisson `sigma_a2` inverts the marginal LRT (interior endpoint pinned to the χ²₁ root, clamps on the flat near-zero profile, nests by level; asymptotic, no coverage calibration); **`fit_laplace_reml` and `laplace_reml_interval` are now exported** (experimental public surface; the marginal-loglik kernels and `ResponseFamily` types stay internal), with the Gaussian-limit gate and Poisson interval exercised through the unqualified public names; the fit returns a distinct **`NonGaussianFit`** struct with the same extractor contract as `AnimalModelFit` — `breeding_values(fit)` (→ `BreedingValues(ids, values)`), `variance_components(fit)`, `fixed_effects(fit)`, `EBV(fit)` — so it does NOT collide with the multivariate `NamedTuple` extractors (tested); a `MarginalMethod` dispatch type (`Laplace`/`Variational`, internal — the canonical engine↔R method-name mapping, accepting `:laplace`/`:LA`/`:variational`/`:VA`) and the exported `nongaussian_result_payload(fit)` bridge `NamedTuple` (`engine`/`target = "nongaussian_reml"`/`family`/`n_trials` (Binomial trials denominator, `nothing` otherwise — so a binomial payload is self-describing)/`method = "laplace"|"variational"`/`variance_components`/`fixed_effects`/`breeding_values`/`loglik`/`converged`) now exist, mirroring the `multivariate_result_payload` top-level shape (value-preserving; the dispatch is wired into `fit_laplace_reml`, which now also accepts the `:LA`/`:VA` aliases; deliberately carries NO `heritability` — the shape is family-uniform, h² is left to the consumer); dense/validation-scale, REML-style, no Gaussian/multi-component intervals, no external-comparator (GLLVM.jl/gllvmTMB) evidence, not the public default, the R-facing method-string/family-acceptance contract is pending R-lane coordination, no R model-spec |
| Non-Gaussian Bernoulli/logit family (Phase 6) | experimental | `BernoulliResponse` (logit link) extends the engine to binary 0/1 traits for BOTH the Laplace marginal and the VA ELBO; the VA expected loglik/score/weight kernels use a load-time 20-node Gauss–Hermite rule (the logistic log-partition has no closed-form Gaussian expectation), with the same nodes for all three so score/weight are exactly the η̄-derivatives of the expected loglik; validated by finite-difference kernel checks, the `v→0` reduction to the conditional kernels, a β-fixed independent tensor Gauss–Hermite gate showing the VA ELBO is a valid lower bound and the Laplace value is close, a binary guard, and `fit_laplace_reml(...; family = :bernoulli)` converging for `:laplace` and `:variational`; dense/validation-scale; an opt-in harness (`sim/phase6_bernoulli_recovery.jl`, q=1075) recovers the latent breeding values (EBV correlation 0.565–0.701, 5/5) but the variance estimate `sigma_a2` is downward-biased (0.36–0.81 vs truth 1.0 — the known Laplace-for-binary bias) and reported-not-gated; reachable via the exported `fit_laplace_reml(...; family = :bernoulli)` (the `BernoulliResponse` type itself is internal); no `sigma_a2` calibration/bias-correction, no intervals, no threshold/probit comparator, no R model-spec |
| Non-Gaussian Binomial/logit family (Phase 6) | experimental | `BinomialResponse(n_trials)` generalises `BernoulliResponse` (= `n_trials=1`) to `y` successes out of `n_trials`, for BOTH Laplace and VA (the GH VA kernels scaled by `n_trials`, plus the `log C(n_trials,y)` offset); validated by the exact `n_trials=1`→Bernoulli reduction, finite-difference kernel checks, a β-fixed Gauss–Hermite value gate (VA lower bound + close Laplace), guards, and `fit_laplace_reml(...; family=:binomial, n_trials=m)`; the opt-in harness `sim/phase6_binomial_recovery.jl` (q=345, m=20) recovers `sigma_a2` HARD-gated (rel ≤ 0.175, EBV cor 0.90–0.92, 5/5) — showing the single-trial Bernoulli bias is an information effect; reachable via the exported `fit_laplace_reml(...; family = :binomial, n_trials = m)` (the `BinomialResponse` type is internal); dense/validation-scale, no intervals, no varying per-record `n_trials`, no R model-spec |
| GLLVM-style animal models | planned | Laplace + VA marginals (Gaussian/Poisson/Bernoulli/Binomial) and `fit_laplace_reml` variance-component estimation are the first foundation steps (above); fitted-object/EBV extractors, latent genetic factors, more families, external comparators, and the R model-spec remain planned |

Status words:

- `implemented`: code, tests, and docs exist.
- `experimental`: code exists but public claims are restricted.
- `planned`: roadmap/design only.
- `missing`: not yet designed.
