# Validation Debt Register

| ID | Capability | Status | Required evidence |
| --- | --- | --- | --- |
| V0-LOAD | Package loading | covered | `using HSquared` in test suite |
| V0-CTRL | Control validation | covered | default and invalid control tests, planned backend/accelerator vocabulary tests for `auto`, `cpu`, `threads`, `cuda`, `amdgpu`, `metal`, and `oneapi`, and `backend_info()` diagnostics with execution unavailable; execution dispatch remains planned |
| V0-BACKEND-ALGO-ROADMAP | Backend and algorithm roadmap | covered | Documenter and design notes mirror R head `2c18b30`; roadmap only, no backend execution, GPU agreement, APY, AI-REML, Takahashi selected inversion, GLLVM fitting, or performance evidence |
| V0-FORMULA-STATUS | Formula grammar status diagnostics | covered | `formula_status()` row shape and parsed/reserved/planned status tests; diagnostic only, no parser or fitting expansion |
| V0-VALIDATION-STATUS | Validation status diagnostics | covered | `validation_status()` row shape and evidence/claim-boundary tests; diagnostic only, no comparator execution or fitting expansion |
| V0-GENOMIC-QTL-SYNTAX | Planned genomic/QTL vocabulary | covered | `planned_genomic_qtl_terms()` and planned-not-implemented error tests for `genomic()`, `single_step()`, `markers()`, `marker_scan()`, and `qtl_scan()`; syntax reservation only |
| V0-QG-SYNTAX | Planned standard QG vocabulary | covered | `planned_quantgen_terms()` and planned-not-implemented error tests for `permanent()`, `common_env()`, `maternal_genetic()`, `maternal_env()`, `paternal_genetic()`, `paternal_env()`, `cytoplasmic()`, `imprinting()`, `dominance()`, `epistasis()`, `relmat()`, and `HSquared.precision()`; syntax reservation only |
| V0-PLACEHOLDER | Honest placeholder errors | covered | Phase 0 error tests |
| V1-DATA | HSData in-memory input container | covered | phenotype, pedigree, genotype, expression ID-map tests plus genotype-status diagnostics, marker-map metadata, genotype-marker alignment failure-mode, pedigree-status diagnostics, expression-status diagnostics, annotation-feature diagnostics, environment-key diagnostics, and `data_status()` diagnostic tests in `test/runtests.jl`; no automatic genotype-to-relationship construction, expression/annotation/environment joins, environmental model terms, genotype parsing, Ainv construction from raw data containers, eQTL/omics fitting, or genomic modelling claim |
| V1-PED | Pedigree validation | covered | tiny malformed and valid pedigrees in `test/runtests.jl` |
| V1-AINV | Sparse `Ainv` | covered | hand-checked tiny pedigrees and dense inverse comparison in `test/runtests.jl`; shared out-of-order calf/sire/dam fixture with expected Ainv `[1.5 0.5 -1.0; 0.5 1.5 -1.0; -1.0 -1.0 2.0]` mirrored by R head `fe7e346`; optional R-side `nadiv::Mrode9` / `nadiv::makeAinv()` comparator at R head `369d14a`; **inbreeding computed by Meuwissen–Luo O(n)** (`_meuwissen_luo_inbreeding`, F1/B3) — matches the dense `_numerator_relationship` diagonal EXACTLY on the calf/inbred/selfing fixtures + a deterministic q=2000 inbred pedigree, and scales past the prior 1e4 dense cap (q=12000 in `test/runtests.jl`; q=3×10⁴ confirmed on DRAC; q≥10⁵ scale benchmark opt-in) |
| V1-CSC | Sparse CSC bridge marshalling | covered | zero-based R-slot, one-based Julia-slot, malformed-slot, and direct payload integration tests in `test/runtests.jl` |
| V1-SPEC | Low-level animal model spec validation | covered | dimension, ID, family, and method tests in `test/runtests.jl` |
| V1-LIK | Univariate Gaussian ML/REML likelihood | partial | hand-calculated tiny likelihood tests plus a Mrode9-shaped supplied-variance fixture with pinned ML/REML likelihood values; still needs fitted Mrode and comparator checks |
| V1-SPARSE-REML | Sparse REML likelihood identity | partial | `sparse_reml_loglik()` matches dense REML on tiny fixtures and a Mrode9-shaped supplied-variance fixture; still needs fitted Mrode and comparator checks and does not estimate variance components |
| V1-SPARSE-REML-OPT | Sparse REML validation optimizer | partial | `fit_sparse_reml()` and `fit_animal_model(...; target = :sparse_reml)` optimize the sparse REML objective on tiny fixtures and improve over supplied starting variances, and recover the same REML optimum (variance components, h², loglik, EBVs) as the dense `fit_variance_components(:REML)` on an interior 8-animal fixture, with multi-start and σ²a=0 boundary agreement (`test/runtests.jl`); still needs AI-REML, fitted Mrode and external comparator checks, production sparse diagnostics, and large sparse fixtures |
| V1-OPT | Dense variance-component optimizer | partial | tiny likelihood-improvement tests, and the dense and sparse REML optimizers recover the same optimum on an interior 8-animal fixture (`test/runtests.jl`); still needs Mrode and external comparator checks |
| V1-DENSE-GUARD | Dense validation path size guard | covered | `max_dense_cells` failure and boundary tests for likelihood, optimizer, spec dispatch, and direct payload dispatch in `test/runtests.jl` |
| V1-DENSE-COND | Dense-inverse conditioning caveat | partial | The validation-scale dense estimators that form an explicit dense `A = inv(Ainv)` — the two-effect/repeatability dense REML (`fit_two_effect_reml`, `fit_repeatability_reml`) and the opt-in recovery harnesses (`sim/*recovery*.jl`) — are O(q³) and lose precision when `Ainv` is ill-conditioned (deep or inbred pedigrees). A deep-inbreeding stress test now pins the boundary (`test/runtests.jl`): a selfing chain to `F = 1 − (1/2)^k` (`F₆ ≈ 0.984`) drives `cond(A) ≈ 1.7e3`, yet the DIRECT Henderson `pedigree_inverse` stays EXACT (`max|Ainv·A − I| < 1e-9`, `Ainv == inv(A)`) and the supplied-variance MME solve stays finite — so the sparse Henderson / REML / non-Gaussian Laplace+VA paths (which use `Ainv` / `cholesky(Ainv)` directly, never the explicit inverse) are conditioning-robust, and the caveat is specifically about the dense-`inv(Ainv)` estimators. Still needs an explicit large-pedigree dense-inverse degradation demonstration and the production sparse fitting path (planned) as the structural remedy |
| V1-MME | Supplied-variance Henderson MME solve | partial | sparse `henderson_mme()` matches the shared R/Julia supplied-variance fixture for Ainv, fixed effects, EBVs, fitted values, and `h2`; R head `ca8bce1` also compares Julia against an independent R MME reference when available; a Mrode9-shaped supplied-variance fixture pins Ainv, fixed effects, EBVs, fitted values, PEV, reliability, derived accuracy, and `h2`; a Julia-native published Mrode (2014) Example 3.1 anchor pins the stated animal-model data, `sigma_a2 = 20`, `sigma_e2 = 40`, published EBVs, and the invariant male-minus-female sex contrast; a Julia-native FITTED REML target bundle (`test/fixtures/animal_model_fitted_target/`, #46) serializes the engine's own single-trait fit (VCs/β/EBVs/PEV/reliability/`h²`/`loglik`) with a committed self-consistency test (Henderson MME at the stored VCs reproduces β/EBVs/PEV/reliability/loglik) — this is a serialized confrontation TARGET, not external evidence; the opt-in JWAS.jl probe (#49; JWAS 2.3.6 from `https://github.com/reworkhow/JWAS.jl`) now runs outside CI and records Bayesian/MCMC agreement against that target (`chain_length = 50000`, `burnin = 10000`, seed `20260620`, 20 aligned animals, EBV correlation 0.999, max abs EBV difference 0.1103). This is cross-estimator agreement only, not same-estimand REML parity. Same-estimand fitted-output comparator evidence remains open; still needs estimated-VC published/comparator evidence beyond the existing R-lane anchors and production sparse solve validation |
| V1-SIRE-FIT | Fitted sire-model native target (Mrode Ch.4) | partial | A Julia-native FITTED sire-model REML target (`test/fixtures/sire_model_fitted_target/`, #16): the engine fits its OWN sire model — an ordinary `AnimalModelSpec` with a record→sire incidence `Z` and a sires-only `pedigree_inverse(Ainv)`, estimated by `fit_variance_components(:REML)` (NO new fitting kernel) — and serializes the estimated sire variance `σ²s`, residual `σ²e`, fixed effects, sire EBVs, PEV/reliability (`:selinv`), and the full-REML loglik, with a committed self-consistency test (`test/runtests.jl`): the Henderson MME at the stored variance components reproduces β/EBVs/PEV/reliability (atol 1e-6), `sparse_reml_loglik` reproduces the stored loglik (atol 1e-6, a distinct dense-REML vs sparse-Henderson route), and the sire→narrow-sense identity `h² = 4σ²s/(σ²s+σ²e)` is pinned. HONEST CAVEAT (fixture README + a CI assertion that the generic mapping differs, plus a corrupted-EBV mutation pin): the engine's generic `heritability()` accessor returns `σ²s/(σ²s+σ²e)` — NOT `h²` — for a sire-model spec, so the fixture stores the corrected `h²` itself. Registered in `test/fixtures/comparator_targets.toml` (id `sire_model_fitted_target`). This is a serialized confrontation TARGET, not external evidence — the same-estimand REML sire-model comparator (R-lane nadiv/pedigreemm REML fit of the SAME serialized data, tolerance + versions) is OPEN; the existing R-side published supplied-variance Mrode Example 3.2 anchor (`hsquared/tests/testthat/test-mrode-sire-anchor.R`, σ²s = 5, σ²e = 55) is a supplied-variance anchor, not estimated-VC corroboration; still needs the executed R-lane REML sire-model confrontation and a published estimated-VC sire target |
| V1-DENSE-OUT | EBV/fitted-value/heritability/PEV/reliability/accuracy extractors | partial | `breeding_values(fit)`, `EBV(fit)`, `BLUP(fit)`, and `fitted_values(fit)` are MME-backed at the fit's variance components and match `henderson_mme()` in tests; hand-checked identity-relationship BLUP/fitted-value, variance-ratio, PEV, reliability, and derived-accuracy tests plus Henderson MME, Mrode9-shaped supplied-variance, and published Mrode Example 3.1 supplied-variance EBV fixtures; `variance_components()`, `heritability()`, `prediction_error_variance()`, `reliability()`, and range-checked `accuracy()` also cover supplied-variance `HendersonMMEResult`; still needs estimated-VC textbook/comparator checks for fitted outputs |
| V1-DIAGNOSTICS | Fit diagnostics metadata | partial | `fit_diagnostics()` tests cover stored metadata on `AnimalModelFit` and supplied-variance `HendersonMMEResult`; still needs production sparse diagnostics, gradient/information diagnostics, backend/device diagnostics, and public bridge return tests |
| V1-BRIDGE | R-to-Julia payload parity | partial | R payload builder exists in `hsquared` head `b57b48e`; R head `bacef9c` adds exported `model_spec()` preview of the same formula-to-bridge payload without fitting or Julia execution; R head `36efbf3` lets `model_spec()` and `hsquared()` consume `hs_data()` while preserving the same payload shape; R heads `74eef82` and `39ca990` allow `animal(1 \| id)` only when `hs_data()` supplies pedigree, normalizing to the same explicit-pedigree contract; Julia direct payload target has spec-dispatch, parent-index, `ids`, sparse `Z`, Julia-side `Ainv`, sparse CSC slot tests, and explicit `target = :henderson_mme` supplied-variance dispatch; R head `398e019` records sparse `Z` marshalling through Julia `sparse_csc_matrix()`; R head `00b9e33` records an opt-in supplied-variance Henderson MME bridge target; still needs relationship-object marshalling beyond `Z`, production bridge hardening, and Mrode fitted-output validation |
| V1-RESULT | R-Julia result shape parity | partial | R fitted-object extractor contract exists in `hsquared` head `e543cd7`; Julia `result_payload()` has matching field-name/value tests; R head `c837f2d` normalizes the Julia result into an internal `hsquared_fit`; R head `8235289` enriches the opt-in tiny/local bridge with PEV/reliability from exported Julia extractors; the base `result_payload(::AnimalModelFit)` now carries `prediction_error_variance`/`reliability` as standard `(ids, values)` fields via the `:selinv` selected inverse (machine-precision-equal to dense — `V1-SELINV-PEV`), pinned in the `result_payload` testset (`propertynames` tuple + value parity vs both `:selinv` and `:dense`), so the R unpack (`hs_julia_id_values()`, `hsquared#21`) reads them directly; R head `060988d` adds `fit_diagnostics()` and Julia mirrors it without further widening `result_payload()`; still needs public live-bridge return tests and remains validation-scale (no production large-pedigree reliability claim) |
| V1-HSDATA-BRIDGE | R `hs_data()` to Julia `HSData` parity | partial | R `hs_data()` exists in `hsquared` head `644c75e`; R head `36efbf3` connects `hs_data()` to the R v0.1 parser and unchanged bridge payload; R head `f067cd9` adds genotype-status diagnostics; R head `06cdf59` adds expression-status diagnostics; R head `e7fbb31` adds environment-key diagnostics; R head `87888d9` adds annotation-feature diagnostics; Julia `HSData` mirror exists; still needs live Julia object marshalling tests and explicit tests that genotype/omics/expression/annotation/environment bundle fields are not used automatically |
| V1-REML | Production sparse Gaussian REML optimizer / AI-REML | partial | `fit_ai_reml` recovers the same optimum as the dense/sparse NelderMead optimizers on tiny + simulated fixtures, and its AI matrix matches an independent finite-difference REML Hessian (~8%, `test/runtests.jl`, see `V1-HERIT-CI`); known-truth recovery (R-lane DGP study) and the published gryphon anchor (Wilson 2010) + sommer comparator agreement are validated externally via the bridge; a committed deterministic **420-animal** large-pedigree test (#6) now hardens the sparse path at scale — `fit_ai_reml` converges and its β + EBVs are EXACTLY self-consistent with `henderson_mme` at the fitted variance components, and the selinv PEV/reliability matches the dense diagonal (atol 1e-8) (correctness-at-scale, NO performance claim); **boundary hardening (#6 testset)** adds deterministic gates for (i) a **highly-inbred pedigree** (3-generation selfing chain, F up to 0.875, 8 animals): `fit_ai_reml` converges, β + EBVs are self-consistent with `henderson_mme` (atol 1e-8/1e-7), and `pedigree_inverse == inv(additive_relationship)` holds (atol 1e-8) — Henderson Ainv rule exact under high inbreeding; (ii) a **near-boundary low-h² optimum** (y near-constant): the fit reports finite positive VCs without NaN garbage (converged may be false, that is correct and not asserted either way) and self-consistency still holds at the returned VCs — `fit_ai_reml` now STOPS gracefully (`converged=false`) when 60 step-halvings cannot keep σ positive at the σ²→0 boundary, rather than throwing "could not keep variance components positive" (which was an intermittent CI failure on degenerate single-step fixtures — the finite Newton step grows large relative to a tiny σ; pinned by a deterministic graceful-boundary testset); (iii) **selinv PEV exact at the inbred boundary**: `prediction_error_variance(:selinv) ≈ :dense` (atol 1e-8) and `reliability(:selinv) ≈ :dense` (atol 1e-8) on the inbred pedigree — the Takahashi recursion stays numerically exact under high inbreeding. Correctness-at-boundary only; NO performance claim. **F3 (convergence):** the stopping rule is now scale-invariant — `fit_ai_reml` also stops on the RELATIVE change in the variance components, not only the absolute REML score (which scales with n and is unreachable at large q). Measured on DRAC (opt-in, `sim/drac/f0_scale_benchmark.jl`): q=300k went **35.6 s / non-converged → 2.3 s / converged** (a false-negative convergence status fixed — σ̂² was already at truth), q=100k 2.8 → 0.88 s; regression-guarded in-CI only by the existing `ai.converged` test (the tiny flat 8-animal fixture cannot reproduce the large-n non-convergence — it converges via the score path in ~69 iters regardless). **EM-REML warm-start (opt-in, `em_warmup`, default 0 = byte-identical):** a few monotone, in-bounds EM-REML iterations (the closed form that zeroes the score) before the AI step — verified OPTIMUM-INVARIANT (identical σ̂² across `em_warmup` 0/3/5/15 on a seeded identified fixture, `test/runtests.jl`) and to RESCUE CONVERGENCE from extreme starts (`sim/em_warmstart_benchmark.jl`: a (1e4,1e-2) start is non-converged at `em_warmup=0` but converges at `em_warmup ≥ 3`, q=140 + q=1000), while NOT fixing the σ²→0 / non-identified boundary (still `converged=false`; on the #182 single-step fixture the non-converged estimate is path-dependent — the flag is the honest signal) and NOT altering the boundary contract; ROBUSTNESS only — not a new estimand. Still needs Julia-native known-truth recovery/fitted fixtures and >2-component generalization |
| V1-HERIT-CI | Variance-component covariance + heritability interval | partial | `variance_component_covariance`/`_standard_errors`, `heritability_standard_error`, and `heritability_interval` (logit-delta) build on the AI matrix; AI matrix matches a finite-difference REML Hessian (~8%), interval stays in (0,1) and nests by level, Acklam normal quantile matches known z-values; an opt-in profile-likelihood interval (`method = :profile`) inverts the REML LRT and clamps to (0,1) on flat surfaces, with the root-finder unit-tested and the flat-fixture clamp pinned; a companion `variance_component_interval(fit)` gives the profile-LRT interval for the additive variance component `sigma_a2` itself (profiling `sigma_e2` as nuisance), with self-describing `lower_clamped`/`upper_clamped` flags, pinned in `test/runtests.jl` (recovers the fitted REML optimum at σ̂²a, brackets/nests by level, REML-only + level/method guards); the additive-variance profile-LRT interval `variance_component_interval(fit)` applies UNCHANGED to a genomic GBLUP / supplied-`Ginv` REML fit (`fit_gblup_reml`, or any `:REML` spec with `Ginv` in the `Ainv` slot), since the profiler reads only the spec's precision via `sparse_reml_loglik` — pinned on a genomic `Ginv` fixture in `test/runtests.jl` (recovers the fitted genomic REML optimum at σ̂²a, brackets/nests by level or self-describes via clamp flags, shares the pedigree result shape); on a genomic spec `sigma_a2` is the GENOMIC additive variance CONDITIONAL on the supplied `Ginv` (ridge + centering), so `diag(inv(Ginv)) ≠ 1` and it is NOT a pedigree-scale σ²a (cross-ref `V2-GBLUP`); a parametric-bootstrap cross-check is now provided (C6): `bootstrap_variance_component_interval(fit)` simulates Gaussian responses at the fitted `(β, σ²a, σ²e)` over the supplied relationship, refits each replicate with the same REML estimator, and returns percentile CIs for `sigma_a2`/`sigma_e2`/`h²` via the in-package type-7 `_empirical_upper_quantile` (no `Statistics` dep), with self-describing `n_converged`; deterministic (fixed-seed `rng`), pinned in `test/runtests.jl` (bracketing, point passthrough, level-nesting, byte-identical determinism, the percentile contract, REML-only/level/n_boot/estimator guards); its own COVERAGE is NOT calibrated in CI (the cross-check the delta/profile debt names, not evidence they are correct); still needs large-n COVERAGE calibration of all three methods (delta/profile/bootstrap), multivariate/non-Gaussian bootstrap CIs, ML information, and genomic-σ²a interval coverage calibration + the ridge-sensitivity characterization of the conditional genomic interval |
| V1-HERIT-TCAL | Small-sample t-calibration for Gaussian variance-component and h² intervals | planned | Current Gaussian interval helpers are explicitly asymptotic: `heritability_interval(method = :delta)` and `variance_components_plot_data` use normal-z from `_standard_normal_quantile`, while profile intervals use a χ²₁ cutoff through `q = z * z`; no design-based degrees-of-freedom or `qt` path exists. The C6 parametric bootstrap is finite-sample-aware but remains opt-in and not coverage-calibrated. An opt-in ADEMP checkpoint, df/grid checkpoint, NotebookLM SW/Satterthwaite scout, local freqTLS transfer note, and named-design harness (`sim/phase1_small_sample_interval_calibration.jl`) now exercise z/profile/bootstrap/t-probe labels plus a prototype-only `sigma_a2_satterthwaite_chisq_probe`; the harness is resumable via a replicate-level detail TSV with deterministic per-replicate seeds and diagnostics (`df_eff`, failure reason, boundary flag, bootstrap convergence count). The committed smoke output, 200-rep no-bootstrap triage TSV, and 10-rep/9-bootstrap focused subset are calibration triage only, not promotion evidence. The 2026-06-27 triage found no clean win for residual/family t probes and found the scaled-chi-square probe unstable in low-h² small designs; the bootstrap subset only proves the finite-sample-aware path is wired and resumable. Before any interval-method implementation, default change, or public/R-facing claim: choose and document the animal-model degrees-of-freedom or scaled-reference target (not naive `n - p`, because BLUPs are integrated out; consider Satterthwaite/Welch-style effective-df or scaled-chi-square probes separately from fixed-effect KR/Satterthwaite machinery), add only an explicit prototype/method label, test the quantile/df dispatch and returned interval labels, run a predeclared small-sample coverage simulation comparing delta/profile/bootstrap/prototype-calibrated intervals across pedigree designs, record seeds/versions/coverage/MCSE, and require Fisher + Curie + Rose review. **W1 (2026-06-29) RAN that predeclared coverage simulation** (`sim/drac/phase1_interval_coverage*.sbatch`; ADEMP `docs/dev-log/recovery-checkpoints/2026-06-29-w1-drac-ademp-predeclaration.md`; tiny/small/medium × h²∈{0.1,0.3,0.5,0.7} × {0.90,0.95}, 1000 reps/cell, coverage on `interval_success`; `2026-06-29-w1-c1-interval-coverage*` + `…-medium-coverage*`): the asymptotic σ²a delta/Wald interval **under-covers** at small sample (≈0.84–0.92 vs 0.95) and **improves toward nominal as n grows** at low–moderate h² (e.g. σ²a 0.855→0.925 at h²=0.5, small→medium), with a residual **high-h² narrowness** (~0.87 across all three methods even at q=240); the small-n/low-h² **boundary non-interpretability resolves by q=240**; the methods differ in **failure mode** — the delta/Wald default is **anti-conservative** for σ²a at small n while **profile-LRT is conservative** (near-nominal for h², over-covers σ²a at medium), the safer existing default (see `docs/dev-log/recovery-checkpoints/2026-06-29-w2-profile-lrt-default-proposal.md`); the bootstrap also under-covers. Characterization only — no method implemented, no default change, the df/scaled-reference target still unchosen; row stays `planned`. |
| V1-SELINV-PEV | Sparse selected-inversion PEV/reliability | partial | `prediction_error_variance`/`reliability` accept `method = :selinv` (Takahashi selected inverse of the sparse MME coefficient matrix, kernel adapted from DRM.jl MIT); the selected-inverse PEV diagonal matches the dense MME inverse diagonal to machine precision (well-conditioned validation-scale fits) on a tiny 3-animal pedigree (supplied-variance MME + `AnimalModelFit` paths) and on an 8-animal Mrode9-shaped pedigree with `nfixed = 2` (`AnimalModelFit` path, PEV-diagonal + reliability parity), and on a larger 110-animal 4-generation pedigree (`nfixed = 2`, off-diagonal `Ainv` nnz = 550; PEV-diagonal + reliability parity to rtol 1e-8), `test/runtests.jl`; the fused `selinv_trace_against` (used by `fit_ai_reml` for the `tr(A⁻¹C^uu)` REML score) equals the prior `sum(Ainv .* takahashi_selinv(...)[uu])` to rtol 1e-10 on tiny + 8-animal fixtures and leaves `fit_ai_reml`'s optimum unchanged (`test/runtests.jl`); the `:selinv` PEV/reliability is now the path used to populate the standard `result_payload(::AnimalModelFit)` bridge fields (`V1-RESULT`); **boundary hardening (#6)**: `prediction_error_variance(:selinv) ≈ :dense` (atol 1e-8) and `reliability(:selinv) ≈ :dense` (atol 1e-8) confirmed on a highly-inbred pedigree (selfing chain, F up to 0.875, 8 animals) — selinv stays numerically exact under high inbreeding; correctness-at-boundary only, NO performance claim; still needs production-scale/large-pedigree validation and external comparator checks |
| V1-EBV | Production sparse EBVs/BLUPs, reliability, and PEV | planned | known fitted values, reliability, and prediction error variance against comparator |
| V2-QG | Standard QG effects and custom kernels | planned | model-spec contracts, matrix validation, recovery examples, and comparator checks for permanent/common environment, maternal/paternal, cytoplasmic, imprinting, dominance, epistasis, relmat, and precision terms; current work covers syntax reservation only |
| V3-REPEAT | Repeatability / permanent-environment supplied-variance solve | partial | `repeatability_mme` solves the additive + permanent-environment two-random-effect model at supplied variances; matches an independent marginal-GLS BLUP (~1e-9), reduces to the animal model as `sigma_pe2 → 0`, with a pinned repeated-records fixture in `test/runtests.jl`; still needs the R `permanent()`/repeatability model-spec, multi-effect extractors, and comparator checks |
| V3-REPEAT-REML | Repeatability REML variance-component estimation | partial | `fit_repeatability_reml` estimates (σ²a, σ²pe, σ²e) via the dense two-random-effect REML loglik (returns repeatability `t` and `h²`); loglik reduces to the animal-model REML when σ²pe=0, BLUPs match `repeatability_mme`, the optimum beats a grid (`test/runtests.jl`), and a seeded n=70 sim recovers (1.0,0.6,1.5)→≈(0.94,0.83,1.48) (one-off); the committed opt-in harness `sim/phase3_qg_recovery.jl` recovers the repeatability `t` on 5/5 predeclared seeds (max rel 0.254), but the σ²a/σ²pe split (`h²`) is under-identified at this validation-scale design (2/5 seeds hit the σ²pe→0 boundary) and is reported but ungated. A follow-up identifiability study (`docs/dev-log/recovery-checkpoints/2026-06-18-phase3-repeatability-h2-identifiability.md`) shows a relatedness-richer full-sib design improves the split (q=315/n=1575 recovers `h²` 4/5 within ~0.26 vs the original 2/5 boundary failures) but it is STILL not reliably gateable even at n=1575 (1 seed misses at relh 0.58) — the additive-vs-permanent-environment contrast is intrinsically ill-conditioned at validation scale; `t` remains the gated summary. `repeatability_interval` now gives an asymptotic delta-method CI for `t` (logit scale; observed information from a central finite-difference REML Hessian, `cond ≈ 28` on the validated fixture; throws on a non-PD/flat information), validated for bracketing, the `(0,1)` range, level nesting, and point-estimate match on a seeded fixture (`test/runtests.jl`). Still needs an `h²` interval (deferred — the split is ill-conditioned), an external comparator cross-check, and (for reliable `h²`) a far deeper multi-generation pedigree beyond dense scale |
| V3-TWOEFFECT | General two-random-effect MME (common environment, maternal) | partial | `two_effect_mme` solves two independent random effects (own incidence/relationship-inverse/variance each); common-environment fixture matches an independent marginal-GLS BLUP (~1e-9), and `repeatability_mme` is the `Z2=Z1, A2=I` special case (`test/runtests.jl`); still needs correlated direct–maternal genetic (2×2 G), the R `common_env()`/maternal model-spec, and comparator checks |
| V3-TWOEFFECT-REML | Two-effect REML (common-environment / maternal estimation) | covered | `fit_two_effect_reml` estimates (σ1, σ2, σe²) of the general two-effect model by dense REML; equals `fit_repeatability_reml` in the `Z2=Z1, A2=I` reduction, loglik reduces to the animal-model REML at σ2=0, common-env fit converges (`test/runtests.jl`); the committed opt-in harness `sim/phase3_two_effect_recovery.jl` (q=860 half-sib, 80 common-environment groups assigned INDEPENDENTLY of the pedigree, truth (σ1²,σ2²,σe²)=(1.0,0.5,1.0)) RAN **5/5** recovering ALL THREE components (max rel σ1 0.286, σ2 0.277, σe 0.123) — demonstrating that the earlier additive underestimation was a confounding artifact of the aliased design, not an estimator flaw; the external same-estimand REML comparator is now SATISFIED (point-estimate, single seed) — `blupf90+` 2.60 AI-REML from a NEUTRAL start independently converges (6 rounds) on the seed-20260618 dataset to the engine (σ1²,σ2²,σe²)=(1.1457,0.4793,0.8867) to ~1e-5 (`docs/dev-log/recovery-checkpoints/2026-06-30-v3-two-effect-blupf90-comparator.md`); the PRE-DECLARED bias/MCSE recovery gate now PASSED too (`sim/phase3_two_effect_bias_mcse.jl`, predeclared `41bd18f6` BEFORE running; 48 seeds, 48/48 converged, all three `|bias|≤2·MCSE` — σ1² −2.15%/0.57·MCSE the largest, read as no detectable bias, never "unbiased"). **COVERED** (experimental, validation-scale, opt-in; NOT the public default) via the doc-33 substitutable gate — gate PASS + the `blupf90+` same-estimand comparator; "covered" means the engine correctly implements two-effect REML (animal ~A·σ1² + common-environment ~I·σ2²), NOT small-sample accuracy on σ1². Maintainer **authorized** the partial→covered promotion (G10, 2026-06-30; "Yes merge and keep going"). Standing debt (covered does NOT retire): ratio (σ1/σ2) intervals, correlated direct–maternal genetic (2×2 G), the R-facing model-spec |
| V4-MV | Multivariate Gaussian animal model (supplied covariance) | partial | `multivariate_mme` solves the multi-trait animal model at supplied `G0`/`R0` (Kronecker MME `Ainv⊗G0⁻¹`, block-diagonal residual precision); β and EBVs match independent loop-built MME, marginal-GLS BLUP, `t=1`, and diagonal single-trait reductions in `test/runtests.jl`; unbalanced/missing-trait records are validated vs loop-built MME + marginal GLS; Julia-side accessors/copy-return behavior are tested without changing `result_payload()`; the R lane now records a published supplied-covariance Mrode Example 5.1 BLUP/MME anchor (hsquared `6a1065e`) for fixed effects and animal BLUPs at supplied `G0`/`R0`. Still needs per-trait designs and additional supplied-covariance comparators (covariance estimation now in `V4-MV-REML`) |
| V4-MV-REML | Multivariate REML covariance estimation | covered | `fit_multivariate_reml` estimates `G0`/`R0` by dense REML; deterministic Julia checks cover the `t=1` reduction, target-covariance self-consistency, grid-beating optimum, EBV consistency with `multivariate_mme`, and copy-returning extractors. Recovery evidence includes the predeclared 10-seed run that did not pass (6/10, failure-mode triage: 3 G-only failures plus 1 G+R failure), the Julia 12-seed bias/MCSE and cold-start studies, and the R-lane 100-rep cold-start study (`hsquared/data-raw/multivariate-recovery-study.R`: 100/100 converged; all 9 targets inside bias +/- 2*MCSE; EBV accuracy 0.790/0.742). External evidence now includes one same-estimand `sommer` 4.4.5 comparator leg against `test/fixtures/phase4_multitrait_parity/` (`max abs(dG0)=7.529e-05`, `max abs(dR0)=7.626e-06`, `max abs(dbeta)=1.801e-06`, `max abs(dh2)=6.821e-05`, EBV correlations 1.000/1.000; loglik not compared because additive constants differ), the R-lane published Mrode Example 5.1 supplied-covariance BLUP/MME anchor (hsquared `6a1065e`), and an R-lane `MCMCglmm` 2.36 Bayesian agreement probe (hsquared `dbf97a7`) whose HPD intervals contain the serialized target but which is not REML parity. The BLUPF90/AIREMLF90 packet is preflighted AND now EXECUTED (2026-06-29): `blupf90+` 2.60 AI-REML (Mac x86_64 under Rosetta, statically linked / no MKL) from a neutral non-degenerate start independently converged in 7 rounds to the same fixture optimum — G0/R0 ~1e-5 (BLUPF90 5-sig-fig printout-limited), β ~1e-7, EBV correlation 1.000 both traits (`docs/dev-log/recovery-checkpoints/2026-06-29-v4-blupf90-comparator.md`). Covariance SEs + LRTs are provided for validation-scale inference (`multivariate_covariance_standard_errors`, `covariance_structure_lrt`) with asymptotic/coverage caveats; `genetic_correlation_interval(fit, Y, X, Z, Ainv; method = :delta)` (C2, exported) adds an experimental Fisher-z delta CI per off-diagonal genetic correlation reusing that validated SE (endpoints in (−1,1); cross-checked vs the SE path + tanh form + level-nesting + structured-fit rejection + boundary-throw in `test/runtests.jl`) — a Wald approximation, NOT coverage-calibrated (`:profile` + an opt-in coverage harness are follow-up), and it does NOT extend the covered point-estimate claim. Covered (experimental, validation-scale, opt-in; NOT the public default) via the doc-33 substitutable gate: a pre-declared 48-seed cold-start recovery gate passed (`2026-06-22-mv-reml-substitutable-gate.md` `a7b1f9ad` -> `2026-06-22-mv-reml-predeclared-48seed.md` `24ee2d9c`; all six `|bias| <= 2*MCSE`, EBV ~0.90), read as NO detectable bias and never "unbiased" (G[1,1] -5.7%/1.57*MCSE, REML finite-sample), with one `sommer` same-estimand leg <=8e-5. Standing debt (covered does NOT retire it): the full-unstructured `sommer` leg as a skip-guarded in-suite test, broader recovery (full-sib/3+ traits — now DISCHARGED 2026-06-30, see STILL OWED below), and the deep-inbreeding boundary. (The 2nd same-estimand REML comparator is now DISCHARGED (point-estimate, single fixture) — `blupf90+` 2.60 executed 2026-06-29, independent neutral-start convergence to the fixture optimum ~1e-5; ASReml/DMU/WOMBAT remain optional further legs; `MCMCglmm`/JWAS stay Bayesian-agreement-only, not REML.) **W1 (2026-06-29) characterizes (partial)** the broader-DGP/larger-n portion (Rose-audited, additive evidence — covered status unchanged): an 8-cell × 50-seed cold-start factorial over r_g/h²-balance/records (half-sib, 2-trait; 50/50 converged per cell; **5/8 pass**; R9-clean on `base_inside` → no covered-claim regression; `docs/dev-log/recovery-checkpoints/2026-06-29-w1-c2-v4-broaderdgp-summary.md`) plus a single-DGP q=80→800 sweep (`…-sigma_a2-bias-vs-n.md`) showing the additive-variance downward bias decays monotonically with n (≈−9.7%→−1.0%; the small-q level is itself MCSE-noisy — q≈80 G[1,1] bias is −4.7%/−5.7%/−9.7% across the 50/48/30-seed runs — so the monotone DECAY is the robust signal, not the level), iteration-stable at q=400 (inferred at small-n). Two characterized boundaries: the n-vanishing σ²a bias (becomes MCSE-detectable at larger n, e.g. `size_med`) and a single-record × extreme-r_g identifiability limit (per-seed pass ~2%, EBV ~0.74 — a scope limit, not a bug; `records_1` at base r_g still passes). SCOPE OF VALIDITY (where recovery is claimed to hold): two-trait unstructured REML on the covered half-sib q=80/n=240 design plus the W1 balanced/moderate broader cells (additive vs residual covariance separable with ≥3 records, or a single record only at base r_g; per-trait h² symmetric or mildly asymmetric; r_g ∈ {0.10, 0.42, 0.70}); the two boundaries above are documented scope edges, not covered recovery. **Full-sib design recovery and 3+-trait recovery are now DISCHARGED (2026-06-30, point-estimate, single fixture)** via pre-declared 48-seed cold-start gates that BOTH PASS all four criteria (`docs/dev-log/recovery-checkpoints/2026-06-30-mv-fullsib-48seed.md` + `2026-06-30-mv-3trait-48seed.md`; Curie/Fisher/Mendel pre-run panel; pre-declarations committed `0a39e93a` (full-sib) / `7e4a7d53` (3-trait), panel amendments `4f3fcde6`, all BEFORE the run): full-sib (t=2) 48/48 converged, all 6 `|bias| <= 2*MCSE`, EBV ~0.90, R9-clean (G[1,1] at 1.05*MCSE vs the half-sib covered gate's 1.57*MCSE — the EASIER regime, so a confirmatory pass, NOT a stress test); 3-trait (t=3, the substantive 12-parameter cell) 48/48 converged, all 12 `|bias| <= 2*MCSE`, EBV ~0.90, no off-diagonal MCSE inflation — read as "no detectable bias" (~7% floor), never "unbiased". STILL OWED (the 2nd same-estimand REML comparator is DISCHARGED — point-estimate, single fixture — by the executed `blupf90+` 2.60 leg): the in-suite unstructured `sommer` test (needs live R), the deep-inbreeding boundary |
| V4-FA | Structured / factor-analytic multivariate genetic covariance | partial | `diagonal_covariance`, `lowrank_covariance`, and `factor_analytic_covariance` construct `diag(σ²)`, `ΛΛ'`, and `ΛΛ' + Ψ`; `fit_multivariate_reml(...; genetic_structure = :diagonal \| :lowrank \| :factor_analytic, rank = K)` estimates constrained genetic covariance structures by the existing dense REML core. Tests cover constructor identities/guards, structure metadata, copy-returning `genetic_structure`/`genetic_loadings`/`genetic_uniqueness` accessors, returned loglik equality to `_multivariate_reml_loglik`, PSD/PD covariance properties, constrained loglik ≤ unstructured loglik, and deterministic sign-canonicalization of returned loading columns. The rotation/interpretation convention is now DECIDED (`docs/dev-log/decisions/2026-06-19-fa-rotation-convention.md`): bridge/inference uses rotation-INVARIANT functionals of `G` (eigenstructure / evolvability / `Ψ`), never raw loadings; the sign-only metadata convention remains for returned loadings. Opt-in script `sim/phase4b_structured_covariance_recovery.jl` records seeded low-rank and factor-analytic known-truth recovery on a repeated-record half-sib design, accepts explicit `--seeds` lists, and prints per-case summaries (not CI). `docs/dev-log/decisions/2026-06-14-multivariate-recovery-calibration-protocol.md` defines the seed-count, run-plan, and reporting gate required before any broad multi-seed calibration claim, and the executed structured run did not pass (factor-analytic 8/10 with G-only/G+R failures, low-rank 9/10 with 1 R-only failure, all fits converged). A nested-structure LRT is now available — `covariance_structure_lrt(constrained, full)` compares e.g. `:diagonal`/`:lowrank`/`:factor_analytic` against `:unstructured` (statistic `2Δℓ`, `df` = covariance-parameter difference, χ²`df` p-value), and it **flags rank/PSD-boundary nulls as `boundary = true`** so the reported p-value is treated as asymptotically conservative (the true null is a χ² mixture). SEs are intentionally NOT provided for structured fits (the loadings are rotation-nonidentified); the 2026-06-19 convention confirms SEs are admissible only on rotation-invariant functionals of `G`, never on loadings/individual eigenvectors. Still needs the R-lane-ratified eigenbasis bridge exposure (#42), published multi-trait fixtures, a passing or revised broad multi-seed calibration protocol, and sommer/ASReml/BLUPF90 comparator parity |
| V4-BRIDGE | Multivariate diagonal/unstructured bridge payload | partial | `multivariate_result_payload(result)` returns the bridge-ready `NamedTuple` (`engine`, `target = "multivariate_reml"`, `genetic_structure`, `n_traits`, `traits`, `genetic_covariance`, `genetic_variances`, `residual_covariance`, `genetic_correlation`, `residual_correlation`, `heritability`, `fixed_effects`, `breeding_values`, `loglik`, `n_genetic_params`, `converged`) for a `fit_multivariate_reml` result, mirroring the univariate `result_payload`; tested (`test/runtests.jl`) for the `:diagonal` and `:unstructured` payload shape, `n_genetic_params` (`:diagonal` = t, `:unstructured` = t(t+1)/2), `genetic_variances = diag(G0)`, the diagonal-vs-unstructured LRT `df` identity (interior null), and the `:lowrank`/`:factor_analytic` rejection (loadings rotation-nonidentified). `test/fixtures/structured_covariance_parity/` serializes a deterministic two-trait `:diagonal` target (inputs + fitted diagonal `G0`/`R0`/β/EBVs/`h²`/`loglik`) with CI self-consistency — `multivariate_mme` at the stored diagonal covariances reproduces β/EBVs/`loglik`. `multivariate_result_payload` is bridge-exposed for the rotation-free `:unstructured`/`:diagonal`; the rotation-gated `:lowrank`/`:factor_analytic` are now bridge-exposed via the companion `structured_genetic_payload(result)`, which surfaces only rotation-INVARIANT functionals of G (reconstructed `genetic_covariance`, `genetic_pca` eigenstructure, `mean_evolvability`, and FA `genetic_uniqueness` Ψ) and NEVER the rotation-nonidentified loadings, validated in `test/runtests.jl` (payload omits `genetic_loadings`, eigenstructure matches `genetic_pca(G)`, unstructured/diagonal routed away); still needs external comparator parity against the serialized targets and the R-side activation + LRT wiring (cross-lane #42/#47) |
| V4-EVOLVE | Evolvability / G-matrix geometry | partial | `evolvability`/`conditional_evolvability`/`respondability`/`autonomy`/`variance_along_gradient`/`genetic_pca`/`g_max`/`mean_evolvability` (Hansen & Houle 2008) on a supplied/estimated `G`; hand-checked identities on diagonal, isotropic, and `[3 1; 1 3]` fixtures, `mean_evolvability = tr(G)/t`, `c ≤ e`, the scale-free `isposdef` PD-guard on the inverse metrics (singular `G` throws; a well-conditioned PD `G` at any scale is accepted), scale-relative symmetry/PSD admissibility tolerances (a meaningfully-indefinite large-variance `G` is rejected), the scalar-variance clamp at 0 (no negative "variance" from roundoff on a near-singular admissible `G`), PSD-safety of the rest, and EXPLICIT rotation-invariance `evolvability(ΛΛ') = evolvability((ΛQ)(ΛQ)')` (`test/runtests.jl`); rotation-invariant (operates on `G`, not the loadings) so unaffected by the FA rotation convention. Still needs an external comparator (evolqg / a Hansen worked example), and these are deferred: population-averaged conditional-evolvability/autonomy (random-skewers, no simple closed form), a matrix-of-gradients overload, and mean respondability; metrics on an estimated `G` inherit all `V4-MV-REML`/`V4-FA` caveats |
| C10-LRT | Reusable nested-model LRT helper (df + single-boundary chi-bar correction) | partial | `nested_lrt(loglik_constrained, loglik_full; df, boundary_df)` is a fit-agnostic, EXPORTED likelihood-ratio helper extracted from `covariance_structure_lrt`: statistic `2(ℓ_full − ℓ_constrained)`, dependency-free χ² survival (`_chisq_sf`, no `SpecialFunctions`), and a single-boundary (`boundary_df = 1`) chi-bar-squared 50:50 mixture p-value `0.5·χ²_df + 0.5·χ²_{df−1}` (Self & Liang 1987; Stram & Lee 1994); `boundary_df ≥ 2` falls back to a flagged-conservative naive χ² (`mixture = :chisq_conservative`) and a `full < constrained` (negative) statistic is flagged. Deterministic checks (`test/runtests.jl`) pin the interior χ² reduction, the 50:50 mixture identity and that it is strictly smaller than the naive χ² (s = 2.706 → p ≈ 0.05 vs 0.10), the `boundary_df ≥ 2` conservative fallback, monotonicity/range, df/boundary_df guards, the negative-statistic flag, and bit-for-bit preservation of the existing `covariance_structure_lrt` outputs (which now delegates to it). Still needs a simulated-null calibration that the corrected single-boundary p-values are closer to Uniform(0,1) than naive χ² under a true boundary null (opt-in, non-CI), general multi-boundary (k ≥ 2) chi-bar weights, and bridge exposure. Asymptotic-theory helper only — NOT recovery-/coverage-validated, no model-selection/AIC wrapper, no R bridge |
| V3-RR-DESC | Random-regression covariance-function descriptors | partial | `legendre_basis`/`standardize_covariate` + supplied-`K_g` descriptors `rr_genetic_variance`/`rr_genetic_covariance_surface`/`rr_genetic_correlation_surface`/`rr_heritability` (normalized-Legendre, Kirkpatrick/Meyer convention). Hand-checked: `φ` closed forms at `t = -1,0,1`, trapezoid orthonormality, the documented fixture numbers (`v_g`, `ρ_g(-1,+1)`, `h²`), `v_g == diag(Φ K_g Φᵀ)`, `K_g = I ⇒ v_g = ‖φ‖²`, correlation diagonal = 1 / `|ρ|≤1` / equals `genetic_correlation`, heteroscedastic-residual `h²`, and guards (`test/runtests.jl`). DESCRIPTIVE, supplied-covariance only — `K_g` not estimated. The supplied-covariance RR MME solve `random_regression_mme(y, X, Phi, Z, Ainv, K_g, sigma_e2)` (slice 2) now exists — random design `W = face-splitting(Z, Phi)`, genetic precision `Ainv ⊗ inv(K_g)`, validated against an INDEPENDENT dense marginal-GLS oracle (β + per-animal coefficients to 1e-8, unequal-diagonal PD `K_g` pinning the ordering) and the degree-0 reduction to `henderson_mme` (`test/runtests.jl`); still supplied-covariance, homogeneous-residual, validation-scale. REML estimation of `K_g`/residual (slice 3) now exists — `fit_random_regression_reml(y, X, Phi, Z, Ainv)` (log-Cholesky NelderMead on the marginal `V = W(A⊗K_g)Wᵀ + σ²e I`), validated by an INDEPENDENT dense marginal oracle (loglik at the estimate ~1e-6, beats off-optimum points), the degree-0 (`k=1`) reduction to `fit_sparse_reml` via `K_g[1,1] = 2σ²a` (equal `σ²e`, equal loglik ~1e-8, interior-σ²a fixture), and BLUP/β agreement with `random_regression_mme` at the estimate (~1e-7) (`test/runtests.jl`); see `V3-RR-REML` in `validation_status()`. The eigen-function (covariance-function) decomposition (slice 4) now exists — `rr_eigenfunctions(K_g, ts)` returns the Kirkpatrick (Lofsvold & Bulmer 1990) eigenvalues, Legendre-coefficient eigenvectors, evaluated eigenfunctions `ψ_j(t)=φ(t)ᵀv_j`, and variance explained via `genetic_pca`, validated by the spectral reconstruction `ΦK_gΦᵀ = Σ_j λ_j ψ_jψ_jᵀ` (atol 1e-10), eigenfunction orthonormality on `[-1,1]` (trapezoid `∫ψ_iψ_j=δ_ij`, atol 1e-3), the diagonal-`K_g` eigenvalue identity, the rank-1 variance-concentration reduction, and PSD/shape/`|t|≤1` guards (`test/runtests.jl`); rotation-invariant, supplied-covariance, descriptive. The plotting-layer `*_plot_data` preparers (`rr_eigenfunctions_plot_data`/`rr_genetic_variance_plot_data`/`rr_covariance_surface_plot_data`) now re-shape these descriptors into the backend-free `marker_*_data` NamedTuple convention (honest-status-flagged `supplied`/`rotation_invariant`) for the R `autoplot.R` + Julia `HSquaredMakieExt` drawing layers (`docs/design/13-plotting-layer.md`; `test/runtests.jl`). Deferred (later slices): curve-valued EBV-trajectory PEV/reliability, heterogeneous residual structure + permanent-environment term, known-truth `K_g` recovery harness, the R-facing `rr()` model-spec, and WOMBAT/ASReml comparator parity |
| V-PLOT-DRAW | Julia drawing extension (`HSquaredMakieExt`) | partial | `hsquared_figure(data; kind)` is an exported method-less STUB in `src/plotting_ext.jl`; the drawing METHODS live in `ext/HSquaredMakieExt.jl` (`Makie` weak-dep, `[compat] = "0.24"`), loading only when a Makie backend is in scope. It consumes the set A/B/C/D `*_plot_data` NamedTuples and renders the honest-status behaviors ON the figure (raw whiskers never clamped; `[0,1]` annotated on the h² panel only; supplied/estimated + "NOT coverage-calibrated" subtitle; EBV ± `√PEV` with the validation-scale caveat; the G view is a scree gated on `is_eigenstructure_not_loadings` — a loadings biplot throws `ArgumentError`; a non-PD `G` suppresses %-variance labels; the genetic-correlation **heatmap** (`D⁻¹GD⁻¹`) is gated on `rotation_invariant` and flags low-h² traits in the subtitle; PLUS **set D** — `:manhattan` (chromosome-coloured scatter of cumulative `plot_positions` vs `-log10(p)`, a VISUAL-ONLY Bonferroni guide line, subtitle "nominal Wald p-values, NOT genome-wide calibrated #48") and `:qq` (observed vs expected `-log10(p)` with the y=x uniform-null line, same NOT-calibrated subtitle; λGC INTENTIONALLY not recomputed in the drawing layer — the preparer carries no χ²); PLUS **set A (RR)** — `:rr_variance` (genetic-variance `v_g(t)` + an `h²(t)` panel only when a residual is supplied, subtitle "supplied-K_g descriptive; h²(t) can overstate without a PE term"), `:rr_surface` (covariance/correlation surface heatmap, diverging RdBu centred at 0; colorrange `(-1,1)` for the correlation surface ONLY, data-driven symmetric-about-0 for covariance), and `:rr_eigenfunctions` (covariance-function eigenfunctions with a per-axis variance-explained legend, subtitle "rotation-invariant; signs arbitrary; span-ambiguous under repeated eigenvalues")). **CI exercises only the STUB** (`test/runtests.jl`, 11 assertions: method-less `Function`, `MethodError` until a backend loads across all nine payload shapes — one per kind) — Makie is deliberately OUT of default CI (cost discipline), so the actual draw is **not** a CI-gated claim. The full draw is verified LOCALLY ONLY with CairoMakie 0.15.11 / Makie 0.24.x (all NINE `kind`s return a `Makie.Figure` inferred + explicit; the `_infer_kind` eigenvalues-collision guard holds — `genetic_pca_plot_data` still infers `:g_geometry`, `rr_eigenfunctions_plot_data` infers `:rr_eigenfunctions`; the supplied/NaN/[0,1]-boundary forest, loadings-biplot / non-PD-`G` / non-rotation-invariant guards, the Manhattan/QQ NOT-calibrated subtitles, the reaction-norm single-vs-two-panel split, and the correlation-`(-1,1)` vs covariance-data-driven surface colorrange all fire; figures rasterize to PNG) — `docs/dev-log/check-log.d/2026-06-22-l1-makie-figures.md` + the 2026-06-22 after-task report. DRAWING only — no estimation, no statistical claim is promoted by this row. Still needs: the draw exercised under CI or a reproducible docs-render (so the draw is CI-gated, not just locally attested), R-twin `autoplot.R` ↔ `hsquared_figure` drawing parity, and a Makie-compat-churn guard (a pinned render diff) before any "Julia plots" public claim |
| V2-GRM | Genomic relationship matrix (VanRaden G, methods 1 & 2) | partial | `genomic_relationship_matrix(markers; method = :vanraden1 \| :vanraden2)` builds VanRaden method 1 (centered, single scale — hand-computed tiny fixture, pinned entries) and method 2 (per-marker standardized, equal weight — validated against its explicit `ZₛZₛ'/m` construction, symmetric, PSD, differs from method 1, with a monomorphic-marker guard), and optional per-marker `weights` for weighted GBLUP (method 1; `G = Z·diag(w)·Z'/Σw_j·2p_j(1-p_j)`, validated to reduce exactly to method 1 at equal weights, scale-invariant, symmetric/PSD, with length/positivity/method-2 guards) in `test/runtests.jl`; still needs real marker datasets and AGHmatrix/sommer/BLUPF90 comparator parity (GBLUP wiring and single-step are now covered by `fit_gblup`/`fit_gblup_reml` and `single_step_inverse`/`fit_single_step`) |
| V2-GINV | Regularized genomic inverse (Ginv) | partial | `genomic_relationship_inverse(G; ridge)` returns `inv(G + ridge·I)`; tested for the `(G + ridge·I)·Ginv ≈ I` identity, symmetry, a pinned `ridge = 0` hand inverse, a rank-deficient marker-`G` round-trip, and square/PD/negative-ridge guards in `test/runtests.jl`; still needs GBLUP wiring into the MME, single-step `A`/`G` blending, and external comparator checks |
| V2-GRM-GPU | GPU VanRaden G/Ginv (CUDA extension) | partial | `gpu_genomic_relationship_matrix` / `gpu_genomic_relationship_inverse` (`HSquaredCUDAExt`, `CUDA` weak-dep) are the GPU twins of the CPU VanRaden `G` / `Ginv`, reusing the validated CPU `centered_markers` verbatim (same allele frequencies, VanRaden scale `k`, guards → the SAME estimand by construction); only the dense `W·Wᵀ/k` GEMM (+ `:vanraden2`/weighted) and the ridge `inv(G + ridge·I)` Cholesky inverse run on-device, copied back to a CPU `Matrix{Float64}`. CI gates ONLY the method-less stubs — they throw `MethodError` without CUDA (7 assertions, `test/runtests.jl`); CUDA is OUT of CI (no GPU). The CPU↔GPU agreement (to tolerance) + the honest GPU benchmark are AUTHORED as opt-in cluster scripts (`sim/drac/g1_gpu_genomic.jl` + `sim/drac/g1_tamia.sbatch`) and **RUN on tamia** (4× H100, CUDA 12.6, SLURM job 352612, 2026-06-23): the CPU↔GPU agreement **PASSED** — GPU `G` (vanraden1/2/weighted) + ridge `Ginv` match the CPU twin to **~1e-14** (maxΔ 8.9e-16–8.5e-14; the `(G+rI)·Ginv ≈ I` true-inverse check holds), and the benchmark (end-to-end Float64, n=4000) records the `G` GEMM **1.3×→~5×** (m=2000→40000) and the ridge `Ginv` **~2.7×–2.9×** (one transient dip to 2.1× at m=20000; `sim/drac/results/g1_gpu_352612.tsv`; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-23-g1-gpu-agreement-benchmark.md`) — a machine-specific measurement, not a competitive claim. Still needs: device-resident chaining (G2 GBLUP solve, keeping `G` on-device across ops), a Float32 path (larger speedups), and a real-marker-panel benchmark |
| V2-GBLUP | Genomic BLUP (supplied-variance + REML) | partial | `fit_gblup` matches an independent dense MME assembly (~1e-15) and reproduces pedigree BLUP when `G = A` (~1e-30); the convenience `fit_gblup_reml(y, X, Z, Ginv; initial, target)` now ESTIMATES `(σ²_g, σ²_e)` by REML on the `Ginv` spec and is pinned equal to generic `fit_ai_reml` on that spec (VCs rtol 1e-6, breeding values atol 1e-8) (`test/runtests.jl`); `test/fixtures/genomic_gblup_snpblup_target/` now serializes a real marker panel, supplied allele frequencies, positive-definite VanRaden `G`, `Ginv`, beta, and GEBVs with a CI self-consistency test; hsquared PR #84 (`52507da`) mirrors and consumes that fixture in a Julia-free R recomputation of supplied-frequency `G`, `Ginv`, GBLUP MME, and SNP-BLUP route agreement; still needs AGHmatrix/sommer/BLUPF90/JWAS same-estimand comparator parity against that target, sparse/APY `G`, and broader marker-panel evidence; genomic reliability/PEV use the diag(G) denominator (pinned) and selinv PEV matches dense to machine precision; the additive-variance profile-LRT interval `variance_component_interval(fit)` applies UNCHANGED to a `fit_gblup_reml` / supplied-`Ginv` fit (see `V1-HERIT-CI`), reporting the GENOMIC `sigma_a2` CONDITIONAL on the supplied `Ginv` (`diag(inv(Ginv)) ≠ 1`, not a pedigree-scale σ²a) — coverage calibration + ridge-sensitivity of that conditional interval are open |
| V2-SNPBLUP | SNP-BLUP / GBLUP↔SNP-BLUP equivalence (supplied-variance + REML) | partial | `fit_snp_blup` GEBV `= W·â` equals GBLUP GEBV (via the marginal `V`) to ~1e-15 for `n<m` and `n>m`, with `k`, marker effects, and predictions pinned; the convenience `fit_snp_blup_reml(y, X, markers; initial, target)` now ESTIMATES `σ²_g` (= σ̂²_marker·k) and σ²_e by REML on the centered-marker spec and reproduces supplied-variance `fit_snp_blup` at the estimated variances (GEBV/marker-effects atol 1e-8) (`test/runtests.jl`); the #49 target fixture serializes supplied allele frequencies, marker effects, SNP-BLUP GEBVs, and GBLUP/SNP-BLUP route agreement; hsquared PR #84 consumes that route-agreement target without live Julia; still needs weighted/standardized-marker variants and JWAS/sommer/BLUPF90 comparator parity |
| V5-MARKER-FIXED | Fixed-effect single-marker scan | partial | `single_marker_scan` residualizes `y` and centered marker dosages against `X`, then returns marker effects, supplied-variance standard errors, Wald z-scores, χ² statistics, approximate two-sided Gaussian/Wald p-values, Bonferroni-adjusted p-values, Benjamini-Hochberg q-values, and fixed-effect known-variance LOD-equivalent scores. `marker_scan_table` prepares deterministic row-aligned scan tables in original scan order with allele variances, marker-variance contributions, optional total-variance proportions, optional scan variance components / marker groups when present, and optional `HSMarkerMapSpec` / `HSData` chromosome-position alignment by exact marker ID. `gwas_table`, `qtl_table`, and `eqtl_table` label those already-computed direct scan tables as GWAS/QTL/eQTL output and optionally carry trait or expression-feature metadata. `marker_region_data` prepares deterministic one-chromosome or coordinate-window regional scan data from those row-aligned fields, preserving scan indices, optional marker-variance proportions, position order, and p-floor display values. `marker_significance_summary` prepares deterministic nominal returned-marker-set raw, Bonferroni, and BH significance flags/counts plus top-marker provenance from the same scan fields. `marker_effects` prepares deterministic sorted top-marker effect summaries with the same metadata alignment. `marker_variance_explained` prepares deterministic sorted marker-level variance-contribution summaries as `2p(1-p) * effect^2`, with optional total-variance proportions and the same marker metadata alignment. `marker_manhattan_data` prepares plot-ready marker IDs, chromosomes, positions, cumulative plot positions, raw p-values, and `-log10(p)` values; it can consume already-validated marker metadata. `marker_qq_data` prepares QQ plot data with sorted observed p-values, expected uniform order-statistic p-values, sorted marker IDs, observed/expected `-log10` values, and p-floor display handling. `marker_genomic_inflation` computes a genomic-control-style lambda_GC diagnostic from returned χ² values. Tests pin hand-computed intercept-only effects (`17/14`, `0.5`), denominators (`2.8`, `4.0`), supplied-variance SEs, z/χ² consistency, p-values, Bonferroni/BH adjustments, LOD-score identity, scan-table order/variance/proportion/metadata/optional-field behavior, GWAS/QTL/eQTL wrapper labels and metadata, regional chromosome-window filtering/order/flank/p-floor behavior, marker-significance raw/Bonferroni/BH flags/counts/top-marker provenance, marker-effect summary ordering/top-N/metadata alignment, marker-variance summary identities/top-N/proportions/metadata alignment, Manhattan defaults, chromosome-offset ordering, marker-map-backed metadata alignment, Manhattan and QQ p-floor handling, QQ order/expected p-values, lambda_GC median-χ² identities, covariate-adjusted residualization against an independent calculation, default/supplied marker IDs, and guardrails for invalid residual variance, marker metadata lengths/values, duplicate or mismatched marker-map IDs, invalid p-value adjustment inputs, invalid inflation, marker-significance-summary, marker-scan-table, GWAS/QTL/eQTL wrapper metadata, marker-region, marker-effect-summary, and marker-variance-summary inputs, collinear markers, rank-deficient `X`, row-count mismatch, and malformed QQ inputs. Opt-in `sim/phase5_marker_scan_recovery.jl` records default-seed (`20260614`) recovery smoke for the fixed, supplied-variance mixed, and supplied LOCO direct scans outside CI with top causal marker `m08` and effect relative errors `0.008513` / `0.000349` / `0.019075`. Still needs formula-driven mixed-model marker scans, public LOCO workflows, expression-wide eQTL scans, regional plotting / fine mapping, broad multi-seed calibration, calibrated mixed-model p-values, calibrated/correlated-marker genome-wide thresholds, calibrated PVE/model R² claims, interval-mapping or mixed-model LOD workflows, plotting backends, advanced/correlated-marker multiple-testing workflows, external comparator parity, and R-facing `marker_scan()` syntax |
| V5-MARKER-MIXED | Supplied-variance mixed-model marker scan | partial | `mixed_model_marker_scan` forms dense `V = σ²a Z A Z' + σ²e I` from supplied variance components and a supplied relationship precision, then runs marker-by-marker GLS Wald tests conditional on `X`. Tests pin reduction to `single_marker_scan` when the random-effect design contributes zero covariance, agreement with an independent GLS calculation on a pedigree-covariance fixture, compatibility with marker-scan-table, marker-effect, and marker-variance summaries, Manhattan/QQ/inflation helpers, and guards for variance components, dimensions, positive-definite `Ainv`, rank-deficient `X`, and marker collinearity under the supplied covariance. Opt-in `sim/phase5_marker_scan_recovery.jl` records default-seed (`20260614`) supplied-variance mixed marker-signal recovery outside CI on a half-sib simulated random-effect design. A post-fit `mixed_model_marker_scan(fit::AnimalModelFit, markers)` / `single_marker_scan(fit, markers)` entry point runs the scan at a fitted model's design + relationship precision + fitted variance components, pinned to reduce EXACTLY to the explicit-argument scans (#45); `marker_scan_result_payload(scan)` now returns the stable bridge shape and `test/fixtures/marker_scan_parity/` serializes a deterministic mixed scan payload for R-lane parity without live Julia. The returned p-values remain UNcalibrated (gate #48). Still needs marker-scan variance-component estimation, sparse production scanning, calibrated mixed-model p-values, calibrated PVE/model R² claims, interval-mapping or mixed-model LOD workflows, plotting backends, advanced/correlated-marker multiple-testing workflows, external comparator parity, map-annotated GWAS/QTL/eQTL table workflow activation, and R-facing `marker_scan()` syntax |
| V5-MARKER-LOCO | Leave-one-group-out marker scan construction and selection | partial | `loco_relationship_precisions` constructs dense leave-one-group-out VanRaden-plus-ridge relationship precisions from marker groups, and `loco_mixed_model_marker_scan` selects a supplied relationship precision by marker group before running the same dense GLS scan as `mixed_model_marker_scan`. Tests pin explicit LOCO precision identities, marker-wise agreement with separate `mixed_model_marker_scan` calls using the corresponding constructed or supplied precision, preserve marker-scan-table, marker-effect, and marker-variance summaries, Manhattan/QQ/inflation helper compatibility, and guard missing groups, marker-group length mismatches, empty precision maps, invalid precision dimensions, invalid LOCO construction inputs, and marker collinearity under the selected covariance. Opt-in `sim/phase5_marker_scan_recovery.jl` records default-seed (`20260614`) supplied LOCO marker-signal recovery outside CI using constructed VanRaden-plus-ridge LOCO precisions. Still needs public LOCO workflow defaults, marker-scan variance-component estimation, sparse production scanning, calibrated mixed-model p-values, calibrated PVE/model R² claims, interval-mapping or mixed-model LOD workflows, plotting backends, external comparator parity, and R-facing `marker_scan()` syntax |
| V5-MARKER-THRESHOLD | Permutation-calibrated genome-wide threshold | covered (scoped) | **COVERED (SCOPED, validation-scale, opt-in; NOT public default — public-covered FITTING = 1)** for genome-wide significance via the EXACT per-dataset add-one permutation rule (`genome_wide_marker_scan` / R `gwas(genome_wide = TRUE)`), type-I-CONTROL only, fixed-effect/intercept-only, on the tested LD designs — doc-33 substitutable gate (validation #203/#204 + production REBUILD #207 type-I gates PASSED, PLINK max(T) #205 executed, R activation hsquared #113 live-verified, Rose PROMOTE; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-30-v5-covered-promotion.md`). FENCED OUT (NOT covered): mixed-model/LOCO genome-wide null, power/coverage, broader-LD/covariate-adjusted calibration, the `(1-alpha)` quantile rule + fixed-null-reuse shortcut, map-annotated formula-level scan API. STANDING DEBT (covered does NOT retire it): 2nd external comparator (GCTA/statgenGWAS), mixed-model calibration, broader-LD/covariate-adjusted + coverage characterization, #45 dependency. EVIDENCE TRAIL: `genome_wide_threshold_from_null(null; alpha, statistic)` (= the `(1-alpha)` empirical quantile of supplied per-scan maxima) and `genome_wide_pvalue(observed, null)` (= add-one empirical p `(1+#{null>=obs})/(n+1)`), with `_scan_max_statistic` (max χ²/-log10 p) and the in-package type-7 `_empirical_upper_quantile`. Correlation/LD-aware because it uses the distribution of the MAXIMUM over jointly-scanned markers, but finite empirical thresholds are not guaranteed to be below Bonferroni. Deterministic CI tests pin max-statistic extraction, quantile interpolation vs hand values, threshold shape + alpha-monotonicity, add-one p identities (never zero), the threshold↔add-one-p relationship (they agree only asymptotically; the type-7 quantile threshold is mildly anti-conservative at small `n_null`, e.g. add-one p `6/101 ≈ 0.059` at the `n=100` threshold, converging to `alpha` by `n=1000`; `genome_wide_pvalue` is the exact/conservative decision rule), non-finite-input guards, and the other guards (`test/runtests.jl`). Calibration-property evidence (deterministic, #7 2026-06-20) verifies the known analytic type-7 quantile, add-one p formula, strict monotonicity, and the finite-null threshold↔p gap. The opt-in `sim/phase5_threshold_calibration.jl` harness now defaults to fixed-marker-panel type-I smoke, keeps a fresh-panel sensitivity mode, supports comma-separated multi-seed runs, and can write TSV evidence. Fixed-panel mini-smoke evidence (2026-06-21, `docs/dev-log/recovery-checkpoints/2026-06-21-threshold-fixed-panel-calibration.tsv`): 3 seeds (`n=240`, 120 correlated markers, 300 permutations, 200 type-I replicates, alpha=0.05) gave empirical type-I 0.015/0.065/0.050 (mean 0.043, range 0.015-0.065); threshold-vs-Bonferroni direction was mixed (1/3 below). hsquared PR #83 (`1c239ec`) records local marker-scan comparator/threshold tooling blockers (PLINK/plink2/GEMMA/GCTA/BOLT-LMM/SAIGE scripts absent; GenABEL/qvalue and other R scan stacks absent except `sommer`). This is validation-scale smoke plus blocker evidence only. A PRE-DECLARED calibration gate (2026-06-30, `sim/phase5_qtl_threshold_gate.jl`; predeclaration committed BEFORE the run, no post-hoc relaxation; 20 seeds, `n=300`, `m=200`, nperm=2000, alpha=0.05) then **FAILED** `|mean type-I − alpha| ≤ 2·MCSE` in the **anti-conservative** direction — mean empirical type-I **0.069** (bias +0.019 = 2.42·MCSE; per-seed range 0.022-0.131) — confirming the `(1-alpha)` empirical-quantile threshold is too permissive at finite nperm. The calibrated path is the conservative add-one `genome_wide_pvalue` rule (exact by construction) and/or much larger nperm; banked NEGATIVE (stays `partial`, `gwas()` held; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-30-v5-qtl-threshold-gate-predeclaration.md`). A PRE-DECLARED ADD-ONE gate (2026-06-30, `sim/phase5_qtl_addone_gate.jl`; predeclaration committed `d26896c9` BEFORE the run — the constructive follow-up; SAME NULL DGP/design, only the accept/reject rule swapped to the add-one `genome_wide_pvalue ≤ alpha`; 20 cold seeds 20260920..20260939) then **PASSED** the one-sided-upper (not-anti-conservative) criterion `mean type-I − alpha ≤ 2·MCSE` — mean empirical type-I **0.0543** (excess +0.0043 ≤ 2·MCSE 0.0170; per-seed range 0.014-0.179) — confirming the conservative add-one rule controls family-wise type-I at alpha (the exact-permutation-test construction) on byte-identical nulls where the quantile rule failed. This DISCHARGES the type-I-control calibration leg for the add-one rule at this design (checkpoint `docs/dev-log/recovery-checkpoints/2026-06-30-v5-qtl-addone-gate-predeclaration.md`); the row STAYS `partial`, NOTHING promoted, `gwas()` wording stays held — covered still owes the external comparator + broader designs + R activation. A PRE-DECLARED BROADER-DESIGN SWEEP (2026-06-30, `sim/phase5_qtl_addone_design_sweep.jl`; predeclaration committed `fa159abc` BEFORE the run) then HARDENED the leg into a design GRID — the same one-sided-upper criterion across (n,m) ∈ {(200,100),(300,200),(500,300)} (10 cold seeds each, 20260940..20260969) **PASSED at ALL THREE** points (mean type-I 0.068/0.058/0.061, each within 2·MCSE of alpha), so the add-one rule controls family-wise type-I across small/medium/larger designs (checkpoint `docs/dev-log/recovery-checkpoints/2026-06-30-v5-qtl-addone-design-sweep-predeclaration.md`); STILL `partial`, NOTHING promoted. The broader-designs item is thereby substantially addressed for type-I CONTROL. An EXTERNAL COMPARATOR (2026-06-30, maintainer-authorized, `comparator/prepare_plink_threshold.jl`) then DISCHARGED the NEEDS-EXTERNAL leg: **PLINK 1.9 `--assoc --mperm 2000`** (EMP2 = max(T) family-wise add-one p, an INDEPENDENT implementation — estimated-variance OLS + own RNG) reproduces `genome_wide_pvalue` across 5 datasets β=0→0.8 on the same n=300/m=200 LD DGP — SAME top marker in all 5, genome-wide p agreeing to within MC error (0.717 vs 0.726; 0.402 vs 0.397; add-one floor 0.0005 ×3), per-marker χ²/T² cor 0.998-1.000 (checkpoint `docs/dev-log/recovery-checkpoints/2026-06-30-v5-plink-maxt-comparator.md`); STILL `partial`, NOTHING promoted. A PRODUCTION-SCALE campaign (2026-06-30, Totoro) then characterized type-I control at realistic marker counts AND surfaced an honest refinement: the pre-declared production REUSE gate (`sim/phase5_qtl_production_calibration.jl`; (n,m) up to (2000,10000), 50 seeds) **FAILED** (means 0.056-0.061; banked NEGATIVE), a verified diagnostic (`sim/phase5_reuse_vs_rebuild_diagnostic.jl`) isolated the cause as the SIMULATION's fixed-null-reuse shortcut (REUSE 0.0642 vs REBUILD 0.0478), and a pre-declared production REBUILD gate for the EXACT per-dataset rule real `gwas()` uses (`sim/phase5_qtl_rebuild_production_gate.jl`, m=2000) **PASSED** (means 0.0542/0.0504, at α). REFINEMENT: the #203/#204 validation gates use the SAME reuse shortcut, so their "type-I at α" is the reuse-procedure's (mildly anti-conservative ~0.006-0.016, below their MCSE); the EXACT rule is well-calibrated (theorem + diagnostic + REBUILD PASS). The R `gwas(genome_wide = TRUE)` activation (hsquared #113) then LIVE-verified the exact rule element-wise against the engine (R CMD check 0/0/0), completing the substitutable gate → the SCOPED covered flip (2026-06-30). |
| V2-SSHINV | Single-step (`H`-matrix) construction + fitting | partial | `single_step_inverse(Ainv, A, G, genotyped_rows; ...)` (public) assembles `H⁻¹ = A⁻¹ + scatter(τGʷ⁻¹ − ωA₂₂⁻¹)`, and `fit_single_step` / `fit_single_step_reml` solve the single-step MME (supplied-variance / REML). Validated: reduces to `A⁻¹` when `G = A₂₂`, locality/symmetry, the pinned `A₂₂⁻¹ ≠ (A⁻¹)[g,g]` distinctness (1.833 vs 2.5), scattered rows, singular-`G` guard, and an independent dense-H oracle verifies `_single_step_Hinv * H ≈ I`, `H * _single_step_Hinv ≈ I`, and `inv(H) ≈ _single_step_Hinv` for trailing and scattered genotyped rows. The G=A₂₂ fit reductions reproduce the pedigree animal model exactly (BLUP atol 1e-9) and the pedigree-REML optimum (loglik rtol 1e-6) (`test/runtests.jl`). The supplied-Γ bridge primitive `metafounder_single_step_inverse(pedigree, group_of, Γ, G, genotyped_rows; ...)` builds `H^Γ⁻¹` by replacing `A` with `A^Γ`; `fit_metafounder_single_step` / `fit_metafounder_single_step_reml` delegate through that precision. Validated: `Γ=0` reduction to ordinary single-step, equality to manually building `A^Γ` and calling `single_step_inverse`, symmetry, supplied-variance wrapper identity, REML reduction at `Γ=0`, nonzero-Γ REML bridge payload/diagnostics smoke (`result_payload`, `fit_diagnostics`, PEV/reliability IDs, and selinv-vs-dense parity), and size/singular-`G` guards. Still needs comparator-validated blending/tuning defaults (AGHmatrix::Hmatrix/BLUPF90), published Mrode Ch.11 H/H⁻¹ numbers or equivalent external target, external metafounder single-step comparator evidence, R-facing metafounder/single-step formula payload, and sparse/APY scaling |
| V2-GREML | Genomic REML variance-component estimation | covered | the existing REML optimizers estimate genomic variance components on a `Ginv` spec — `fit_ai_reml` and `fit_sparse_reml` reach the same optimum and `fit_gblup` at the estimate reproduces the REML breeding values; the named one-call convenience is `fit_gblup_reml` (genomic) / `fit_snp_blup_reml` (marker, returns `σ²_g = σ̂²_marker·k`) (`test/runtests.jl`); a seeded n=400 sim recovers σ²g (1.0→0.997) and h² (0.40→0.42, one-off); the `blupf90+` 2.60 same-estimand REML leg is **EXECUTED** (2026-06-30, point-estimate / single fixture / same-`Ginv` isolation — BLUPF90 is handed the engine's own `Ginv` via `user_file`, so it isolates the REML estimator from G-construction; from a NEUTRAL start it converges in 6 rounds to the `fit_gblup_reml` optimum, σ²g/σ²e/h² agreeing to ~1e-5 = the BLUPF90 5-sig-fig printout floor; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-30-v2-genomic-blupf90-comparator.md`); the committed PRE-DECLARED bias/MCSE recovery gate (`sim/phase2_genomic_reml_recovery.jl`; predeclaration committed BEFORE the run; 48/48 seeds, `|bias| ≤ 2·MCSE` on σ²g/σ²e/h²; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-30-v2-genomic-recovery-gate-predeclaration.md`) now PASSES — with the BLUPF90 leg this clears the doc-16 G11 bar (validation-scale, opt-in; real Rose audit → PROMOTE; maintainer G10). Still owed (covered does NOT retire): a `sommer`/`rrBLUP` 2nd REML leg, broader N/M/h² designs, and VanRaden G-construction parity (`V2-GRM`). Covered = the supplied-`Ginv` REML ESTIMATOR only; the public-default fitting surface stays v0.1 Gaussian |
| V5-GBLUP | Genomic and single-step models | planned | JWAS/sommer/BLUPF90 style comparator checks; current work covers syntax reservation only |
| V5-QTL | Marker scans and QTL/eQTL | planned | marker-map validation, estimand definition, genome-wide multiple-testing calibration, and comparator/simulation evidence; current work covers syntax reservation only |
| V6-LAPLACE | Non-Gaussian Laplace marginal (Phase 6 foundation) | partial | `laplace_marginal_loglik` reduces EXACTLY to `sparse_reml_loglik` for the Gaussian family (rtol 1e-8; mode == Henderson MME solution), the Poisson Newton mode solves the penalized score equation (‖∇‖<1e-8), and the per-family score/weight match central finite differences (`test/runtests.jl`); dense/validation-scale; input guards added (`GaussianResponse` requires `sigma_e2 > 0`, `PoissonResponse` requires non-negative integer counts, and `loglik`/`elbo` return `NaN` with `converged = false` on non-convergence so a non-mode value is never mistaken for a valid marginal); the VA (`:VA`) sibling now exists (see `V6-VA`); still needs the Laplace `gradient_norm`-at-returned-mode nit, variance-component estimation, a fitted non-Gaussian/GLLVM path, recovery + external-comparator (GLLVM.jl/gllvmTMB) evidence, and an R model-spec |
| V6-VA | Non-Gaussian variational (VA) marginal (Phase 6 foundation) | partial | `variational_marginal_loglik` (full-covariance, β integrated under a flat prior) reduces EXACTLY to `sparse_reml_loglik` for the Gaussian family (rtol 1e-8; ELBO tight, mode == BLUP, `S` == Henderson `H_uu⁻¹`), the Poisson ELBO is stationary at the optimum (‖∇‖<1e-8), and the closed-form expected-loglik/weight kernels match their definitions (`test/runtests.jl`); designed and adversarially vetted by the team (Gauss/Noether/Curie re-derived + verified the Laplace base to ~3e-15; Karpinski/Kirkpatrick/Fisher specified full-`S` + the β-integration that makes the Gaussian ELBO tight); dense/validation-scale; a β-fixed tensor Gauss–Hermite quadrature of the true Poisson marginal confirms the VA ELBO is a valid LOWER BOUND (and the Laplace value is close, documenting its gap) (`test/runtests.jl`); a `:diagonal` mean-field option exists (closed-form `S = diag(1 ./ diag H_uu)`) and is verified looser than full covariance (`ELBO_full ≥ ELBO_diagonal`); still needs variance-component estimation, a fitted GLLVM path, recovery + external-comparator (GLLVM.jl/gllvmTMB) evidence, and an R model-spec |
| V6-FIT | Fitted non-Gaussian (Laplace/VA REML) (Phase 6) | partial | `fit_laplace_reml` maximises the Laplace/VA marginal over the variance components; for the Gaussian family it recovers `fit_sparse_reml` (marginal loglik rtol 1e-6, VCs rtol 1e-2 — both the `:laplace` and full-covariance `:variational` objectives, since each equals the exact REML loglik for Gaussian) and the Poisson fit returns a positive `sigma_a2` and converges, and the fit returns the breeding values (== the BLUP at the fitted variance components for Gaussian) (`test/runtests.jl`); the opt-in harness `sim/phase6_poisson_recovery.jl` recovers the Poisson `sigma_a2` on 5/5 predeclared seeds (rel ≤ 0.323, mild Laplace downward bias) with breeding-value recovery correlation 0.81–0.88; dense/validation-scale; an opt-in profile likelihood-ratio interval (`laplace_reml_interval`) inverts the marginal LRT `2·(ℓ̂−ℓ(σ²a))=χ²₁,level` for the single-component `sigma_a2` across `:poisson` / `:bernoulli` / `:binomial` (`:binomial` takes the same scalar-or-per-record `n_trials` contract) — for Poisson the interior upper endpoint is pinned to the χ²₁ root (`dev(upper)≈3.841` at 95%, `2.706` at 90%) with a lower clamp; a Binomial is two-sided interior when `σ̂²a` is clear of zero (`dev(upper)≈dev(lower)≈3.841`/`2.706`, nests) but lower-clamps when `σ̂²a` is small — fixture-dependent, not a family property; a binary Bernoulli profile is flat so BOTH endpoints clamp (degenerate). The interval returns `lower_clamped`/`upper_clamped`/`converged` flags so a non-crossing endpoint is self-describing (tested: scalar two-sided, per-record lower-clamp, Bernoulli double-clamp), `marginal = :variational` is rejected (the ELBO is not a χ²₁ LRT), and all finite intervals bracket the estimate (`test/runtests.jl`, 20 assertions); the interval covers `:poisson`/`:bernoulli`/`:binomial`/`:bernoulli_probit` UNIFORMLY through one shared `_resolve_single_family` + `target`/`_profile_root` path, with a CI test pinning the IDENTICAL return-field contract across all four families (H6); coverage is now CHARACTERIZED (not just shape-pinned) by the opt-in `sim/phase6_nongaussian_interval_coverage.jl` (capped BLAS, outside CI; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-22-nongaussian-interval-coverage.md`), sweeping family × level∈{0.90,0.95} × truth σ²a∈{0.25,1.0} (q=165, 15 reps/cell), reporting per-cell coverage over NON-DEGENERATE reps + endpoint-clamp rates separately — coverage ~0.71–1.00, read as CONSERVATIVE/over-covering (informative Binomial m=20 clean 0.93–1.00; binary Bernoulli predominantly LOWER-clamped one-sided so its coverage is inflated by the clamped bound), a descriptive validation-scale characterization at small reps, NOT a calibrated coverage guarantee; `fit_laplace_reml` and `laplace_reml_interval` are now EXPORTED (experimental public surface, exercised through the unqualified names; the marginal-loglik kernels and `ResponseFamily` types stay internal), and the fit returns a distinct `NonGaussianFit` struct with the `AnimalModelFit` extractor contract — `breeding_values(fit)`/`variance_components(fit)`/`fixed_effects(fit)`/`EBV(fit)` (tested; a distinct type so it does not collide with the multivariate `NamedTuple` extractors); a `MarginalMethod` dispatch type (`Laplace`/`Variational`, internal — wired into `fit_laplace_reml`, which now also accepts the `:LA`/`:VA` aliases) and the exported `nongaussian_result_payload(fit)` bridge `NamedTuple` (`engine`/`target`/`family`/`n_trials`/`method`/`variance_components`/`fixed_effects`/`breeding_values`/`loglik`/`converged`; value-preserving, family-uniform, no `heritability` field; `n_trials` makes a binomial payload self-describing) now exist, mirroring `multivariate_result_payload` (`test/runtests.jl`); `test/fixtures/non_gaussian_parity/` serializes Poisson Laplace and per-record Binomial variational payload targets, hsquared PR #95 (`05fbdd3`) consumes them in R Julia-free normalizer tests, and hsquared PR #96 (`e7c7a4a`) records the existing opt-in R `target = "nongaussian"` bridge for Poisson/Binomial LA/VA fits; still needs per-record varying-trial R formula/bridge activation, Gaussian/multi-component intervals (nuisance profiling), large-n coverage calibration, latent genetic factors, external GLLVM.jl/gllvmTMB comparators, and broader R-side validation/comparator/calibration depth |
| V6-BERNOULLI | Non-Gaussian Bernoulli/logit family (Phase 6) | partial | `BernoulliResponse` (logit link) extends the non-Gaussian engine to binary 0/1 traits for BOTH the Laplace marginal and the VA ELBO; because the logistic log-partition has no closed-form Gaussian expectation, the VA expected loglik/score/weight kernels are computed by a load-time 20-node Gauss–Hermite rule (the same nodes for all three, so score/weight are exactly the η̄-derivatives of the expected loglik). Validated (`test/runtests.jl`): conditional and expected score/weight match central finite differences, the expected kernels reduce to the conditional kernels as `v→0`, a β-fixed independent tensor Gauss–Hermite quadrature confirms the VA ELBO is a valid LOWER BOUND on the true marginal (gap ≈4e-4 on the 3-animal fixture) and the Laplace value is close (gap ≈0.028), and `fit_laplace_reml(...; family = :bernoulli)` converges for both `:laplace` and `:variational`; a binary guard rejects non-`{0,1}` responses. Dense/validation-scale. The opt-in harness `sim/phase6_bernoulli_recovery.jl` (q=1075 half-sib, truth σ²a=1.0 on the logit scale, μ=0) RAN 5/5 on the gated criteria — converged, non-collapsed (interior `sigma_a2`), and EBV recovery correlation `cor(û,u) ≥ 0.5` (0.565–0.701) — while the variance point estimate is REPORTED-not-gated and confirms the known Laplace-for-binary DOWNWARD bias (σ̂²a 0.362–0.813, mean ≈0.65 vs 1.0; one persistent low-bias seed); the rank/EBV signal is reliable, the variance magnitude is not yet calibrated. Still needs a bias correction or many-trial binomial design for `sigma_a2`, intervals, a probit/threshold-model comparator, external GLLVM.jl/gllvmTMB parity, and an R model-spec |
| V6-BINOMIAL | Non-Gaussian Binomial/logit family (Phase 6) | partial | `BinomialResponse(n_trials)` generalises `BernoulliResponse` (= `n_trials = 1`) to `y` successes out of a common `n_trials`, for BOTH the Laplace marginal and the VA ELBO (same Gauss–Hermite VA kernels, scaled by `n_trials`, plus the constant `log C(n_trials, y)` offset). Validated (`test/runtests.jl`): the `n_trials = 1` kernels match `BernoulliResponse` exactly, conditional + expected score/weight match central finite differences, a β-fixed independent tensor Gauss–Hermite quadrature (m=8) confirms the VA ELBO is a valid LOWER BOUND (gap ≈2e-3) and the Laplace value is close (gap ≈0.031), guards reject `n_trials < 1` and `y ∉ 0:n_trials`, and `fit_laplace_reml(...; family = :binomial, n_trials = m)` converges (the keyword is required). The opt-in harness `sim/phase6_binomial_recovery.jl` (q=345 half-sib, m=20 trials, truth σ²a=1.0) RAN **5/5** with `sigma_a2` HARD-gated: rel ≤ 0.175 and EBV correlation 0.900–0.916 — demonstrating that the single-trial Bernoulli `sigma_a2` bias is an INFORMATION effect, not an estimator flaw (more trials ⇒ tight, gateable recovery); the opt-in `sim/phase6_binomial_information_gradient.jl` study (checkpoint `docs/dev-log/recovery-checkpoints/2026-06-22-phase6-binomial-information-gradient.md`) records the full gradient on a fixed `u` across m ∈ {1, 2, 5, 10, 20} + per-record — mean σ̂²a rel-bias 0.42 → 0.085 and EBV cor 0.62 → 0.91 as trials grow, the per-record case at rel 0.14 / cor 0.87 (descriptive characterization, not a CI gate). **Per-record (varying) `n_trials` — the general `cbind(successes, failures)` GLMM — is now implemented and validated** via the internal `BinomialVectorResponse(n_trials::Vector{Int})`, resolved per record by `_fam_record` into the scalar `BinomialResponse(n_trials[i])` across all Laplace + VA kernels (the scalar path is untouched). Validated (`test/runtests.jl`, 39 assertions): constant-vector `fill(m,n)` == scalar to ~1e-12 (Laplace + VA + the fitted path to 1e-6), all-ones == Bernoulli, heterogeneous `n_trials` finite/converged and distinct from the scalar fit, score/weight == finite-diff at a heterogeneous record, a β-fixed value gate vs an INDEPENDENT per-record tensor Gauss–Hermite oracle (Laplace |Δ|<0.2, VA ELBO ≤ truth), `_check_counts`/constructor guards, and integer-valued-Float64 acceptance (R bridge) + non-integer rejection; the opt-in harness adds a mixed-regime per-record scenario (`nₐ ~ Uniform{1..30}`, q=345): **5/5, rel ≤ 0.062, EBV cor 0.85–0.89**. A profile-LRT `sigma_a2` interval is now available (`laplace_reml_interval(...; family = :binomial, n_trials)`) — two-sided interior χ²₁ roots when `σ̂²a` is clear of zero (else a lower clamp; fixture-dependent), with `lower_clamped`/`upper_clamped`/`converged` flags making a clamped endpoint self-describing and `marginal = :variational` rejected (the ELBO is not a χ²₁ LRT); validated (`test/runtests.jl`, 20 assertions): the scalar-`m=20` two-sided fixture + nesting, the per-record lower-clamp, the variational / non-integer / missing-`n_trials` / length guards. Dense/validation-scale, asymptotic (interval coverage now characterized but not calibrated — opt-in `sim/phase6_nongaussian_interval_coverage.jl`, conservative/over-covering, see V6-FIT); still needs a probit/threshold comparator, external GLLVM.jl/gllvmTMB parity, and an R model-spec |
| V6-NBINOM | Non-Gaussian negative-binomial (NB2, log link) family + overdispersion (Phase 6, H1) | partial | `NegativeBinomialResponse(theta)` (internal) extends the non-Gaussian engine to overdispersed counts under the NB2 parameterization `Var(y\|μ) = μ + μ²/theta` (log link, `μ = exp(η)`); `theta` is an EXTRA estimable scalar, so `fit_laplace_reml(...; family = :nbinom, theta_init = …)` profiles `(sigma_a2, theta)` JOINTLY by NelderMead (the single-parameter Poisson/Binomial branch cannot be reused). LAPLACE ONLY — the NB variational ELBO has no closed form, so `marginal = :variational` is rejected. Validated (`test/runtests.jl`): the conditional score/weight kernels match central finite differences of `_fam_loglik`; an INDEPENDENT limiting-case oracle pins the `theta`-dependent loggamma normalizer an η-only finite difference cannot — the loglik reduces to `PoissonResponse` as `theta → ∞` (Poisson limit) and to the closed-form geometric pmf at `theta = 1`; the Laplace Newton mode returns `NaN`/`converged = false` on non-convergence; `theta ≤ 0` and non-integer/negative counts are guarded; the fitted path returns a positive `(sigma_a2, theta)`, the `NonGaussianFit` extractor contract, and a `nongaussian_result_payload` with `family = "nbinom"`, a `theta` variance-component entry, `n_trials = nothing`, and NO `heritability` field; reuses the module's existing `_loggamma` (Lanczos, `multivariate.jl`) — no `SpecialFunctions`. An opt-in seeded recovery harness (`sim/phase6_nbinom_recovery.jl`, outside CI; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-22-h1-nbinom-recovery.md`) on 5 pre-declared seeds (q = 165, one record/animal): the hard gate (converged ∧ interior σ̂²a ∧ EBV cor ≥ 0.5) passes 5/5 (EBV-rank cor 0.61–0.77), while the `sigma_a2` MAGNITUDE is REPORTED-NOT-GATED (3/5 within rel ≤ 0.45; mean σ̂²a 0.395 vs 0.50, ~21% downward; θ̂ weakly identified, 1.8–6.5) — the known Laplace-for-count information effect (cf. `V6-BERNOULLI`), reducible by more information per animal (cf. `V6-BINOMIAL`, m = 20). Still needs a same-estimand external comparator (glmmTMB/MCMCglmm/INLA), `theta` recovery calibration + its SE, a `sigma_a2` profile interval for `:nbinom` (H6), NB variational kernels, and an R `nbinom()` formula/bridge activation |
| V6-BETABINOMIAL | Non-Gaussian beta-binomial / overdispersed-logit family (Phase 6, H2) | partial | `BetaBinomialResponse(n_trials, rho)` (internal) adds an OVERdispersed binomial family (logit link): the success probability is Beta-distributed across records with intra-class correlation `ρ ∈ (0,1)` (`ρ → 0` is the Binomial limit), marginalised in closed form to `ℓ(y\|η,ρ) = lbeta(α+y, β+n−y) − lbeta(α,β) + log C(n,y)` with `α = p(1−ρ)/ρ`, `β = (1−p)(1−ρ)/ρ`, `p = logistic(η)`. Laplace marginal path ONLY; the conditional kernel reuses the module's existing `_loggamma` (Lanczos, `multivariate.jl`) via a new `_lbeta` and a series `_digamma` — no `SpecialFunctions`. The IRLS working weight is the FISHER (expected) information `E[(dℓ/dη)²] = Σ_k score(k,η)²·P(k\|η,ρ)`, NOT the observed `−d²ℓ/dη²`: the logit beta-binomial is concave in `p` but NOT log-concave in `η`, and the observed information DOES go negative at larger `n` in the surprising tail (confirmed at `m=20, ρ=0.5, y=0, η=3` → `−d²ℓ/dη² ≈ −0.099`), which would break `cholesky(Symmetric(H))`; the Fisher information is `≥ 0` by construction so the Newton Hessian stays PD. Validated (`test/runtests.jl`): the `ρ→0` reduction of both `_fam_loglik` and the Fisher weight to `BinomialResponse(m)` (the binding trusted-path anchor), conditional score == central finite differences (rtol 1e-5), the pmf integrating to 1 with mean `n·p` and the closed-form overdispersed variance `n·p(1−p)(1+(n−1)ρ)`, a zero-mean score with `Fisher weight == E[score²]`, a strictly positive Fisher weight across a wide `(ρ,y,η)` grid including the negative-observed regime, guards (`n_trials<1` / `ρ∉(0,1)` / `y∉0:n_trials`), and a β-fixed INDEPENDENT tensor Gauss–Hermite quadrature of the TRUE beta-binomial marginal close to the Laplace value on the 3-animal fixture; `fit_laplace_reml(...; family = :beta_binomial, n_trials, rho)` estimates `sigma_a2` (Brent) at a SUPPLIED FIXED ρ (both keywords required; `marginal = :variational` rejected), and `nongaussian_result_payload` self-describes the fit with `family = "beta_binomial"` and the fixed ρ in a new `dispersion` field. An opt-in seeded recovery harness (`sim/phase6_betabinomial_recovery.jl`, outside CI; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-22-h2-betabinomial-recovery.md`) on 5 pre-declared seeds (q = 345, m = 20, ρ = 0.2 supplied at truth) passes the hard gate 5/5 (converged ∧ interior σ̂²a ∧ EBV cor 0.74–0.82) and the σ̂²a magnitude recovered well (mean 0.949 vs 1.0, mean rel 0.153, 5/5 within rel ≤ 0.45 reported-not-gated) — m = 20 makes the overdispersed fit informative, conditional on a correctly supplied ρ. Still needs joint `(sigma_a2, ρ)` estimation, a VA/ELBO kernel, a per-record `BetaBinomialVectorResponse`, a `sigma_a2` profile interval (H6), an external comparator (MCMCglmm/aods3/glmmTMB), latent-scale h², and an R `beta_binomial()` formula/bridge activation; INTERNAL (not exported), not the public default, not covered |
| V6-PROBIT | Non-Gaussian probit / threshold (liability-scale) family (Phase 6, H3) | partial | `BernoulliProbitResponse` (internal) extends the non-Gaussian engine to a BINARY threshold (liability-scale) animal model with the probit link `P(y=1) = Φ(η)` (liability `η + e`, `e ~ N(0,1)`, observed `y = 1[liability > 0]`). With `s = 2y−1`: conditional `ℓ = log Φ(sη)`, score `s·φ(sη)/Φ(sη)` (signed inverse-Mills), IRLS weight `M(sη)·(M(sη)+sη) ∈ (0,1)`. The probit conditional is LOG-CONCAVE, so its observed information is strictly positive and equals the expected information — no Fisher-scoring substitution (contrast beta-binomial). Dependency-free standard-normal primitives added (`_norm_pdf`/`_norm_logpdf`/`_norm_logcdf`/`_norm_mills`): `Φ`/`erfc` reuse the module's validated incomplete-gamma (`erfc(z)=Q(½,z²)`) and a LOG-form continued fraction keeps `_norm_logcdf` FINITE into the deep tail (`x=−40`); the coarse genomic `_standard_normal_cdf_approx` (7.5e-8) is NOT reused for derivatives. Validated (`test/runtests.jl`): `Φ` matches standard values to 1e-12; `_norm_logcdf` finite at `x=−10/−20/−40` with `Φ(x)+Φ(−x)=1`; the log-form tail cf agrees with the package `Q`; conditional score AND weight match central finite differences (rtol 1e-5); the weight is strictly positive across a `(y,η)` grid incl. `η=−6`; the Laplace mode is stationary; a β-fixed INDEPENDENT Gauss–Hermite quadrature of the TRUE probit marginal is close to the Laplace value on the 3-animal fixture (binary gap documented); `fit_laplace_reml(...; family = :bernoulli_probit)` estimates `sigma_a2` (Brent) and converges; `laplace_reml_interval(...; family = :bernoulli_probit)` returns the clamp-flagged interval; `nongaussian_result_payload` carries `family = "bernoulli_probit"` with NO heritability (liability scale); a non-{0,1} guard throws. LAPLACE ONLY — the probit variational expected information is response-dependent (`−E[ℓ″]` varies with `s`), which the response-free `_fam_expected_weight` signature cannot carry; `marginal = :variational` is rejected (VA is follow-up). Opt-in recovery `sim/phase6_threshold_recovery.jl` (liability DGP, outside CI; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-22-threshold-recovery.md`): 5 pre-declared seeds (q = 345) pass the hard gate 5/5 (EBV cor 0.61–0.76), σ̂²a magnitude reported-not-gated and downward-biased as expected for binary (mean 0.633 vs 1.0, ~37% low, 3/5 within rel ≤ 0.45) — the Laplace-for-binary information effect (cf. V6-BERNOULLI). Still needs ORDINAL (≥3 categories, estimated cutpoints), a VA kernel, the observation-scale h² transform (H7), a same-estimand external comparator (MCMCglmm `threshold`/ASReml-R/Mrode Ch.10 anchor), and an R `probit` activation. INTERNAL (not exported), binary single-threshold, not the public default, not covered |
| V6-ORDINAL | Non-Gaussian ordered-categorical probit (ordinal threshold) family (Phase 6, T1) | partial | `OrderedProbitResponse(thresholds)` (internal) generalizes `BernoulliProbitResponse` to K ORDERED categories `1..K` on a standard-normal latent scale with `K-1` SUPPLIED, strictly-increasing cutpoints `θ`: liability `l = η + e`, `e ~ N(0,1)`, `P(y=k|η) = Φ(θ_k−η) − Φ(θ_{k-1}−η)` (`θ_0=−∞`, `θ_K=+∞`). The conditional `ℓ = log P(y|η)` is LOG-CONCAVE in η (log of a Gaussian interval probability), so the score `(φ(θ_{k-1}−η) − φ(θ_k−η))/P` and the OBSERVED-information IRLS weight `score² − (a·φ(a) − b·φ(b))/P` are exact and the weight is `> 0` — same convention as the binary probit (no Fisher-scoring substitution). The interval probability `Φ(b)−Φ(a)` is evaluated in whichever tail avoids 1−1 cancellation, reusing the validated tail-stable `_norm_logcdf`/`_norm_pdf` (with an explicit `_norm_cdf(±∞)` short-circuit). Validated (`test/runtests.jl`, T1): the `K=2, θ=[0]` REDUCTION to `BernoulliProbitResponse` is EXACT for loglik, score, AND observed weight (category 2 ↔ `y=1`); on a 3-category family the probabilities sum to 1, the score is zero-mean (`Σ_k P(k)·score(k)=0`), the score matches a central finite difference of the loglik (rtol 1e-5), the observed weight matches `−(second finite difference)` and is `> 0` (rtol 1e-3); an END-TO-END gate confirms the ordinal Laplace marginal at `θ=[0]` equals the Bernoulli-probit marginal on the same binary data recoded `1/2` (`laplace_marginal_loglik`, Δ ~1e-16); strictly-increasing / non-empty threshold and `1:K` category-range guards throw. A ≥3-category ordinal design is MORE informative than binary (the calving-ease trait, T1). SUPPLIED thresholds only — JOINT cutpoint estimation with the variances (the estimated-cutpoint `OrderedProbitResponse` the V6-PROBIT row anticipates) is the next slice; not wired into `fit_laplace_reml`'s `:symbol` resolver (reachable only by passing the family object to `laplace_marginal_loglik`); Laplace-only (no VA); a deep-latent-tail category whose probability underflows needs a follow-up log-space `logsubexp` loglik; no observation-/liability-scale h² transform; no same-estimand external comparator (`ordinal::clmm` (Laplace-ML) is the correct same-estimand tool — glmmTMB does NOT fit cumulative-link ordinal models — with MCMCglmm `threshold` as Bayesian-agreement-only); no recovery gate. INTERNAL (not exported), not wired to R, not the public default, not covered |
| V6-GAMMA | Non-Gaussian Gamma (log-link, positive continuous) family (Phase 6, T-Gamma) | partial | `GammaResponse(shape)` (internal) extends the non-Gaussian engine to a STRICTLY-POSITIVE CONTINUOUS response with the log link: mean `μ = exp(η)`, SUPPLIED shape `ν > 0`, `y | η ~ Gamma(shape ν, mean μ)`. The conditional `ℓ = ν(log ν − η) + (ν−1)log y − ν y e^{-η} − log Γ(ν)` is LOG-CONCAVE in η, so the score `ν(y e^{-η} − 1)` and the OBSERVED-information IRLS weight `ν y e^{-η}` are exact and strictly `> 0` — same convention as Poisson/probit (no Fisher-scoring substitution, unlike beta-binomial). Reuses the module's existing `_loggamma` (no new dependency). Validated (`test/runtests.jl`, T-Gamma): the `ν = 1` REDUCTION to the EXPONENTIAL is EXACT for loglik/score/observed weight (`ℓ = −η − y e^{-η}`); on a `ν ∈ {0.5, 2, 5}` × `(y, η)` grid the score matches a central finite difference (rtol 1e-5) and the observed weight matches `−(second finite difference)` and is `> 0` (rtol 1e-3); the Laplace marginal is finite end-to-end on a Gamma fixture; positive-shape / strictly-positive-response guards throw. SUPPLIED shape only — JOINT shape estimation, the `:symbol` resolver + `fit_laplace_reml`/R wiring, a same-estimand comparator (**`glmmTMB` `Gamma(link="log")` IS a valid same-estimand leg here**, unlike the ordinal case), a recovery gate, and observation-scale h² are owed. INTERNAL (not exported), not wired to R, not the public default, not covered |
| V6-NS-H2 | Non-Gaussian latent- and observation-scale heritability (Nakagawa–Schielzeth / de Villemereuil) (Phase 6, H7) | partial | `nongaussian_heritability(fit; mu, n_trials, predictor_variance)` + `nongaussian_heritability(sigma_a2, mu, family::ResponseFamily; predictor_variance)` (EXPORTED) compute the latent/link-scale h² = V_A/(V_A + V_link + V_fixed) and the observation/data-scale h² — the NS (2017) / de Villemereuil (QGglmm) transform filling the gap the family-uniform `nongaussian_result_payload` leaves (it carries NO heritability). Logit (Bernoulli/Binomial): V_link = π²/3 exactly (latent closed form, rtol 1e-12); observation scale on the PROPORTION estimand via the module's existing 20-node Gauss–Hermite, `V_A,obs = Ψ²·V_A` (Ψ = E[p(1−p)]; by Stein's lemma the regression-of-mean-on-breeding-value variance, so 0 < h²_obs < 1), `V_P,obs = Var(p) + Ψ/n_trials`. CRITICAL CONVENTION: π²/3 is the LATENT residual and is NOT added to the observation-scale integration variance N(μ, V_A+V_fixed) (matching QGglmm `binom1.logit`; corrects an over-broad reading of the spec). Poisson (log): latent h² = NaN (DEGENERATE — no latent residual); count scale via the log-normal–Poisson closed form. Gamma (log): latent h² is NON-degenerate — V_link = ψ₁(shape) = trigamma(shape) = Var[log Y] (EXACT, doc-19 §3.1; NOT the ln(1+1/ν) lognormal/CV approx), h²_latent = V_A/(V_A + ψ₁(ν) + V_fixed) (`method = :gamma_trigamma_latent`); the Gamma observation/data scale is a fenced follow-up (NaN). Gaussian reduces to V_A/(V_A+σ²e) on both scales (machine precision). Validated (`test/runtests.jl`): the Gaussian reduction; the `_trigamma` closed forms (ψ₁(1)=π²/6, ψ₁(2)=π²/6−1, ψ₁(½)=π²/2) + recurrence + the Gamma latent closed form (μ-independent, V_fixed denominator, monotone-in-ν, shape guards); the logit latent closed form; the logit observation-scale anchored against an INDEPENDENT 64-node quadrature of the same integrals (rtol 1e-3, 0<h²_obs<1); the Poisson degeneracy + closed form vs independent integration (rtol 1e-6); the Binomial n_trials information gradient (h²_obs rises 1→5→20); and guards (refuses a non-converged fit; per-record varying n_trials → NaN+caveat; ambiguous μ on a multi-β fit throws; Bernoulli information_limited = true; unsupported families throw). Dependency-free (reuses the 20-node GH). NOT added to `nongaussian_result_payload`. Still needs a same-estimand external comparator (QGglmm / MCMCglmm on the identical fit) + a Fisher/Falconer review of the exact decomposition, single-trial Bernoulli bias correction, per-record / multivariate data-scale h², probit families + the Gamma/threshold OBSERVATION scale (the Gamma LATENT scale is now done via trigamma), beta-binomial / negative-binomial families, intervals/SEs, and an R-facing surface. EXPORTED but experimental — not the public default, not covered |
| V6-GGLLVM-DESC | Genetic-GLLVM latent-structure descriptors + Gaussian latent solve (supplied loadings) (Phase 6, #50 slices 1–2) | partial | `genetic_gllvm_descriptors(Λ; uniqueness)` reports the rotation-INVARIANT descriptors of a genetic-GLLVM latent layer from SUPPLIED `traits × K` loadings `Λ` (+ optional positive uniqueness `Ψ`): `genetic_covariance` `Σ_g = ΛΛ' (+ diag Ψ)`, `genetic_variances`, `genetic_correlation`, `communality` `c²_t = (ΛΛ')_tt / Σ_g[t,t]`, `genetic_pca`/`g_max`, and `rank`/`n_latent_factors = K` — a pure composition of `lowrank_covariance`/`factor_analytic_covariance`/`genetic_correlation` (`multivariate.jl`) and `genetic_pca`/`g_max` (`evolvability.jl`), no new estimation. Validated (`test/runtests.jl`, 24 deterministic RNG-free assertions): exact `Σ_g == lowrank_covariance(Λ)` (Ψ absent) and `== factor_analytic_covariance(Λ, Ψ)`; `communality == 1` at `Ψ = 0`, matches `(ΛΛ')_tt / Σ_g[t,t]` and is strictly in `(0,1)` with positive `Ψ`; the binding rotation-invariance gate (for orthogonal `Q`, `Λ → ΛQ` leaves `Σ_g`, `genetic_variances`, `communality`, `genetic_correlation`, and the `genetic_pca` eigenvalues invariant); the `K = t`, `Λ = I`, `Ψ = 0` reduction (`Σ_g = I`, communality 1, eigenvalues all 1); and delegated dimension/positivity/rank guards. DESCRIPTIVE, supplied-covariance only — `Λ`/`Ψ` NOT estimated, no marginal/likelihood/fit, no R model-spec or bridge payload, only rotation-invariant functionals (never raw loadings). Slice 2 adds the Gaussian latent solve `genetic_gllvm_gaussian_mme(Y, X, Z, Ainv, Λ, R0; uniqueness)`: since `Cov(vec(U)) = G_lat ⊗ A` under a Gaussian response, the Gaussian genetic GLLVM IS the multivariate animal model at `G0 = G_lat = ΛΛ'(+diag Ψ)`, so the function builds `G_lat` and delegates to `multivariate_mme`, returning that solve plus the rotation-invariant `latent_structure`. Validated (`test/runtests.jl`): the defining identity (`== multivariate_mme` at `G0 = G_lat`, exact β + EBVs), rotation invariance of β/EBVs under `Λ → ΛQ`, the `t = 1, K = 1` reduction to the univariate `henderson_mme`, and guards (trait-count mismatch; a singular pure-low-rank `G_lat` with `K < t` and no `Ψ` rejected — `G_lat` must be PD). The solve's numerical correctness reduces to `multivariate_mme` (V4-MV); `Λ`/`Ψ`/`R0` are NOT estimated (slice 3). An overload `genetic_gllvm_descriptors(result)` reports the same rotation-invariant descriptors for an ESTIMATED `:factor_analytic`/`:lowrank` multivariate REML fit from its identified `G` and `Ψ` (never the loadings), `communality = 1 − Ψ/diag(G)`, validated against the real deterministic `fa`/`low` fixture fits plus a synthetic structured result and rejecting the rotation-free `:diagonal`/`:unstructured` structures (`test/runtests.jl`). Still needs the NON-Gaussian latent marginal (Poisson/Bernoulli/Binomial `G_lat ⊗ A` into the Laplace/VA marginal), genetic-GLLVM REML estimation of structured `G_lat` (slice 3), and external GLLVM.jl/gllvmTMB comparator parity |
| V6-GGLLVM-MARGINAL | Genetic-GLLVM non-Gaussian K-factor latent Laplace marginal (supplied loadings) (Phase 6, #50 slice 2) | partial | `gllvm_laplace_marginal_loglik(Y, Ainv, loadings, family; X)` (internal) evaluates the Laplace-approximate marginal log-likelihood of the K-FACTOR genetic GLLVM — latent field `vec(g) ~ N(0, I_K ⊗ A)` (each factor `g[·,k] ~ N(0,A)` independently), `η[i,t] = (Xβ)[i,t] + Σ_k Λ[t,k] g[i,k]`, `y[i,t] | η ~ family` (Gaussian/Poisson/Bernoulli/Binomial), `β` flat-integrated. It generalizes the single-factor `laplace_marginal_loglik` by replacing `(Z, Ainv/σ²a)` with `(W, I_K ⊗ Ainv)` (`W` = the Λ-weighted latent design: record `(i,t)` scatters `Λ[t,:]` into animal `i`'s `K` factor slots), via penalized-IRLS Newton over `[β; vec(g)]` then a Gaussian integral at the mode. Validated (`test/runtests.jl`) by TWO INDEPENDENT trusted-path reductions plus convergence: (1) the `K=1, T=1` case reduces EXACTLY to `laplace_marginal_loglik` with `σ²a = λ²` for BOTH Gaussian (machine precision) and Poisson (rtol 1e-7) — the Laplace approximation is invariant under the affine latent reparameterization `u = λg`; (2) the Gaussian, full-rank-`Λ` (`K=T`) case equals `_multivariate_reml_loglik` at `G0 = ΛΛ'`, `R0 = σ²e·I` (Gaussian Laplace is exact, rtol 1e-7); plus Bernoulli/Binomial convergence with mode-stationarity `‖∇‖ < 1e-8` and dimension/count guards. The non-Gaussian K>1 loglik VALUE is anchored by a block-diagonal-`Λ` test (independent traits ⇒ the K=2 Poisson marginal equals the SUM of two single-factor marginals), and K>T / singular-`ΛΛ'` cases (handled because `P = I_K ⊗ Ainv` is full-rank) are pinned against `_multivariate_reml_loglik`. A bounded 3-lens adversarial-verification Workflow (Gauss/Noether/Curie + Rose) returned `confirmed_correct`. The genuinely new genetic-GLLVM capability (a `K>1` latent field carrying genetic relatedness under a non-Gaussian response — the gap §3 of the scope doc). The `family` argument also accepts a length-`T` `Vector` of `ResponseFamily` (per-trait families) as well as a single scalar family — a uniform vector equals the scalar path EXACTLY, mixed Poisson+Gaussian traits converge, and a wrong-length vector is guarded (`test/runtests.jl`, 8 assertions). Experimental, dense/validation-scale, SUPPLIED loadings (NOT estimated), per-trait or shared families, balanced/fully-observed `Y`, INTERNAL (not exported). Still needs a fitted-object/EBV extractor surface, per-trait families threaded through the REML optimizer (the marginal already supports them), unbalanced/missing-record support, and external GLLVM.jl/gllvmTMB comparator parity (genetic-GLLVM REML over this marginal — slice 3 — now exists, see `V6-GGLLVM-REML`) |
| V6-GGLLVM-REML | Genetic-GLLVM REML — estimate the latent G_lat over the Laplace marginal (Phase 6, #50 slice 3) | partial | `fit_gllvm_laplace_reml(Y, Ainv, family; rank, X)` (internal) ESTIMATES the rank-`K` latent loadings `Λ` (`G_lat = ΛΛ'`) by maximizing the K-factor Laplace marginal (`gllvm_laplace_marginal_loglik`) over `vec(Λ)` (NelderMead). The marginal depends on `Λ` only through `G_lat = ΛΛ'`, so it is rotation-invariant; the result reports the rotation-INVARIANT `genetic_covariance` / `latent_structure` (never the raw `Λ̂`, an arbitrary point on the rotation manifold). Validated (`test/runtests.jl`): the `K=1, T=1` Poisson case reduces to the single-factor `fit_laplace_reml` (`σ²a = λ̂²`, rtol 2e-3); a multi-trait Poisson rank-1 fit converges and its optimum improves over the start (objective maximized); and the Gaussian optimum's marginal equals `_multivariate_reml_loglik` at the estimated `Λ̂Λ̂'` (`R0 = σ²e·I`, rtol 1e-7) — self-consistency of the reported `G_lat`. A `structure = :factor_analytic` option additionally estimates a per-trait specific genetic variance `Ψ > 0` (`G_lat = ΛΛ' + diag(Ψ)`, on the `log` scale, by augmenting the loadings to `[Λ | diag(√Ψ)]` and reusing the marginal unchanged); validated by the Gaussian self-consistency at `Ĝ = Λ̂Λ̂'+Ψ̂`, `communality < 1` (since `Ψ̂ > 0`), `FA loglik ≥ low-rank loglik` (FA nests low-rank), a converging Poisson FA fit, and length/structure guards. For `GaussianResponse(σ²e)` the scalar residual `σ²e` is FIXED (not estimated); the non-Gaussian families have no residual. A correctness-validated ESTIMATOR (reductions + optimum-improvement + Gaussian self-consistency). An opt-in known-truth recovery harness (`sim/phase6_gllvm_recovery.jl`, outside CI; checkpoint `docs/dev-log/recovery-checkpoints/2026-06-20-genetic-gllvm-reml-recovery.md`) now records FOUR scenarios across THREE families: (A) Poisson rank-1 (`Λ=[1.0,0.7,0.5]`, `q=240`): 5/5 seeds, mean rel `0.091`; (B) Poisson rank-2 NON-degenerate-ρ (`q=120`): 5/5, mean rel `0.205`, genetic correlations `mean|Δρ|=0.089`; (C) Bernoulli rank-1 logit (`Λ=[0.9,0.6,0.4]`, `q=240`, `μ=0`): REPORTED-NOT-GATED — mean rel `0.540`, 3/5 below threshold, one seed rel `1.15` — the known Laplace-for-binary DOWNWARD BIAS on the latent `G_lat` scale (single-trial information effect; the same bias documented for `V6-BERNOULLI`); (D) Binomial(20) rank-1 logit (same DGP, `m=20` trials): 5/5, mean rel `0.104` — confirming the Bernoulli failure is an information-quantity effect, not a structural flaw (more trials ⇒ less bias). All 20 fits across all four scenarios converged. **Consumability additions (2026-06-20):** `family` now accepts a length-`T` `Vector{ResponseFamily}` (per-trait families) — threaded through to `gllvm_laplace_marginal_loglik` (which already supported them) by widening the REML function signature from `ResponseFamily` to `Union{ResponseFamily, AbstractVector}`; a uniform vector gives numerically identical objectives to the scalar path (same optimizer path, exact `genetic_covariance` equality on the 8-animal Poisson rank-1 fixture). `fit_gllvm_laplace_reml` now returns a typed **`GeneticGLLVMFit`** struct (internal, not exported) instead of a bare `NamedTuple`, with typed extractors `genetic_covariance(fit)`, `breeding_values(fit)`, `latent_structure(fit)`, and `loglik(fit)` dispatching on the concrete struct type — not on `NamedTuple`, so no collision with the multivariate extractors; field access `fit.fieldname` continues to work unchanged. All 21 prior REML testset assertions pass with the new return type; 19 new consumability assertions added (testset "Genetic-GLLVM consumability"). Experimental, dense/validation-scale (low-rank + factor-analytic `G_lat`), balanced/fully-observed `Y`, INTERNAL (not exported). Still needs FA(+Ψ) recovery, Bernoulli bias correction / many-trial path (Binomial already demonstrating this), unbalanced/missing-record support, and external GLLVM.jl/gllvmTMB comparator parity |
| V3-CYTO | Cytoplasmic / maternal-lineage relationship (Phase 3 inheritance) | partial | `maternal_lineage(pedigree)` (forward-pass maternal-founder trace) and `cytoplasmic_relationship(pedigree)` (dense 0/1 same-maternal-line indicator) are exported and validated by a hand-checked multi-lineage fixture (A:{A,C,D,F}, B:{B,E}): exhaustive `C[i,j] == (lineage[i]==lineage[j])` check, symmetry, unit diagonal, founder self-labelling, all-founder→identity, and `(ids,sire,dam)` convenience agreement (`test/runtests.jl`); `C` is correctly singular (rank = #lineages). Construction only — still needs a cytoplasmic-variance estimation example (the grouping feeds the existing i.i.d. second random effect in `fit_two_effect_reml`), an external/textbook comparator, and the R model-spec for `cytoplasmic()`. An exploratory probe (maternal-line pedigree, q≈262) indicated the cytoplasmic and additive variances **partially confound** — `σ²c` recovers moderately but `σ²a` destabilises across seeds — as expected for a maternally-correlated effect; a calibrated, committed recovery harness needs a design that breaks that confounding (e.g. cross-fostering / embryo-transfer-style decoupling of maternal line from additive relatedness) and is left as future work rather than committed as a confounded gate |
| V3-SELFING | Self-fertilization (Phase 3 inheritance) | partial | `normalize_pedigree(...; allow_selfing = true)` opts into `sire == dam`; the additive-relationship recursion (`_numerator_relationship`) and Henderson direct `Ainv` rules (`pedigree_inverse`) were already self-correct, so no estimation code changed. Validated (`test/runtests.jl`): the canonical repeated-selfing inbreeding series `F = 0, 1/2, 3/4`, diagonals `A_ii = 3/2, 7/4`, `A[i, P] = 1` (selfed offspring of a non-inbred founder), symmetry, and `pedigree_inverse == inv(A)` for both a pure-selfing pedigree and a mixed sexual/selfed pedigree; default-reject preserved. Still needs textbook/`nadiv` selfing comparator parity, a selfing-aware recovery example, and the R model-spec |
| V3-CLONAL | Clonal / asexual relationship (Phase 3 inheritance) | partial | `clonal_relationship(pedigree, clone_of)` constructs the dense numerator relationship for clonal ramets by aliasing each ramet to its ultimate genet (`C[i,j] = A[rep(i), rep(j)]`; clone links resolved transitively with a cycle guard). Validated (`test/runtests.jl`) on a hand fixture (genet G = P×Q, ramets r1/r2): clonemates and ramet↔genet `= 1`, ramet inherits the genet's `0.5` to each parent, non-clones unchanged, `clone_of` all-marker → `_numerator_relationship`, transitive chains collapse, symmetry, and guards on length / unknown genet / cycle / self-clone; exported. `C` is rank-deficient (clonemates identical) — used directly, not inverted. Still needs an external/textbook comparator, a clonal-variance estimation example, and the R model-spec |
| V3-DOMINANCE | Dominance relationship matrix (Phase 3) | partial | `dominance_relationship(pedigree)` builds the dense Cockerham dominance relationship `D[x,y] = ¼(A[sx,sy]A[dx,dy] + A[sx,dy]A[dx,sy])` from the additive numerator relationship (unit diagonal; `0` when either animal has an unknown parent). Validated (`test/runtests.jl`): full sibs `= 1/4`, half sibs / parent–offspring `= 0`, symmetry, unit diagonal, exhaustive agreement with the explicit Cockerham formula against `A`, and `(ids,sire,dam)` convenience. Off-diagonal general; unit diagonal assumes non-inbred parents. Still needs dominance-inbreeding (inbred-parent) corrections, a textbook/`nadiv` comparator, a dominance-variance estimation example, and the R model-spec |
| V3-EPISTASIS | Epistatic relationship matrices (Phase 3) | partial | `epistatic_relationship(pedigree; kind)` builds Henderson (1985) Hadamard-product epistatic relationship matrices (`A∘A`, `A∘D`, `D∘D`). Validated (`test/runtests.jl`): equals the explicit Hadamard products of the additive `A` and dominance `D` matrices; full sibs `A∘A = 1/4`, `A∘D = 1/8`, `D∘D = 1/16`; half sibs `A∘A = 1/16`, `A∘D = D∘D = 0`; unit diagonal, symmetry, `(ids,sire,dam)` convenience, invalid-`kind` guard. Inherits the dominance non-inbred-parent assumption. Still needs higher-order terms, an external/textbook comparator, an epistatic-variance estimation example, and the R model-spec for `epistasis()` |
| V7-INHERIT | Other non-standard inheritance (haplodiploid, polyploid) | planned | relationship construction and biological interpretation checks; need sex/ploidy-specific coancestry canon (nadiv/textbook) before shipping coefficients. **Haplodiploid (J1, 2026-06-22): the convention is now DERIVED and dual-lens RATIFIED — the canon this row required is satisfied — but NO kernel ships yet.** `docs/dev-log/decisions/2026-06-22-haplodiploid-relationship-convention.md` proves the design spec's anchor set is INTERNALLY IMPOSSIBLE (a √2 positive-diagonal-congruence contradiction) and ratifies, with Mendel + Falconer sign-off, the gametic scale `A = 2θ` with **haploid-drone self = 2**: Female i `A[i,j]=½(A[s,j]+A[d,j])`, `A[i,i]=1+½A[s,d]`; Drone i (dam only) `A[i,j]=A[d,j]`, `A[i,i]=2`. Verified anchors (drone-self 2, sire→daughter 1, queen→daughter 1/2, queen→son 1, full-sisters 3/4, drone-brothers 1, half-sisters 1/4). Honest fences: name the scale in every claim; this is a CONSTRUCTION PRIMITIVE, NOT the honeybee-BLUP covariance (Brascamp & Bijma 2014 carry haploidy in a non-diagonal Mendelian-sampling `D`, not a rescaled diagonal); external comparator (`nadiv::makeS` dosage-compensated) is standing debt; inbreeding edge cases uncovered. Implementation is GATED on **maintainer ratification** of (a) the `A = 2θ`/drone-diagonal-2 scale and (b) the construction-only fence; on ratification it splits into a new `partial` `V3-HAPLODIPLOID` row + capability-status experimental row (polyploid stays planned). Status stays `planned` (derived + spec'd, awaiting ratification — no capability claimed) |

Rows must be updated before public docs promote a capability.
