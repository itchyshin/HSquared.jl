# After-task — v0.7 G-A + v0.8 S1/S2 fleet campaign (2026-07-02)

**Session:** Claude solo (Opus), autonomous. Executed the doc-23 v0.7/v0.8 programme through a
DRAC fleet compute campaign under full measure-first + pre-declaration discipline, with a
user-approved pre-compute checkpoint.
**Repos:** `HSquared.jl` only (Julia engine). R twin frozen — untouched.
**Branch:** `feat/2026-07-02-v07-v08-fleet` (PREDECL `cad28efb` pushed BEFORE any run).

## Headline

Two engine slices built + locally correctness-gated + benchmarked on DRAC, plus one banked
negative — all pre-declared, all measure-first. **No covered flip; honesty pins held**
(`validation_status()` rows **53** / covered **13** / `public_covered_count` **5** UNCHANGED).

1. **v0.8-S1 — METIS ordering: BANKED NEGATIVE (as pre-declared).** Before writing any ordering
   code, measured METIS nested dissection vs CHOLMOD AMD for the multi-effect MME. METIS is NOT
   a robust fill-reducing enabler: helps at moderate q but **~3.2× SLOWER (2.5× more fill) than
   AMD at q=50k K=3** (DRAC fir, reproduces local Mac). Per the pre-declared ≥1.2×-at-every-cell
   rule → FAIL → **AMD retained, no Metis dependency added.** This falsified doc-23's headline
   hypothesis and redirected v0.8 to S2.

2. **v0.8-S2 — matrix-free multi-effect PCG: the real scale enabler (positive).** New
   `solve_multi_effect_pcg` solves the K-independent-effect MME matrix-free — never forms/factors
   `C`, so it bypasses the K≥2 Cholesky fill wall. Correctness-gated in-suite (matrix-free ==
   direct ~1e-8; operator `C·eᵢ==C[:,i]` ≤1e-12; K=1 reduces to `solve_animal_model_pcg`).
   **Benchmark (DRAC fir, pre-declared): feasible to q=1,000,000** (converged, relres≤1e-8), PCG
   iterations DECREASING 42→36 as q grows 500× (Jacobi conditioning does not degrade),
   near-linear (log-log OLS slope ≈1.08), 19× faster than direct Cholesky at q=50k (direct
   nnz(L)-cap-excluded beyond). Machine-specific SUPPLIED-VARIANCE-SOLVE measurement — NOT a full
   REML fit, NOT a GPU/portable claim.

3. **v0.7-G-A — device-resident GBLUP: agreement gate PASSED.** New `gpu_fit_gblup` keeps
   `G`/`Ginv`/`C` on-device across `G→Ginv→dense MME solve`. **CPU↔GPU agreement gate PASSED on
   tamia H100 to ~1e-15** (β + GEBV, vanraden1/2/weighted) — device-resident GBLUP is numerically
   identical to the CPU `fit_gblup` pipeline. Device assembly was also CPU-mirror-validated to
   ~1e-15 before any GPU run. End-to-end GPU-vs-CPU speedup benchmark (job 360589, H100, q≤16000):
   device-resident GBLUP is **5.2× → 23× faster** than CPU (q=2k→16k; dense O(q³), so the GPU
   advantage compounds), agreement holding through the benchmark (maxΔ ≤ 2e-13). First run OOM'd on
   the q=32000 CPU reference; rerun capped at the feasible q≤16000 (harness byte-identical).

## Method — the fleet campaign (doc-24)

Two streams on disjoint hardware, byte-identical frozen harnesses replicated across machines;
honest cross-machine rail (no absolute-time comparison across boxes — feasibility frontier +
within-machine ratios + cross-device agreement only). All 8 DRAC clusters reachable; Totoro was
not connected this session (its ControlMaster socket was down — DRAC alone carried the campaign).
Headline nodes: **fir** (CPU: S2 + S1) and **tamia** (GPU H100: G-A). PREDECL `cad28efb`
committed + pushed BEFORE any run; harness byte-identity verified on each node.

## Process notes

- **Measure-first caught a false premise:** the S1 measurement was run BEFORE implementing any
  ordering change, and it falsified doc-23's "METIS is the concrete enabler" — the pivot to the
  matrix-free PCG (S2) is the direct consequence. This is the discipline working.
- **G-A first run OOM-killed on the benchmark** (q=32000 CPU reference exceeded the 64G request,
  MaxRSS 67G); the AGREEMENT gate had already PASSED. Per the Phase 5 declared-grid-deviation
  pattern, the benchmark was capped at the feasible q-range (≤16000) and re-run — harness
  byte-identical, only the grid arg + SLURM memory differ (infrastructure the harness
  parameterizes), disclosed honestly.
- **S2 over-provisioned memory** (249G) needlessly delayed scheduling; resubmitted at 64G
  (matrix-free is memory-light) and it ran immediately.
- **Real Rose audit** (rose-systems-auditor, Opus) on the S1+S2 evidence → **PROMOTE-WITH-CHANGES**:
  independently reproduced every load-bearing number (validation count 53, S2 q=1e6/19×, S1
  3.2×/0.40×, the correctness assertions), verified pre-declaration-before-run ordering, found no
  overclaim leak. 4 required fixes — all the expected post-run status-string refreshes
  ("OWED"→"MEASURED") plus the C1 slope (endpoint 1.04 → pre-declared OLS 1.08) — all applied.

## Evidence

- Pre-declarations: `docs/dev-log/recovery-checkpoints/2026-07-02-v08-s2-matfree-pcg-predeclaration.md`,
  `...-v07-ga-device-gblup-predeclaration.md`, `...-v08-s1-ordering-predeclaration.md`.
- Results: `...-v08-s2-matfree-pcg-result.md`, `...-v08-s1-ordering-result.md`,
  `...-v07-ga-device-gblup-result.md`.
- Raw: `sim/drac/results/s2_fc30564_46705208.tsv`, `s1_ordering_46705016.tsv`,
  `v07_ga_gpu_360583.out` (agreement), `g_a_gblup_360589.tsv` (benchmark).
- Fleet plan: `docs/design/24-v07-v08-fleet-campaign.md`.

## Checks

- `Pkg.test()` GREEN (count guard 53; new S2 testset + G-A stub assertion pass). `docs/make.jl`
  GREEN.
- Honesty pins: rows 53 / covered 13 / `public_covered_count` 5 UNCHANGED (Rose-verified live).
  Nothing promoted; these are engine performance/scale/agreement measurements, not new estimands
  or a public default change.

## Next / owed

- **v0.8:** wire the matrix-free operator into the REML iterations (the AI-REML Takahashi score
  needs a factor → a stochastic-trace/EM matrix-free FIT path); a sparse-path recovery gate;
  preconditioners beyond IC(0); an external comparator at scale.
- **v0.7:** G-B (Float32/mixed-precision path + accuracy characterization); G-C (real-marker-panel
  benchmark); a G-A agreement replicate on a 2nd/3rd GPU architecture (Killarney/Vulcan H100,
  Narval A100) — the fleet clusters are set up for it; G-D backend dispatcher.
- Totoro reconnect for the no-queue big-CPU replicate lane.
