# After-task — 0.8 SS h² fence (reported, not claim-gated; no flip)

Date: 2026-09-03. Lane: Julia engine (`HSquared.jl`). Branch:
`cursor/08-ss-20260903`. Type: honesty fence (no campaign, no flip).

```
PLATFORM: cursor | LANE: cursor/08-ss-h2-fence-20260903
OTHER LANES: cursor/08-fa #292 · cursor:g5-stale-copy #296 ·
             H1/H3 #294 · Codex DRAFT #137/#274 cite-only
Active lenses: Rose (this fence) · Ada/Shannon fence
Spawned subagents: none
Current lane: Julia SS #295 — §3 #3 fence only
```

## Goal

Clear the cheap design-41 §3 #3 path after the n=240 gate reported h²
and did not gate it: write an explicit **reported, not claim-gated**
fence for V2-SSHINV H-scale heritability. No identity test. No covered
flip. Darwin stays UNSIGNED. Count stays 7.

## What landed

- Decision:
  `docs/dev-log/decisions/2026-09-03-v08-ss-h2-fence.md`
- This report + matching `check-log.d` entry + board one-liner.
- Substitution disclosure §3 #3 row updated to **FENCED** (identity
  path still named as the alternative, not taken).
- Rose packet `~/local-scratch/h2-08-ss-rose-packet-2026-09-03.md`
  marks this item **DRAFTED pending Darwin**.

Did **not** edit `capability-status.md`, `validation_status.jl`, the
public-claims register, Darwin’s unsigned sheet, or any `src/` /
`test/` identity pin.

## Public claim audit

Allowed: "n=240 reported h² = 0.4147 vs 0.40 (0.79·MCSE) as a dump;
H-scale heritability is reported, not claim-gated; a covered sentence
that names h² still owes identity + locked citation; else fence it
out; Standard-Tier derived-estimand rule."
Blocked: h² covered / recovered / gated; borrowing GREML or MV
derived locks; `V2-SSHINV` covered; count 8; 0.8.0; 1.0 / CRAN;
Rose CLEAN; Darwin SIGN.

## Checks this slice

Docs only. No `Pkg.test` / Documenter (no `src/` change). Re-read
the frozen predeclaration and the banked n=240 table; did not re-run
the gate.

## Tests of the tests

The n=240 TSV/log and freeze SHA `8e6e038b` were re-read, not re-run.
This slice does not change the 48/48 bar or add h² to PASS.

## Coordination notes

Lease `cursor:08-ss-h2-fence-20260903` on the new fence files, the
substitution-disclosure one-row edit, and the board one-liner.
`capability-status.md` stays with `cursor:g5-stale-copy-20260903`.

## Known limitations

Fence satisfies §3 #3’s “else fence h² out” clause only. The
identity-test + locked-citation close is **not** written. Darwin
remains UNSIGNED. Rose stays **NOT CLEAN**.

## Next

Darwin SIGN (owner ink). Then maintainer nod on design-56, R
element-wise catch-up, claim-surface honesty under the capability
lease, and a **new** Rose packet on that later tip. No auto-flip.
