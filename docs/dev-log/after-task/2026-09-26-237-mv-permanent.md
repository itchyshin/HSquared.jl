# After-task: multivariate animal + permanent environment (#237 Julia)

Date: 2026-09-26
Lane: Julia `cursor/237-mv-permanent-jl` at `~/local-scratch/lanes/HSquared.jl-237-mv-permanent`
Fence: experimental 0.9.0 / public_covered_count 7; no covered flip; REGISTERED=no

Active lenses: Henderson, Gauss, Hopper, Rose
Spawned subagents: none
Current lane: Julia

## What landed

Dense REML for the multi-trait repeatability model:

    Y = X B + Z u_a + Z u_pe + E
    vec(U_a^T) ~ N(0, A ⊗ G0),   vec(U_pe^T) ~ N(0, I ⊗ P0)

so V = Zfull(A⊗G0)Zfull' + Zfull(I⊗P0)Zfull' + R. Trait-fastest, same
convention as `fit_multivariate_reml`. Animal-only MV absorbs Vpe into G0;
the second Kronecker block stops that.

Exports:

- `fit_multivariate_repeatability_reml(Y, X, Z, Ainv; ...)`
- `multivariate_repeatability_result_payload(result)`

payload-v2: Y + pedigree + iid dispatches `:multivariate_repeatability`.
Animal-only MV still throws Phase0NotImplementedError (needs caller G0/R0).

## Payload contract for the R sibling

`target = "multivariate_repeatability_reml"`
`status = "experimental"`
`estimator = "multivariate_repeatability_reml"`

Fields R needs:

- `genetic_covariance` / `permanent_covariance` / `residual_covariance` (t×t)
- `genetic_variances` / `permanent_variances` / `residual_variances`
- `genetic_correlation` / `permanent_correlation` / `residual_correlation`
- `heritability` (G0kk / (G0kk+P0kk+R0kk))
- `repeatability` ((G0kk+P0kk) / denom)
- `component_names = ["animal", "permanent", "residual"]`
- `component_dimensions = (animal=(q,t), permanent=(q,t), residual=(n,t))`
- `breeding_values` and `permanent_effects` as (ids, traits, values)
- `n_traits`, `n_animals`, `n_genetic_params`, `n_permanent_params`, `n_residual_params`
- `fixed_effects`, `loglik`, `converged`

R still fences `cbind()` + `permanent()`. Live JuliaCall bridge is not in this
slice. Codex (or the R worker) still owes the spec-gate lift and a live
round-trip.

## Checks

See `docs/dev-log/check-log.d/2026-09-26-237-mv-permanent.md` (37/37 focused).

## Not done

- Live R bridge / formula-grammar lift (sibling worker)
- Known-truth recovery at scale (tiny n cannot split Va/Vpe tightly)
- External comparator
- `validation_status()` row (left at 56 so the pin does not move)
- Full `Pkg.test()` / Documenter rebuild
- Merge, version bump, Registrator

## Rose

Experimental only. No covered flip. Count stays 7. Version stays 0.9.0.
REGISTERED=no. Do not merge from this slice.
