# 2026-06-19 Diagonal multivariate bridge payload (#42 scoped)

- Goal: deliver the engine half I committed to the R lane on #61 — a bridge-ready
  multivariate result payload scoped to the **rotation-free** `:diagonal` /
  `:unstructured` structures, so the R lane can wire the diagonal-vs-unstructured
  LRT (#47) without surfacing rotation-arbitrary lowrank/fa loadings (still gated
  on #42 + the rotation convention).
- Lenses: Hopper + Boole + Emmy (bridge/result-payload contract), Gauss + Noether
  (numerics/contract), Rose (claim gate). Adversarial review run as a workflow.

## What was done

- `multivariate_result_payload(result)` (src/multivariate.jl, exported): "boring"
  `NamedTuple` (engine, target="multivariate_reml", genetic_structure, n_traits,
  traits, G0, genetic_variances=diag(G0), R0, genetic/residual correlation,
  heritability, fixed_effects, breeding_values, loglik, n_genetic_params,
  converged), mirroring the univariate `result_payload`. Rejects
  `:lowrank`/`:factor_analytic` (rotation-nonidentified loadings); never surfaces
  `genetic_loadings`/`genetic_uniqueness`.
- `test/fixtures/structured_covariance_parity/`: deterministic two-trait
  `:diagonal` target (same inputs as `phase4_multitrait_parity`), generated by
  fitting `fit_multivariate_reml(...; genetic_structure = :diagonal)` — CSVs +
  README. CI self-consistency test: `multivariate_mme` at the stored diagonal
  G0/R0 reproduces beta/EBVs/h²/loglik.
- Tests: payload shape (diagonal + unstructured), `n_genetic_params` identity
  (`:diagonal`=t, `:unstructured`=t(t+1)/2), `genetic_variances=diag(G0)`,
  diagonal-vs-unstructured LRT `df` (interior null), lowrank/fa payload rejection,
  fixture self-consistency.
- Docs/rows: 03-engine-contract.md spec section; capability-status row;
  validation-debt `V4-BRIDGE` row; validation_status.jl `V4-BRIDGE` row +
  honest V4-MV-REML/V4-FA claim-boundary updates ("no bridge payload change" →
  `:diagonal` now bridge-exposed, lowrank/fa NOT).

## Commands / results

- `~/.juliaup/bin/julia --project=. -e 'using Pkg; Pkg.test()'` → **passed (exit 0)**
  (payload assertions + fixture self-consistency + V4-BRIDGE row green;
  validation_status count 32 → 33).
- `~/.juliaup/bin/julia --project=docs docs/make.jl` → (recorded in after-task).
- Adversarial 3-lens review workflow (Hopper/Gauss+Noether/Rose) → (recorded in
  after-task).

## Claim boundary

Bridge-ready payload for the rotation-free `:unstructured`/`:diagonal` structures
ONLY. No loadings/uniqueness surfaced; no external comparator parity; the fixture
is an input+target bundle, not external evidence; the R-side diagonal activation +
LRT wiring is cross-lane (#42/#47). Experimental, dense/validation-scale. No
capability moved to covered.
