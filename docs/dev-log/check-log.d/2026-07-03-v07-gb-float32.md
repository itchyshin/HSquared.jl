# Check-log — v0.7-G-B Float32 mixed-precision genomic G (2026-07-03)

**Slice:** opt-in `precision = Float32` path for `gpu_genomic_relationship_matrix` + a live tamia
H100 accuracy/speedup characterization. Branch `feat/2026-07-03-v07-gb-float32`. User steer: "V7 GPU
stream (finish doc-25)".

## Key result (tamia H100, job 360780)

- **Float64 gate PASSED:** `precision = Float64` GPU `G` ≡ CPU `genomic_relationship_matrix` to
  **7.3e-15** — the precision kwarg preserves the Float64 contract.
- **Float32 accuracy:** absolute `G` error ~1–5e-6; **negligible GEBV impact ~1e-6** — numerically
  safe for prediction. Large max-rel-error (≤2.6) = benign near-zero-entry artifact. `default` ==
  `pedantic_fp32` → TF32 not engaged (FP32-level in both).
- **Float32 speedup: MODEST ~1.1–1.4×** — honest, underwhelming; not a headline accelerator.

## Evidence

- Code: `ext/HSquaredCUDAExt.jl` (`precision` kwarg), `src/gpu_ext.jl` (docstring),
  `test/runtests.jl` (stub: `precision=Float32` throws MethodError without CUDA).
- Harness: `sim/drac/g_b_float32.jl` + `sim/drac/g_b_tamia.sbatch` (pre-declared before the run,
  `2026-07-03-v07-gb-float32-predeclaration.md`).
- Artifacts (tracked): `sim/drac/results/g_b_float32_360780.tsv`, `.../v07_gb_f32_360780.out`.
  Result checkpoint: `2026-07-03-v07-gb-float32-result.md`.
- Extension precompiled cleanly against CUDA on tamia (`HSquaredCUDAExt` ✓, 11.4s).
- `Pkg.test()` GREEN (count 55 UNCHANGED); `V2-GRM-GPU` stays `partial`.

## Honesty

Float32 OPT-IN, never the default; Float64 unchanged. The prior owed-note's "larger speedups"
assumption was CORRECTED to the measured modest ~1.2×. TF32 tensor-core Float32 not demonstrated. No
covered flip; `public_covered_count` UNCHANGED; GPU = acceleration, machine-specific, no
competitive/portable claim.
