# 2026-09-02 — A25 Grace: regenerate validation-status page + prose sync CI

**Arc:** Grace follow-up to A25 Rose F3 (mislabeled; spine A25 = MV-5 disposition
still open — this is Documenter hygiene only).
**Lane:** Julia engine (`HSquared.jl`).
**Worktree:** `~/local-scratch/lanes/HSquared.jl-h2-twin-20260901`
**Branch:** `claude/lane-h2-twin-20260901`. **Not pushed.**

## Problem

`docs/src/validation-status.md` is generated by
`tools/write_validation_status_page.jl` and carried a "do not edit by hand"
banner. It was last regenerated before A25a (`11f54d9e`) cited C8 into
`src/validation_status.jl`. Live measurement before this slice:

| Check | Result |
|---|---|
| `V4-MV-REML.status` | `covered` |
| source cites C8 job `47925486` | true |
| page cites C8 | **false** |
| page has all row ids | true |

`test/runtests.jl` asserted row **ids** only — prose drift invisible to CI.

## Changes

- Regenerated `docs/src/validation-status.md` from live `validation_status()`
  (56 rows).
- Strengthened the Documenter sync test: after the id check, require each row's
  `claim_boundary` (pipe-escaped as the writer does) to appear on the page.

## Commands and outcomes

| Command | Result |
|---|---|
| `julia --project=. tools/write_validation_status_page.jl` | wrote page; 56 rows |
| scoped Julia assert (ids + claim_boundary sync + C8 tokens) | **PASS** |
| page cites `47925486` / `14/16` / `C8` | **true** |
| `V4-MV-REML.status == "covered"` | unchanged |

## Fence

- No covered flip; `public_covered_count` stays **5**.
- No push; no G10; no Registrator; no Totoro/DRAC.
- Spine **A25 (MV-5 disposition)** remains OPEN (owner decision draft only).
