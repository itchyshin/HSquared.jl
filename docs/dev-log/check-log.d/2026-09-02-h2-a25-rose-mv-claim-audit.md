# 2026-09-02 — A25: Rose multivariate claim-surface audit (honesty patches)

**Arc:** A25 as dispatched — Rose multivariate claim-surface audit for 0.6
readiness. (Numbering note: the spine assigns A25 = MV-5 disposition and
A29 = Rose pre-flip audit. This is **A29-shaped work under the A25 label**;
**MV-5 disposition remains OPEN**.)
**Lane:** Julia engine (`HSquared.jl`). No R-lane file was edited.
**Worktree:** `~/local-scratch/lanes/HSquared.jl-h2-twin-20260901`
    10|**Branch:** `claude/lane-h2-twin-20260901`. **Not pushed.**

## Problem

A24 repaired the post-MV-4 routing-honesty drift on the **R** lane only. The
**Julia** lane kept a class of statements asserting that the R twin has no
multivariate model-spec and no external comparator parity. Both are false:
MV-4 merged 2026-07-12 (`hsquared` PR #132, `ff89ac7`) and routes `cbind()` on
the default call, and same-estimand parity is discharged on the
`phase4_multitrait_parity` fixture by `sommer` 4.4.5 (≤ 8e-5) and `blupf90+`
    20|2.60 (~1e-5). The engine's own `docs/design/capability-status.md` already said
so — so the repo contradicted itself.

These errors understate capability, so they carry no claim-inflation risk. They
are patched because a false *"not implemented yet"* has already cost this
programme once: the 2026-09-01 spine stub described MV-4 as unstarted work, and
`~/local-scratch/h2-post-050-spine-mv4-s6.md` §0 records the correction.

## Changes (four one-line-class honesty patches)

| File | Was (false) | Now |
    30||---|---|---|
| `src/multivariate.jl` (`fit_multivariate_reml` docstring) | "external-comparator (sommer / ASReml / JWAS) parity is still missing … There is no R-facing multivariate model-spec." | `sommer` + `blupf90+` discharged on ONE fixture (point-estimate); **no ASReml or JWAS leg**; R twin routes `cbind()` by default at a `partial` claim; R↔engine element-wise parity still owed |
| `README.md` (Planned-not-implemented list) | "R-facing multivariate model-spec syntax and external comparator parity" | R↔engine element-wise parity in CI + ASReml/JWAS legs remain planned; `cbind()` model-spec and `sommer`/`blupf90+` have landed |
| `docs/src/index.md` (same list, published) | "R-facing multivariate model-spec syntax and comparator parity" | R↔engine element-wise parity in CI + ASReml/JWAS legs |
| `src/validation_status.jl` (`V4-MV-REML.claim_boundary`) | "R-facing multivariate bridge semantics remain opt-in experimental." | R twin reaches the fitter on its **DEFAULT** `cbind()` route (MV-4) — reachability, **NOT** promotion; R claim stays `partial`; parity owed; `public_covered_count` stays 5 |

The ASReml fence is preserved deliberately: the patch **names ASReml and JWAS as
still absent** rather than letting "parity discharged" read as universal.

## Deliberately NOT patched (true in row scope, or not one-line)

    40|- `src/multivariate.jl` `multivariate_mme` docstring and `validation_status.jl`
  `V4-MULTIVARIATE` row — both are the **supplied-covariance MME** path, which
  genuinely has no R route (the R `cbind()` default reaches
  `fit_multivariate_reml`). True as scoped.
- `docs/design/capability-status.md` "Public multivariate G-matrix model-spec /
  syntax | planned" — the `cov = us()/diag()/lowrank()/fa()` grammar is
  genuinely not R-surfaced. True.
- `ROADMAP.md:168` — sits in a Phase-4B structured-covariance context where the
  model-spec/comparator clauses are defensible; its
  "no full loading rotation/interpretation convention" clause looks stale
    50|  against the 2026-06-19 twin ratification. Multi-clause, not a one-liner —
  handed to A29.

## Found, not fixed: the generated status page is stale

`docs/src/validation-status.md` is generated by
`tools/write_validation_status_page.jl` and carries a "do not edit by hand"
banner. It was last regenerated at `713edcf7`, **before** A25a (`11f54d9e`)
cited C8 into `src/validation_status.jl`. Measured live: the source row cites
job `47925486`; the published page does not.

    60|`test/runtests.jl` asserts only that each row **id** appears on the page, not
that row prose matches — so this drift is invisible to CI by construction.

Regeneration is a Grace/Documenter action, not a Rose honesty patch, and it is
owed. Consequence recorded: the evidence-inventory T2 row ("register currency —
closed") was an overstatement and has been corrected in scratch.

## Commands and outcomes

| Command | Result |
|---|---|
| `julia -e 'Meta.parseall(…)'` on both patched `src/` files | `parse_error=false` both |
    70|| `julia --project=. -e 'using HSquared; validation_status()'` | loads; `V4-MV-REML.status == "covered"` (unchanged); row cites `47925486`; stale opt-in clause gone; DEFAULT-route clause present |
| id-coverage assertion mirrored from `test/runtests.jl:184` | `page has all ids = true` — CI assertion still satisfied |
| `page cites C8` | **false** — confirms the generated-page staleness above |
| `git diff HEAD..codex/2026-07-13-v07-performance-localization -- src/multivariate.jl` | disjoint (their change is in `multivariate_covariance_standard_errors` ~L1089; ours is a docstring ~L757). No duplicate fix: the stale clause is still present on that branch too |

Full `Pkg.test()` was **not** run — these are docstring/claim-string edits with
no executable path, verified by parse + live row load.

## Fence

    80|- **No covered flip.** `V4-MV-REML` stays `covered`; R multivariate stays
  `partial`; `public_covered_count` stays **5**.
- No push, no Registrator, no G10 sign, no Totoro/DRAC.
- No R-lane file edited by this arc.
