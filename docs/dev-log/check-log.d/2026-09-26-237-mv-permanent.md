# 2026-09-26 multivariate animal + PE REML (hsquared #237, Julia engine)

## Commands

```sh
cd ~/local-scratch/lanes/HSquared.jl-237-mv-permanent
julia --project=. -e 'using Pkg; Pkg.instantiate()'
julia --project=. -e 'using HSquared; @assert isdefined(HSquared, :fit_multivariate_repeatability_reml); println("G1_FITTER_EXPORTED")'
julia --project=. -e 'using HSquared; include("test/test_multivariate_repeatability.jl")'
julia --project=. -e 'using HSquared; v = string(pkgversion(HSquared)); @assert v == "0.9.0"; println("G5_FENCE_HELD v=$(v)")'
```

## Outcome

- G1: `G1_FITTER_EXPORTED`
- Focused suite `Multivariate repeatability REML (hsquared #237)`: **37 pass / 0 fail** (2.9 s)
- Prints `PE_NOT_ABSORBED` and `G4_PAYLOAD_CONTRACT`
- Version stays **0.9.0**; `public_covered_count` pin stays **7**; `validation_status()` length stays **56**
- Full `Pkg.test()` not run (suite too heavy for this slice); focused file is the gate

## Scope

Engine-only experimental fitter. No covered flip. No Registrator. R `cbind()` + `permanent()` still fenced on the twin.
