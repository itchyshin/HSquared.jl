# Plan vs. actual — H² twin test campaign, wave 2

- Date: 2026-09-13
- Plan: `/Users/z3437171/shinichi-brain/projects/H2-twin/ultra-plan.md`
- Wave-2 addendum: `/Users/z3437171/shinichi-brain/projects/H2-twin/briefs/wave2-addendum.md`
- Wave: 2 of a possible 4 (stopping rule not fired: 4 new clusters, threshold is fewer than 3)
- Reconciler: Melissa. Read-only against the plan, the wave-2 addendum, my own wave-1 record, the
  six wave-2 lane receipts, the mechanical-verify receipt, Rose's wave-2 audit, live GitHub state
  measured in this session, and `checkpoint.md`/`MISSION-CONTROL.md`/`memory/AGENT_LOG.md` for the
  handoff axis. Wave-1 drafts and their verdicts were not re-audited (out of scope, per Rose's own
  note that she did not re-audit wave 1 either).

## Planned vs. actual, per lane

| Lane | Planned focus (addendum) | What ran (receipt) | Drafts | Rose verdict | Filed |
| --- | --- | --- | --- | --- | --- |
| L1 Simulator | R↔Julia parity on covered routes other than gryphon (common_env, multi_effect, direct_maternal, random_regression, genomic) at n≈200–500, full independent reconstruction; negative control; r_g→±0.95 bivariate at n≈300; correctly-scoped selfing successor | `updates/2026-09-13-simulator-w2.md`. Full independent-reconstruction parity delivered for 3 of the 5 named routes (common_env, direct_maternal, genomic — all exact); `multi_effect`/`random_regression` got only a liveness check, not full parity (time redirected to running a genuine `direct_maternal` mismatch to ground — see deviations); negative control ran and correctly flipped on a perturbation; bivariate r_g grid ran 20/20 both directions at n=300 | 2: `hsquared-direct-maternal-initial-ignored-1.md`, `hsquared-selfing-pedigree-path-contract-1.md` | Both **KEEP** | `hsquared#212` (direct-maternal-initial-ignored), `hsquared#215` (selfing-pedigree-path-contract) |
| L2 Data Fitter | Gryphon through public `relmat()` vs. Wilson et al. 2010; BTdata bivariate vs. a working `sommer` reference; ≥2 of {`nadiv::warcolak`, `pedigreemm::milk`, `sommer::DT_cpdata`, AGHmatrix examples, a Mrode Ch.3/4 table} | `updates/2026-09-13-fitter-w2.md`. Gryphon via `relmat()` matched Wilson et al. 2010 to 4 decimals through the full public interface; BTdata `sommer::mmes()` reference fixed and agreed within ~0.5% on all four variance components; `warcolak` + `milk` run (meets the "at least two" floor) — both hit real friction: `warcolak` tripped an undocumented-from-R `max_dense_cells` guard, `milk` ran >20 min with neither result nor error and was killed | 1: `hsquared-dense-cells-cap-no-r-escape-1.md` | **KEEP** | `hsquared#214` |
| L3 Doc Reader | ≥8 more of the 21 R articles chunk-by-chunk; Julia Documenter cold read + cross-links both ways; audit the three "covered" counts; comment sibling vignette drift on `#208` rather than new issues | `updates/2026-09-13-docreader-w2.md`. 9 more articles executed (11/21 cumulative); Julia→R cross-links checked, none dead; three-count reconciliation done: DESCRIPTION/README says 7, `validation_status()` prints 6 (wrong set), Julia `capability-status.md` prints a different 6 | 2: `hsquared-vignette-drift-2.md` (comment draft), `hsquared-covered-count-drift-1.md` (new-issue draft) | vignette-drift-2 **MERGE**; covered-count-drift-1 **DROP** (the package discloses the gap itself, in the same function's banner and in `current-limits.Rmd`) | comment on `hsquared#208`; covered-count-drift-1 not posted |
| L4 Mathematician | Boundary-ride frequency behind `HSquared.jl#327` across 20 seeds × 3 families, whether Gaussian AI-REML shares it; adjudicate multivariate/RR estimands against code and docstrings | `updates/2026-09-13-mathematician-w2.md`. Exceeded the ask: 240 non-Gaussian fits (2 designs × 2 DGPs × 3 families × 20 seeds) plus an 80-fit Gaussian AI-REML arm; 240/240 `converged=true`, 73 on the bracket bound, all lower; Gaussian path discriminates honestly; RR out-of-range clamp asymmetry found (Julia throws, R silently clamps); r_g interval transport gap found | 3: `HSquared.jl-convergence-fence-2.md` (comment), `hsquared-rr-out-of-range-1.md` (new issue), `hsquared-missing-definition-2.md` (comment) | convergence-fence-2 **MERGE**; rr-out-of-range-1 **KEEP**; missing-definition-2 **MERGE** | comment on `HSquared.jl#327`; `hsquared#213`; comment on `hsquared#211` |
| L5 Code Reviewer | Re-read the 96-export survey for the `relmat`/`precision` blind spot; test `marker_effects()`/`marker_variance_explained()`, `metafounder_effects()`/`metafounder_groups()`, `covariance_structure_lrt()` on genuinely matching fits | `updates/2026-09-13-codereviewer-w2.md`. Re-read found no blind spot in its own wave-1 survey. Three new fixtures fit: SNP-BLUP, metafounder, nested multivariate. Ship 5 / blocked 1 (`metafounder_effects`, confirmed reserved-and-documented) / stop 0. Running 96-export total now 82 ship / 13 blocked / 1 confirmed-reserved / 0 stop | 0 | — (Rose: "0 drafts. Nothing passed this gate." Also notes she did not independently verify the blind-spot re-read) | none |
| L6 Speed | Add `pedigreemm` and `MixedModels.jl` comparators at q=1k/5k on the half-sib pedigrees, agreement first | `updates/2026-09-13-speed-w2.md`. `pedigreemm` cannot fit the literal saturated design (architectural: no spare residual df) — confirmed directly, not just cited; a 2-record/animal fallback design disagreed with `HSquared.jl` at q=1,000, traced to the lane's own y-generator being indexed by row position, not animal identity — an unidentified-model artifact, not an engine defect; `MixedModels.jl` has no pedigree/relationship-matrix API at all (checked directly) — SKIPPED as a capability gap. Per the agreement-first rule, **no comparator timing was recorded at any size** | 0 | — (no draft filed; Rose's speed-wording grep found zero speed/benchmark hits across all 8 wave-2 drafts) | none |
| V1 Mechanical verify | Gate re-run: receipt structure, draft front-matter, worktree cleanliness, GitHub state, compute caps | `updates/2026-09-13-mech-verify-w2.md`. 6/6 receipts with `## NOT COVERED`; 8/8 drafts structurally complete; both worktrees clean; issue check (run before posting) correctly showed only the 5 wave-1 issues. **Compute-minutes table wrong**: reports "(not set)" for all six receipts, but `mathematician-w2.md` and `speed-w2.md` both carry a `compute_minutes` field in front-matter (3 and 4 respectively) — the tool failed to read a field that exists. See deviations. | — | — | — |
| V2 Rose | Claim-vs-evidence audit of the 8 drafts | `articles/2026-09-13-rose-audit-w2.md`. Re-ran the load-bearing repro for every KEEP and MERGE; corrected line-number citations in 4 drafts in place; narrowed the dense-cells draft's wording so it does not claim an undisclosed envelope; specified posting order and per-item posting hygiene (strip vault-absolute repro paths and the named scratchpad path before posting). **KEEP 4 · MERGE 3 · DROP 1.** No lane trigger fired (L1's re-brief cleared its pause; no lane hit two DROPs in a row) | — | — | — |

## Deviations

| Axis | Deviation | Tag | Owner | Evidence |
| --- | --- | --- | --- | --- |
| Scope | L1's addendum item (a) asked for full independent-reconstruction R↔Julia parity on all 5 named routes (common_env, multi_effect, direct_maternal, random_regression, genomic). Only 3 of 5 got full reconstruction; `multi_effect`/`random_regression` got a liveness-only check. The lane redirected the freed budget to running a genuine `direct_maternal` mismatch to ground, which became the wave's highest-severity finding (`hsquared#212`) | ADAPTIVE | Ada | `updates/2026-09-13-simulator-w2.md` §"NOT COVERED" ("Full independent-construction R↔Julia parity for `multi_effect` and `random_regression`... time budget went to running the `direct_maternal` mismatch to ground instead") |
| Scope | L6's addendum ask (add comparators "at the SAME sizes wave 1 measured", i.e. q=1k and q=5k) produced zero comparator timing at either size, on either design (saturated: `pedigreemm` cannot fit it at all; replicated fallback: disagreement traced to the lane's own generator bug). This is the addendum's own agreement-first rule firing as designed, not a lane failure, but the net delivery against the literal q=1k/q=5k ask is nil | ADAPTIVE | domain reviewer (Gauss/Karpinski, speed lane) | `updates/2026-09-13-speed-w2.md` §"Blew or gaps" and §"NOT COVERED" ("q=5,000 replicated-design comparator... Not run"); addendum text "(agreement first; a disagreeing cell yields no timing)" |
| Evidence / verification | The mechanical-verify receipt's compute-minutes table reports "(not set)" for all six wave-2 receipts, but two of them (`mathematician-w2.md`, `speed-w2.md`) carry an explicit `compute_minutes` front-matter field (3 and 4 minutes). The verifier's own stated method ("checked for front-matter `compute_minutes:` field") did not actually read it correctly for the two receipts that have it | DRIFT | Ada | `grep -n compute_minutes` on `updates/2026-09-13-mathematician-w2.md:12` (`compute_minutes: 3`) and `updates/2026-09-13-speed-w2.md:13` (`compute_minutes: 4`) vs. `updates/2026-09-13-mech-verify-w2.md` §"Compute minutes" table (both listed "(not set)") |
| Evidence / verification | The remaining four receipts (simulator, fitter, docreader, codereviewer) carry no `compute_minutes` field at all in front-matter, including the Data Fitter — the lane this session's own task brief states reported 55 compute minutes against the 2-hour cap. That 55-minute figure does not appear anywhere in `updates/2026-09-13-fitter-w2.md` (front-matter or body; the body's only minute-figures are two "over 20 minutes, killed" mentions for `warcolak` and `milk`). The 2-hour-per-lane compute cap in `GOAL.md` has no reliable receipt-level record for 4 of 6 wave-2 lanes | UNCLEAR | Ada | `grep -n -i "compute\|minutes" updates/2026-09-13-fitter-w2.md` (no `compute_minutes:` field, no "55" anywhere); `GOAL.md` "compute local by default... ≤2 h per lane per wave" |
| Public claims / posting hygiene | Rose's audit gave an explicit, written posting-hygiene instruction ("replace the vault-absolute repro paths... with the script basename, since that path is unreachable for any reader and carries a personal account identifier") applying to all seven posted items, plus a specific instruction to cut a named scratchpad path from the `#208` comment. As posted: `hsquared#212` and `hsquared#215` comply (no vault path). `hsquared#213`, the comment on `HSquared.jl#327`, the comment on `hsquared#211`, and the comment on `hsquared#208` all still carry `~/shinichi-brain/projects/H2-twin/repros/...` paths. The `#208` comment additionally still contains the exact scratchpad path (`/private/tmp/claude-503/.../scratchpad/run_chunks.R`) Rose named for removal, and the literal home-directory form `/Users/z3437171/shinichi-brain/...` | DRIFT | Ada | `gh issue view 213/215/212 --repo itchyshin/hsquared --json body` (212, 215 clean; 213 carries `~/shinichi-brain/...`); `gh issue view 327 --repo itchyshin/HSquared.jl --comments` and `gh issue view 211/208 --repo itchyshin/hsquared --comments` (all three carry unreachable local paths); rose-audit-w2.md "Posting order" preamble and the `#208` per-draft note |
| Safety gates / sequencing | `GOAL.md`'s "Definition of done" states "Wave N+1 starts only after wave N's V1..., V2..., and V3 (Melissa)... are green." `checkpoint.md` (written before this reconciliation ran) records wave-3 lanes as already "LANES RUNNING" concurrently with "Melissa w2 running" — i.e. wave 3 was launched before this wave-2 V3 reconciliation completed | DRIFT | Ada | `checkpoint.md`: "STATE: waves 1–2 CLOSED (15 items filed); wave 3 LANES RUNNING; Melissa w2 running..."; `GOAL.md` "Definition of done": "Wave N+1 starts only after wave N's V1..., V2..., and V3... are green" |
| Handoff state | `memory/AGENT_LOG.md`'s single 2026-09-13 H2-twin entry is stale: it describes "wave 1 IN PROGRESS," reports Melissa's wave-1 counts, and says "wave 2 launched" — it was never extended to record wave 2's closure (Rose KEEP 4/MERGE 3/DROP 1, the 4 new issues + 3 comments, or that wave 3 is already running). This is the same class of drift wave 1's own record flagged as recurring-class candidate 3 | DRIFT | Ada | `memory/AGENT_LOG.md` line 7 (ends at "wave 2 launched (`wf_49fb2954-60a`)"); `MISSION-CONTROL.md` "Current" table (accurately shows wave 2 closed, 15 items) for contrast |
| Handoff state | `MISSION-CONTROL.md`'s "Current" summary table is accurate and up to date for wave 2, but its append-only "Log" section was not extended with wave-2 timestamped entries (last entry: "08:00 PR #328 opened"); the file's two sections are now on different clocks | UNCLEAR | Ada | `MISSION-CONTROL.md` "## Log" section (ends at wave-1 events only) vs. "## Current — 13 September 2026, wave 2 closed" table above it |
| Safety gates | The pre-authorised `test-campaign` GitHub label still does not exist on either repo, unchanged from wave 1; every wave-2 item continues to carry the `[test-campaign]` title-tag workaround instead | ADAPTIVE (recurring, unresolved) | Ada | `gh label list --repo itchyshin/hsquared \| grep -i test` and the `HSquared.jl` equivalent (both empty, this session); `checkpoint.md` "OPEN GATES... Tooling residuals: `test-campaign` label absent" |
| Evidence / verification | Rose's own audit states she did not independently verify L5's claim to have re-read the wave-1 96-export survey for the `relmat`/`precision` blind spot; nothing from L5 passed her gate (0 drafts), so the wave-1 NOT COVERED item on that survey formally still stands even though L5's receipt reports the re-read as done and clean | UNCLEAR | Rose | `articles/2026-09-13-rose-audit-w2.md` §"Lane triggers", L5 bullet: "the lane says it re-read for it, and I did not verify that re-read" |

No deviation found on the **model routing** axis for the six lanes themselves: the wave-2 workflow ran 6 agents, sonnet ×5 + opus ×1 (mathematician), matching the plan's "Sonnet by default, Opus for the Mathematician" exactly. Whether the mechanical verifier (V1) or any lane's dispatch used the specific named agent types the roster lists (as opposed to a generic type) is **UNMEASURED** for wave 2 — no per-lane Workflow-JSON path was available to this reconciliation the way it was for wave 1, so wave 1's recurring-class candidates 1 and 4 (routing-audit blindness to Workflow-internal dispatch names; dual-named lenses collapsing silently) can be neither confirmed nor ruled out this wave, and are carried forward as open questions rather than re-flagged as new deviations.

## Verdict

Wave 2 delivered materially what the addendum asked: L1's re-brief cleared its wave-1 pause and produced the wave's two strongest findings (a documented `engine_control` key silently dropped on `direct_maternal`, and a correctly-scoped selfing contract question); L2 closed its own wave-1 mistake by fitting gryphon through the public `relmat()` route to 4-decimal agreement with Wilson et al. 2010 and adding an independent `sommer` comparator; L3 executed 9 more articles and reconciled three different "covered" counts into one filed issue; L4 ran an order of magnitude more fits than asked and adjudicated two estimand questions cleanly; L5 re-confirmed the export survey's honesty on three new model families with zero stop findings; L6 correctly reported zero comparator timing rather than manufacture a number from a design it had itself found to be unidentified. Rose's audit produced KEEP 4/MERGE 3/DROP 1, added exactly 4 new clusters (above the stopping-rule floor of 3, so the campaign continues), and every posted item's timestamp falls after her audit and in her specified order. Against that, this reconciliation found six things worth carrying into wave 3, none rising to a MUST STOP: a mechanical-verification tool that misreads a front-matter field that is actually present, uneven compute-minutes bookkeeping across lanes (the one lane closest to any cap, the Data Fitter, has no receipt-level number at all), a posting-hygiene instruction Rose wrote explicitly that was followed on 2 of 7 posted items and not the other 5 (one of which still carries a literal home-directory path and the exact scratchpad path she named for removal), wave 3 having already been launched before this wave-2 reconciliation completed despite the plan's own between-wave gate, a durable log file that went stale the same way wave 1's own record warned it would, and the `test-campaign` label still not existing. No source file in either repo was touched, no capability or version claim moved, and no speed or ASReml wording reached either tracker.

## Recurring-class candidates (for Rose, before wave 3)

1. **Mid-campaign `AGENT_LOG.md` entries go stale within hours — RECURRED from wave 1 (candidate 3).** Wave 1's record predicted this would happen again on an unattended run; it did: the single 2026-09-13 entry still reads "wave 1 IN PROGRESS... wave 2 launched" with none of wave 2's outcome folded in.
2. **The routing-audit tool's blindness to Workflow-internal dispatch names — status UNMEASURED this wave, not confirmed recurred.** Wave 1's candidates 1 and 4 (tool can't see lane agent-type names; dual-named lenses collapse silently) could not be checked this wave for lack of a wave-2 Workflow-JSON path in this reconciliation's inputs. Carry forward rather than close.
3. **Pre-plan recon dispatches tripping the explicit-model gate — did not recur (not applicable).** Wave 2 had no separate RECON dispatch to trip it.
4. **Multi-slice PRs hiding a partial failure — did not recur (not applicable).** No multi-slice PR was opened in wave 2's own scope (the wave-2 records PR is explicitly deferred to after this reconciliation, per `checkpoint.md`).
5. **NEW candidate: a written posting-hygiene instruction from Rose's audit was only partially executed.** 5 of 7 posted wave-2 items ignored an explicit, specific instruction (strip vault-absolute/scratchpad paths) that was followed correctly on the other 2. If this recurs a third time, the posting step itself — not the audit step — needs a mechanical check, the way V1 already checks draft structure.
6. **NEW candidate: mechanical verification tooling reports "field absent" when a field is present.** The V1 compute-minutes check is currently unable to do the one job its table exists for (catching a lane that overran the 2-hour cap) because it cannot reliably read the field it is checking.
7. **NEW candidate: between-wave sequencing is not actually gated.** `GOAL.md` states wave N+1 starts only after wave N's V1/V2/V3 are green; wave 3 was launched while wave 2's V3 was still running. If wave N+1 dispatch is not itself blocked on a V3-complete signal, this will recur every wave.
