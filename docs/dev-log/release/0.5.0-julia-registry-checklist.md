# Julia General registry checklist — HSquared.jl `0.5.0` (Arc A19 prep)

**Role:** Shannon scout + Grace registry hygiene (read-only recon → prep doc).  
**Campaign branch:** `claude/lane-h2-twin-20260901`  
**Worktree:** `~/local-scratch/lanes/HSquared.jl-h2-twin-20260901`  
**Date:** 2026-09-01  
**Status:** **PREP ONLY** — no Registrator comment, no General PR, no git tag, no Zenodo deposit.

Sister-pattern sources (local, read this run):

- DRM.jl `docs/dev-log/plans/registry-checklist-2026-08-01.md` (hygiene gate table + S4 owner fence)
- DRM.jl `test/test_aqua.jl`, `.github/workflows/TagBot.yml`, `CITATION.cff`
- GLLVM.jl `docs/src/index.md` (honest “not in General yet” install block)
- GLLVM.jl / DRM.jl handovers (Registrator mechanics, D-111 fence for DRM — **not** binding for H²)

---

## 1. Release model (twin alignment)

| Item | Contract | Source |
|------|----------|--------|
| First public version | **`0.5.0`**, not `1.0.0` | `hsquared/ROADMAP.md`, `docs/dev-log/decisions.md` (2026-07-11) |
| Experimental label | All five D-41 channels on **both** twins | hub D-41; sister live proof: drmTMB/gllvmTMB still `lifecycle-experimental` at `0.7.0` |
| Registration order | **HSquared.jl → General first**, then **hsquared → CRAN** | `docs/dev-log/decisions.md` (2026-07-09 Julia precedes R CRAN) |
| Twin DOI | **One** data-publication DOI for the R+Julia pair | hub D-23; `ROADMAP.md` Release Model |
| Covered surface at `0.5.0` | Five recovery-gated Gaussian models (`public_covered_count = 5`) | `docs/design/35-api-stability-contract.md` |

**Version alignment target (release pass, not this prep doc):**

| Repo | Current tree version | Target at registration |
|------|----------------------|---------------------------|
| HSquared.jl | `0.0.1` (`Project.toml`) | `0.5.0` |
| hsquared | `0.1.0.9000` (`DESCRIPTION`) | `0.5.0` |

Both tags **`v0.5.0`** should point at the **same campaign merge commit pair** (engine + R twin parity evidence in check-log / after-task).

---

## 2. F7 resolution (registry vs S5 / G10)

**Fog ticket F7** (`LOOP/ultra-plan.md`): does Julia General registration require frozen S5 / G10 sign-offs closed first?

**Prep resolution (A19 hygiene scope):**

| Layer | Requires S5 green + G10 sign-offs? | Notes |
|-------|-----------------------------------|-------|
| **This checklist + hygiene PRs** (Aqua, TagBot, compat, silence, docs honesty) | **No** | Safe parallel to B2/B3 while S5/G10 remain open |
| **Maintainer `@JuliaRegistrator register` submit** | **Owner ASK** — recommend **yes, wait** until B6 batch gates green: A18 CI/deploy hygiene, A14–A16 bridge parity in CI, A17 docs honesty, A09 G10 dossiers signed | Registry is irreversible public distribution; sister DRM pattern waits explicit Shinichi OK after hygiene (S4 gate) |
| **Twin DOI deposit** | After **both** twins tagged `0.5.0` and Julia **accepted** in General | Single Zenodo (or equivalent) record cites **both** GitHub repos + both version strings |
| **R CRAN local gate (A20)** | Julia must be **registered and tagged** first | `docs/dev-log/decisions.md` 2026-07-09 |

**Ordering summary:**

```text
hygiene prep (A19 prep) → Shinichi OK → tag v0.5.0 → Registrator → General merge
→ twin DOI → hsquared CRAN submit (A20+)
```

S5/G10 are **not** mechanical blockers for writing this checklist or landing hygiene commits; they are **evidence blockers** Shinichi should weigh before the actual Registrator trigger.

---

## 3. Must-green before Registrator (sister-derived gate table)

| # | Gate | HSquared.jl at A19 prep (2026-09-01) | State |
|---|------|--------------------------------------|-------|
| 1 | Integration base = campaign branch merged to `main` with R twin parity evidence | Worktree ahead on `claude/lane-h2-twin-20260901`; not merged | **OPEN (A23)** |
| 2 | No load-time `println` on `using HSquared` | `src/` has no bare `println` (scripts/sim only) | **GREEN** (spot-check) |
| 3 | `Project.toml`: `name`, fixed `uuid`, `version = "0.5.0"`, `[compat]` for every `[deps]` + `julia` | `version = 0.0.1`; compat present for listed deps | **OPEN** (bump at release pass) |
| 4 | Aqua hygiene (`test/test_aqua.jl`, `deps_compat=true`, wired in `runtests.jl`) | **Missing** — no `test_aqua.jl` | **OPEN** |
| 5 | Local `Pkg.test()` green on register tip | Not run for this prep doc | **UNVERIFIED** |
| 6 | Linux CI green on register tip (Julia 1.10 + current) | CI workflows present; conclusion not re-checked this prep | **UNVERIFIED** |
| 7 | TagBot workflow present | **Missing** — only `CI.yml` + `Documenter.yml` | **OPEN** |
| 8 | LICENSE MIT; no vendored GPL engine blobs in package tree | `LICENSE` MIT present | **GREEN** (spot-check) |
| 9 | README + Documenter honesty (no “in General” / `Pkg.add("HSquared")` claim until merged) | README honest scaffold; no false General claim spotted | **GREEN** — refresh at A17 |
| 10 | `CITATION.cff` version matches `Project.toml` | **Missing** | **OPEN** |
| 11 | Not already in General (or deliberate re-register) | Expected absent — confirm before submit | **UNVERIFIED** |
| 12 | Documenter deploy key + `DOCUMENTER_KEY` for TagBot SSH | Documenter workflow exists; TagBot absent | **PARTIAL** |
| 13 | Explicit **Shinichi OK** to trigger Registrator | LOOP GOAL: Always ASK on registry registration | **S4 / owner gate** |
| 14 | R twin `hsquared` at `0.5.0` with five D-41 channels staged for CRAN gate | R still `0.1.0.9000` | **OPEN (A20)** |

---

## 4. Registry mechanics (URLs + steps)

### Canonical URLs

| Resource | URL |
|----------|-----|
| Julia General registry | https://github.com/JuliaRegistries/General |
| Registrator app / docs | https://github.com/JuliaRegistries/Registrator |
| TagBot | https://github.com/JuliaRegistries/TagBot |
| Expected General package path (after merge) | https://github.com/JuliaRegistries/General/tree/master/H/HSquared |
| HSquared.jl repo | https://github.com/itchyshin/HSquared.jl |
| hsquared R twin | https://github.com/itchyshin/hsquared |
| Engine Documenter | https://itchyshin.github.io/HSquared.jl/ |
| R pkgdown | https://itchyshin.github.io/hsquared/ |

### Owner steps (Shinichi — **ASK boundary**)

Agents prepare hygiene; **only Shinichi** (or explicit handoff) performs:

1. **Install Registrator** on `itchyshin/HSquared.jl` if not already (GitHub App).
2. **Merge** campaign branch to `main` after A23 completion panel.
3. **Bump** `Project.toml` → `0.5.0`; add matching `NEWS.md` section; add `CITATION.cff`.
4. **Commit** version bump on `main` (release commit).
5. **Trigger registration** — either:
   - comment on an issue/PR: `@JuliaRegistrator register` on the **release commit**, or
   - use Registrator web UI with repo + commit SHA.
6. **Wait** for Registrator bot PR against `JuliaRegistries/General` → review + merge (auto if CI green).
7. **TagBot** (after TagBot.yml + `DOCUMENTER_KEY` landed): merge triggers `v0.5.0` tag, or tag manually if needed.
8. Verify: `julia -e 'using Pkg; Pkg.add("HSquared"); using HSquared'` on clean Julia 1.10+.
9. **Twin DOI** (single deposit, D-23): Zenodo (or institutional archive) with metadata listing **both** `hsquared` and `HSquared.jl`, version `0.5.0`, MIT, and links to both GitHub repos. Record DOI in both `DESCRIPTION` / `CITATION.cff` / README once issued.
10. **Then** hsquared CRAN submission arc (A20+) with `Version: 0.5.0` and the shared DOI.

### Agent-safe prep (no owner credential)

- Add `test/test_aqua.jl` (mirror DRM.jl: `Aqua.test_all(HSquared; ambiguities=false, deps_compat=true)`).
- Add `.github/workflows/TagBot.yml` (mirror DRM.jl / GLLVM.jl).
- Wire Aqua in `test/Project.toml` + `runtests.jl`.
- Add `CITATION.cff` draft (twin-aware abstract; version `0.5.0` placeholder).
- Documenter install block: copy GLLVM pattern — `Pkg.add(url=...)` until General merge; switch after.
- Rose pass: no “registered” / `Pkg.add("HSquared")` language until General PR merges.

---

## 5. Twin DOI sequencing (D-23)

| Step | Actor | Artifact |
|------|-------|----------|
| 1 | Maintainer | Tag `HSquared.jl` `v0.5.0` at release commit |
| 2 | Maintainer | General registry accepts `HSquared` `0.5.0` |
| 3 | Maintainer | Tag `hsquared` `v0.5.0` at twin-aligned commit |
| 4 | Maintainer | **One** Zenodo deposit: title cites H² twin; `relatedIdentifier` / README links both repos; version `0.5.0` |
| 5 | Maintainer | Add DOI to `hsquared` DESCRIPTION `Note` / `X-Citation`, `HSquared.jl` `CITATION.cff`, both READMEs |
| 6 | Maintainer | CRAN submit `hsquared` 0.5.0 (after A20 local gate) |

**Do not** open two DOIs for the pair. **Do not** CRAN-submit before Julia is installable via `Pkg.add("HSquared")` from General (hub decision + G9 unlazy gate).

---

## 6. Sister contrast (what H² copies vs defers)

| Pattern | DRM.jl / GLLVM.jl | H² at A19 prep |
|---------|-------------------|----------------|
| Aqua + `deps_compat` | DRM `test/test_aqua.jl` | **Adopt** — not yet wired |
| TagBot workflow | Both sisters | **Adopt** — missing |
| General membership | Both **not** in General (D-111 OFF for DRM through v0.7.0) | Same — prep only |
| Owner Registrator fence | Explicit Shinichi OK (DRM S4) | **Adopt** — LOOP Always ASK |
| Install docs when unregistered | GLLVM `Pkg.add(url=...)` | **Adopt** in Documenter when refreshed (A17) |
| Version / tag drift lesson | DRM tags vs `Project.toml` drift noted in checklist | **Avoid** — bump `Project.toml`, `CITATION.cff`, git tag together at `0.5.0` |

---

## 7. Do-not-touch fences (this arc)

| Fence | Rule |
|-------|------|
| **No Registrator trigger** | Do not comment `@JuliaRegistrator register`; do not open General PR |
| **No git tag / no push** | Prep doc + hygiene PRs only; merge/push = Shinichi ASK |
| **No CRAN submit** | A20 is separate |
| **No covered flip** | `public_covered_count` stays 5 until predeclared gate |
| **PR137 / PR274 foreign files** | Stay off campaign branch scope |
| **R lane edits** | Registry checklist lives in Julia worktree; R `0.5.0` bump is A20 |

---

## 8. Open blockers after this prep doc

1. Hygiene implementation rows 4, 7, 10, 12 in §3 (Aqua, TagBot, CITATION, TagBot key).
2. Campaign merge to `main` + A23 completion panel.
3. Shinichi explicit OK for Registrator (LOOP standing ASK).
4. Twin `0.5.0` version bump synchronized across both repos.
5. Twin DOI deposit after Julia General acceptance.
6. S5 / G10 — evidence Shinichi should weigh before submit (not hygiene-doc blockers).

---

## 9. Evidence pointer

When registration eventually happens, bank in:

- `docs/dev-log/check-log.md` — exact commands (`Pkg.test`, Aqua, post-register `Pkg.add`)
- `docs/dev-log/after-task/` — Rose audit, General PR URL, tag SHA, DOI URL
- `LOOP/arcs.md` — A19 → `done` only after General merge + verify install
