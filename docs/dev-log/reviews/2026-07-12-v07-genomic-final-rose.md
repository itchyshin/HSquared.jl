# Rose final claim-vs-evidence audit — v0.7 genomic cross-twin arc

**Date:** 2026-07-12  
**Julia branch:** `codex/2026-07-12-v07-genomic-activation`  
**R branch:** `codex/2026-07-12-v07-genomic-activation`  
**Endpoint audited:** negative / held  
**Final re-audit verdict:** **CLEAN-WITH-LIMITATIONS**  
**Initial verdict:** `CHANGES_REQUIRED` (retained below as audit history)

## Final re-audit after the negative-endpoint repair

The two activation blockers are resolved.

- The ordinary R `engine = "fit"` path once again rejects `genomic()` as
  experimental and opt-in. The live supplied-Q and marker-Q identity tests now
  use the explicit `engine = "julia", target = "genomic"` route, and a focused
  negative test pins the default-path rejection.
- `DESCRIPTION`, roxygen sources, generated Rd files, `formula_status()`, the
  grammar and public/status/debt registers, NEWS, and the public vignettes now
  agree that genomic GREML is opt-in and default activation remains
  unimplemented.
- Grace has completed a hardening recheck with a final `CLEAN` verdict. The
  recheck explicitly says the repaired harness is ready only for a fresh run and
  that rows from the pre-repair harness cannot become activation evidence.
- Julia and R validation-status implementations remain unchanged,
  `public_covered_count` remains 5, no G10 exists, and no confirmation was run.

The final terminology recheck is clean. `ROADMAP.md`, the Julia capability and
validation-debt rows, `docs/src/genomic-models.md`, the check-log, the recovery
checkpoint, and the matching R surfaces now use the same evidence boundary:

> 432-seed diagnostic pilot over the preregistered cells, generated by the
> pre-repair Julia-only harness.

The stale phrases “full preregistered pilot,” “432-seed preregistered pilot,”
and “432-seed activation pilot” no longer occur on the audited active surfaces.
The checkpoint keeps the numeric stop result while explicitly denying
authoritative end-to-end activation status. Source and generated R documentation
remain synchronized, `git diff --check` is clean in both twins, and the default
route remains fenced by a deliberate negative test.

Rose therefore closes the **negative endpoint** as
`CLEAN-WITH-LIMITATIONS`: mergeable hardening may retain the explicit opt-in
route, construction/identity evidence, comparator packet, diagnostics, and
honest negative result. It must not add default activation, launch confirmation
from the old rows, claim broad or end-to-end recovery, move a capability/count,
or imply G10. A future activation attempt requires optimizer localization, a
fresh preregistered end-to-end campaign from the repaired committed harness,
independent verification, a new Rose audit, and maintainer G10.

## Post-Rose live-gate honesty event

After the first `CLEAN-WITH-LIMITATIONS` verdict, the exact-commit live run found
one more false-green gate. A stale assertion referenced the removed
`fit_explicit` object, so `devtools::test()` reported a required live-test
failure but still returned shell status zero. The gate checked skips and Julia
shutdown, but not the reporter's failure summary, and therefore printed PASS.

R commit `d4cefe10c155f87625bd5304d77e388a657c4eca` repairs both sides of the
defect:

- the stale default-route identity assertion is removed, consistent with the
  held opt-in endpoint;
- `tools/run-v07-genomic-live-gate.sh` now rejects either a `Failed` section or
  a positive `FAIL` count;
- the gate self-test proves `FAIL 0` is accepted and `FAIL 1` is rejected, in
  addition to the existing zero-skip/positive-skip controls.

The commit-pinned live gate then passed at R
`d4cefe10c155f87625bd5304d77e388a657c4eca` and Julia
`fade1d02cb2a9b404ec5d2d97da73fa291ac1237`. Rose independently inspected the
committed parser and ran its self-test (`V07_GENOMIC_LIVE_GATE_SELFTEST_PASS`);
an attempted duplicate full run correctly refused while Grace's concurrent
recheck made the Julia tree dirty, demonstrating that the clean-tree precondition
also fails closed. Grace's completed post-event recheck independently records the
exact pinned gate PASS and retains a final `CLEAN` reproducibility verdict.

This event **does not broaden or reverse the endpoint verdict**. It strengthens
the reproducibility gate and removes a stale assertion; it creates no recovery
evidence, default activation, confirmation result, capability/count move, or
G10. The final Rose verdict remains **CLEAN-WITH-LIMITATIONS** for the negative,
opt-in endpoint. The gate itself is now another explicit tests-of-the-tests
obligation for any future activation campaign.

## Initial audit bottom line (superseded by the re-audit above)

The scientific decision is conservative and correct: the 432-row run does not
support activation. Three cells were below the frozen 95% convergence threshold,
two additional cells exceeded the 2,000-replicate precision cap, confirmation did
not run, and the rows exercise Julia construction plus supplied-Q fitting rather
than the public R formula route. There is no G10 decision, neither capability count
changed, and the supplied-precision Julia estimator is the only genomic REML
capability that remains covered.

The branch is **not mergeable as a public-activation PR**, however. The held
decision is recorded in the newest status deltas, but the R implementation still
turns the candidate into the default route and several user-facing surfaces
describe that behavior as current. In addition, the recovery checkpoint repeatedly
calls the pre-repair run the full preregistered pilot before later demoting it to a
diagnostic. Those contradictions must be removed before this arc can be closed.

## Claim-to-evidence verdict

| Claim | Evidence | Verdict |
| --- | --- | --- |
| Frozen marker construction and marker-Q route identity | deterministic fixture, independent base-R construction, live R-Julia identity tests, provenance mutation gates | supported at validation scale |
| Exact-candidate-Q external solver comparison | fresh hash-pinned `blupf90+` point-estimate packet | supported as one point-estimate comparison only |
| Broad marker-route recovery | no confirmation; three low-convergence cells and two precision blockers | **not supported** |
| End-to-end R formula-route recovery | simulation calls Julia construction and supplied-Q fitting directly | **not tested** |
| Public/default activation | no passing recovery chain, final Rose clearance, or G10 | **must remain held** |
| Count movement | Julia validation-status implementation and R validation-status implementation are unchanged; status text retains `public_covered_count = 5` | no movement |

## Initial required changes (audit history)

### 1. Blocker — the negative endpoint is not structurally enforced

`hsquared/R/hsquared.R` still detects `default_genomic` and immediately dispatches
`hs_fit_julia_genomic_payload()` under the ordinary `engine = "fit"` path. If the
current R implementation commit is merged, the default route is activated despite
the recorded stop decision. The implementation/evidence commit therefore cannot be
merged intact under the negative endpoint.

Split the R work before any merge:

- retain reusable validation, strict provenance, explicit-target, fixture, and
  diagnostic hardening where it does not change the default public route;
- revert or omit the `engine = "fit"` genomic auto-dispatch and its default-route
  expectations;
- keep the activation delta on a clearly held branch until a fresh preregistered
  end-to-end campaign passes, Rose re-audits it, and the maintainer gives G10.

Merely adding “held” prose is insufficient while the merge itself would activate
the behavior.

### 2. Blocker — public surfaces still present activation as current behavior

The following R surfaces say the genomic model “auto-routes,” “fits by default,” or
is a current default target without placing the hold before that claim:

- `DESCRIPTION`;
- `R/hsquared.R` and generated `man/hsquared.Rd`;
- `R/hs_control.R` and generated `man/hs_control.Rd`;
- `R/genomic-markers.R` and generated `man/genomic_markers.Rd`;
- `R/formula-status.R`, including its printed runtime diagnostic;
- `docs/design/02-formula-grammar.md`;
- `docs/design/capability-status.md`, especially the `hsquared()` entry-point row;
- `docs/design/validation-debt-register.md`, especially the Genomic/QTL grammar
  and univariate-model rows;
- `vignettes/articles/genomic-prediction.Rmd`, including its opening, route table,
  worked calls, and conclusion;
- `vignettes/hsquared.Rmd`.

The newest NEWS bullet, claims-register row, genomic capability row, model-status
article, Julia ROADMAP, and Julia genomic-models page do state the hold. That makes
the repository internally contradictory rather than safely conservative.

After the implementation split, regenerate the manuals and make all public/status
surfaces agree on one present-tense truth: the explicit experimental genomic target
and reusable construction evidence exist, while default auto-routing is a held
candidate and is not shipped or merge-approved.

### 3. Major — “full preregistered pilot” overstates the authority of the run

`docs/dev-log/recovery-checkpoints/2026-07-12-v07-genomic-activation-pilot-stop.md`
opens with “The full preregistered pilot tier ran” and many status surfaces call it
a “432-seed preregistered pilot.” The same checkpoint later concedes that the run
used the pre-repair harness, used the wrong initial sizing summary, had mutable
campaign records and weak resume validation, and bypassed the R route. The Fisher
recheck classifies the repaired harness as clean only for a Julia diagnostic and
requires fresh state; the Grace audit remains `CHANGES_REQUIRED`.

Use one unambiguous label everywhere:

> 432-seed diagnostic pilot over the preregistered cells, generated by the
> pre-repair Julia-only harness; decision-relevant for stopping, but not the
> authoritative preregistered end-to-end activation pilot.

This preserves the valid conservative stop without laundering the rows into an
evidence tier that the audits explicitly reject. Update the checkpoint title/lead,
check-log, Julia ROADMAP/status/debt/genomic-model page, and R NEWS/claims/status/
debt/vignettes consistently.

### 4. Major — the reproducibility review has no closing recheck

The Fisher/Noether review records a repaired-harness recheck, but
`2026-07-12-v07-genomic-reproducibility-grace.md` still ends
`CHANGES_REQUIRED`. Some listed defects appear repaired in the current uncommitted
harness, but no independent Grace recheck establishes that all seven findings and
their negative controls now pass. Do not describe reproducibility review as closed.

Either obtain and record a Grace recheck against the final commit or leave this as
an explicit carried-over blocker. Under either outcome, the existing 432 rows stay
diagnostic and cannot be upgraded retrospectively.

## Neighbouring-surface findings

- `docs/src/mission-control.md` still says there is no public genomic model-spec
  default. That statement matches the required negative endpoint but conflicts
  with the current R branch behavior; resolve the behavior, not this fence.
- Older NEWS entries describe genomic fitting as opt-in. They are historical
  entries and need not be rewritten, but the newest entry must remain visibly
  authoritative and the generated current-reference pages must not contradict it.
- The exact-Q `blupf90+` comparison is correctly fenced as point-estimate evidence;
  it must not be used to bridge over the failed/absent recovery leg.
- The ratio remains correctly labelled `genomic_variance_ratio` on the declared
  relationship scale. No audited surface rebrands it as pedigree or population
  narrow-sense heritability.
- No audited surface claims production-scale genomic fitting, APY activation,
  interval calibration, or exact ridge-regularized GBLUP-SNP-BLUP equivalence.

## Re-audit gate

Rose can return `CLEAN-WITH-LIMITATIONS` for the negative endpoint only after:

1. the default-route implementation is removed from every mergeable R delta or
   isolated on an explicitly held branch;
2. source and generated public docs are synchronized to that behavior;
3. all references call the 432 rows a diagnostic pilot, not authoritative
   end-to-end recovery;
4. the unresolved Grace verdict is rechecked or explicitly carried over;
5. focused tests demonstrate the explicit experimental route still works, while a
   deliberate ordinary-default genomic call goes red with the intended held error;
6. counts remain unchanged and no G10/public-activation status edit is staged.

Until then: **do not merge the R default activation, do not launch confirmation,
do not promote a capability, and do not change `public_covered_count`.**
