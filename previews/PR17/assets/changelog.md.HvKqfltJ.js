import{_ as o,o as i,c as a,ao as d}from"./chunks/framework.DhUTZZSa.js";const u=JSON.parse('{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"changelog.md","filePath":"changelog.md","lastUpdated":null}'),t={name:"changelog.md"};function n(c,e,r,l,s,p){return i(),a("div",null,[...e[0]||(e[0]=[d('<h1 id="Changelog" tabindex="-1">Changelog <a class="header-anchor" href="#Changelog" aria-label="Permalink to &quot;Changelog {#Changelog}&quot;">​</a></h1><h2 id="Unreleased" tabindex="-1">Unreleased <a class="header-anchor" href="#Unreleased" aria-label="Permalink to &quot;Unreleased {#Unreleased}&quot;">​</a></h2><ul><li><p>Added pedigree normalization and sparse <code>Ainv</code> construction utilities.</p></li><li><p>Added low-level animal-model specification validation.</p></li><li><p>Added dense Gaussian ML/REML log-likelihood evaluation at supplied variance components.</p></li><li><p>Added sparse REML log-likelihood evaluation at supplied variance components using the Henderson MME determinant identity.</p></li><li><p>Added experimental <code>fit_sparse_reml()</code> and <code>fit_animal_model(...; target = :sparse_reml)</code> dispatch for REML-only sparse validation optimization.</p></li><li><p>Added an experimental sparse prediction-error-variance / reliability path: <code>prediction_error_variance(...; method = :selinv)</code> and <code>reliability(...; method = :selinv)</code> use a Takahashi selected inverse of the sparse Henderson MME coefficient matrix (kernel adapted from DRM.jl, MIT). The default stays <code>:dense</code> and matches it to machine precision on tiny fixtures.</p></li><li><p>Added an experimental average-information REML estimator: <code>fit_ai_reml()</code> and <code>fit_animal_model(...; target = :ai_reml)</code> estimate the two variance components by AI/Newton steps on the sparse Henderson MME (score from the selected inverse, AI matrix from working-variate re-solves). Validated to recover the same optimum as the NelderMead optimizers; REML-only, Gaussian, experimental.</p></li><li><p>Added <code>genomic_relationship_matrix()</code> — the VanRaden (2008) genomic relationship matrix <code>G</code> from a 0/1/2 (or dosage) marker matrix. Construction utility only (Phase 2 start); no genomic fitting yet.</p></li><li><p>Added <code>genomic_relationship_inverse()</code> — the ridge-regularized dense inverse <code>inv(G + ridge·I)</code> of a genomic relationship matrix, intended for later GBLUP use. Construction utility only; not wired into model fitting, and no single-step (<code>H</code>-matrix) blending.</p></li><li><p>Added <code>fit_gblup()</code> — genomic BLUP at supplied variance components: feeds a genomic <code>Ginv</code> into the existing Henderson MME (<code>Ginv</code> in the <code>Ainv</code> slot). Reproduces an independent dense MME and reproduces pedigree BLUP when <code>G = A</code>; experimental, supplied-variance only, no variance-component estimation, no single-step, no external comparator.</p></li><li><p>Added <code>fit_snp_blup()</code> and <code>centered_markers()</code> — SNP-BLUP / RR-BLUP marker effects via the existing Henderson MME (<code>Z = W</code> centered markers, identity prior, <code>σ²_marker = σ²_g/k</code>). <code>gebv = W·â</code> equals GBLUP to machine precision (the GBLUP↔SNP-BLUP equivalence, validated via the marginal <code>V</code>); experimental, supplied-variance only, unweighted VanRaden method-1.</p></li><li><p>Internal: deduped the numerator-relationship recursion into <code>_numerator_relationship(pedigree[, rows])</code> (one source shared by <code>inbreeding_coefficients</code> and single-step <code>A₂₂</code> construction); removed the test-only duplicate. No public API or behavior change.</p></li><li><p>Documented and tested genomic reliability/PEV/accuracy: for a GBLUP fit the existing extractors use the regularized genomic self-relationship <code>diag(inv(Ginv)) = diag(G) + ridge</code> as the denominator (the ridge perturbs the reported reliability/accuracy), and <code>method = :selinv</code> PEV matches the dense diagonal. No logic change.</p></li><li><p>Added an internal single-step H-inverse construction utility <code>_single_step_Hinv</code> — <code>H⁻¹ = A⁻¹ + scatter(τG⁻¹ − ωA₂₂⁻¹)</code> on the genotyped rows (with the subtle <code>A₂₂⁻¹ = inv(A[g,g])</code>, not <code>(A⁻¹)[g,g]</code>). Property-checked (reduction, locality, symmetry, distinctness, scattered rows, singular-<code>G</code> guard); unexported, not wired into fitting, blending/τ/ω/ridge defaults not comparator-validated.</p></li><li><p>Validated genomic REML variance-component estimation: the existing REML optimizers (<code>fit_ai_reml</code>, <code>fit_sparse_reml</code>) estimate σ²g/σ²e on a genomic <code>Ginv</code> spec — AI and NelderMead reach the same optimum and <code>fit_gblup</code> at the estimate reproduces the REML breeding values. Experimental; no external comparator; no new code (reuses the Phase-1 optimizers).</p></li><li><p>Added a &quot;Genomic models&quot; documentation page with runnable examples for <code>G</code>/<code>Ginv</code>, GBLUP, genomic REML, SNP-BLUP, and the single-step <code>H⁻¹</code> utility, and an explicit experimental / not-yet-R-wired / no-external-comparator boundary.</p></li><li><p>Added experimental heritability uncertainty: <code>variance_component_covariance</code>, <code>variance_component_standard_errors</code>, <code>heritability_standard_error</code>, and <code>heritability_interval</code> — a logit-transform delta interval (always in (0,1)) built on the REML AI matrix, with a self-contained standard-normal quantile. Asymptotic / REML-only; wide and unreliable at small n.</p></li><li><p>Added <code>repeatability_mme()</code> — the first Phase-3 slice: a supplied-variance Henderson solve of the two-random-effect repeatability / permanent-environment animal model (additive <code>a</code> + permanent-environment <code>pe</code>) via a block-diagonal relationship precision. Matches an independent marginal-GLS BLUP and reduces to the animal model as <code>σ²pe → 0</code>. Experimental, supplied-variance only — no REML estimation of the three components, no R-facing model-spec yet.</p></li><li><p>Added <code>fit_repeatability_reml()</code> — REML estimation of (σ²a, σ²pe, σ²e) for the repeatability / permanent-environment model (dense two-random-effect REML loglik + NelderMead), returning the repeatability coefficient <code>t</code> and <code>h²</code>. Deterministic checks (loglik reduces to the animal-model REML at σ²pe=0; BLUPs match <code>repeatability_mme</code>; optimum beats a grid) + a one-off seeded recovery. Experimental, dense/validation-scale, REML-only; no committed recovery test (suite kept RNG-free), no intervals, no R-facing model-spec.</p></li><li><p>Added <code>two_effect_mme()</code> — the general supplied-variance kernel for two independent random effects (each with its own incidence, relationship inverse, and variance), covering common-environment and maternal-environment models; <code>repeatability_mme</code> is now its <code>Z2=Z1, A2=I</code> special case. Validated against an independent marginal-GLS BLUP. Experimental; no correlated direct–maternal genetic, no estimation, no R-facing model-spec.</p></li><li><p>Added <code>fit_two_effect_reml()</code> — REML estimation of the two-effect-model variances (common-environment <code>c²</code>, maternal variance, etc.) via the dense two-effect REML loglik; <code>fit_repeatability_reml</code> is the <code>Z2=Z1, A2=I</code> reduction. Experimental, dense/validation-scale, REML-only; no committed recovery test, no intervals, no R-facing model-spec.</p></li><li><p>Added a &quot;Standard QG models&quot; documentation page with runnable examples for repeatability (MME + REML, repeatability coefficient), common-environment, and maternal-environment models, with the experimental / not-yet-R-wired boundary.</p></li><li><p>Validation status (v0.1 gate): corrected the <code>V1-AI-REML</code> evidence to cite the committed finite-difference REML Hessian check (<code>V1-HERIT-CI</code>) instead of an uncommitted &quot;250-animal observed-information&quot; claim, and recorded the R-lane external validation — <code>V1-MRODE-FIT</code> and <code>V1-COMPARATORS</code> move to <code>covered_external</code> (the engine recovers the published gryphon REML estimate, Wilson 2010 h²=0.470, via supplied <code>A_gryphon</code>, and agrees with sommer, within the maintainer-signed-off band). Status rows + tests only; no engine change.</p></li><li><p>Reconciled <code>ROADMAP.md</code> with merged reality: Phases 1-3 engine utilities (genomic prediction, single-step <code>H</code>-inverse, AI-REML, the Takahashi selected inverse, repeatability / two-effect models) are now listed as landed experimental, validation-scale utilities instead of &quot;not implemented&quot;.</p></li><li><p>Added <code>multivariate_mme()</code> — the first Phase-4 (multivariate Gaussian) slice: a supplied-covariance Henderson solve of the balanced multi-trait animal model (Kronecker MME with genetic precision <code>Ainv⊗G0⁻¹</code> and residual <code>I⊗R0⁻¹</code>), returning per-trait fixed effects and EBVs. Validated against an independent loop-built MME, an independent marginal-GLS BLUP, the univariate animal model at <code>t=1</code>, and <code>t</code> independent single-trait fits at diagonal <code>G0</code>/<code>R0</code>. Experimental, dense/validation-scale; balanced data with a shared design only; no <code>G0</code>/<code>R0</code> estimation, no missing records, no R-facing model-spec.</p></li><li><p>Added <code>genetic_correlation()</code> — converts a covariance matrix (e.g. <code>G0</code>, <code>R0</code>) to the corresponding correlation matrix; also extracts the genetic correlation from a <code>multivariate_mme</code> result.</p></li><li><p>Extended <code>multivariate_mme()</code> to <strong>unbalanced / missing-trait records</strong>: a <code>missing</code> or <code>NaN</code> entry in <code>Y</code> marks an unobserved trait, handled via the per-individual residual precision <code>inv(R0[Sᵢ, Sᵢ])</code>; balanced data reduces to the previous fast path. Validated against an independent loop-built MME and a marginal-GLS BLUP with per-individual residual blocks.</p></li><li><p>Added <code>fit_multivariate_reml()</code> — estimates the multi-trait genetic/residual covariances <code>G0</code>, <code>R0</code> by dense REML (log-Cholesky-parameterized Nelder–Mead; handles missing records), returning the estimates, correlations, per-trait heritabilities, and breeding values at the estimate. Validated by the <code>t = 1</code> reduction to the univariate REML (the loglik equals <code>sparse_reml_loglik</code> exactly), grid-beating, and EBV consistency with <code>multivariate_mme</code>. Experimental, dense/validation-scale; opt-in seeded recovery exists outside CI, but is not multi-seed calibrated and has no external-comparator parity yet.</p></li><li><p>Added copy-returning Julia-side multivariate result accessors: <code>variance_components</code>, <code>fixed_effects</code>, <code>breeding_values</code>/<code>EBV</code>/<code>BLUP</code>, and REML-only <code>heritability</code>. These wrap existing <code>multivariate_mme</code> / <code>fit_multivariate_reml</code> fields and do not change <code>result_payload()</code> or the R bridge contract.</p></li><li><p>Added <code>test/fixtures/phase4_multitrait_parity/</code>, a deterministic two-trait CSV fixture for R-lane sommer/ASReml/BLUPF90 parity work. It records a Julia REML target (<code>G0</code>, <code>R0</code>, beta, EBVs, h², and loglik) and CI checks fast self-consistency at the stored target covariances. Its README and <code>docs/dev-log/decisions/2026-06-14-multitrait-comparator-protocol.md</code> define the comparator protocol; it is not external comparator evidence.</p></li><li><p>Added opt-in <code>sim/phase4_multivariate_reml_recovery.jl</code> for seeded two-trait known-truth recovery of the unstructured multivariate REML estimator outside CI. Default seed <code>20260616</code> passed with relative errors <code>G = 0.174500</code> and <code>R = 0.131056</code> against thresholds <code>0.25</code> and <code>0.20</code>. This is internal recovery evidence, not multi-seed calibration or external comparator parity.</p></li><li><p>Updated the validation-status documentation table to include the current Phase 2, Phase 3, Phase 4, and Phase 4B validation rows and their claim boundaries.</p></li><li><p>Hardened the multivariate engine after a 7-lens adversarial review (each finding verified by running Julia): <code>multivariate_mme</code> / <code>fit_multivariate_reml</code> now reject non-finite observed phenotypes (<code>Inf</code> — only <code>missing</code>/<code>NaN</code> mark an unobserved trait), non-finite <code>X</code>/<code>Z</code>/<code>Ainv</code>, and empty-trait columns with clear errors (previously a silent all-NaN result or an opaque <code>SingularException</code>); and <code>fit_multivariate_reml</code>&#39;s <code>loglik</code> is now the <strong>full</strong> REML log-likelihood including the <code>(N−p&#39;)·log(2π)</code> constant, on the same scale as the other loglik functions (safe for LRT/AIC). Regression-tested.</p></li><li><p>Added Phase-4B structured genetic covariance support for the dense multivariate REML engine: <code>diagonal_covariance()</code>, <code>lowrank_covariance()</code>, and <code>factor_analytic_covariance()</code> build <code>diag(σ²)</code>, <code>ΛΛ&#39;</code>, and <code>ΛΛ&#39; + Ψ</code>, and <code>fit_multivariate_reml(...; genetic_structure = :diagonal | :lowrank | :factor_analytic, rank = K)</code> estimates constrained genetic covariance structures while keeping residual <code>R0</code> unstructured. Validated by deterministic constructor identities, structure metadata, copy-returning <code>genetic_structure()</code>/<code>genetic_loadings()</code>/<code>genetic_uniqueness()</code> accessors, loglik equality to the existing evaluator, PSD/PD covariance checks, constrained loglik ≤ the unstructured fit, and deterministic sign-canonicalization of returned loading columns. The <code>2026-06-14-loading-rotation-identifiability</code> decision note records this as a sign-only metadata convention and defers full loading rotation/interpretation. Experimental, dense/validation-scale; no R-facing covariance-structure syntax, bridge change, full loading rotation/interpretation convention, or external comparator yet.</p></li><li><p>Added opt-in Phase-4B recovery harness <code>sim/phase4b_structured_covariance_recovery.jl</code> for seeded low-rank and factor-analytic covariance recovery outside CI. This strengthens internal recovery evidence but does not add R-facing syntax, bridge payload fields, or external comparator parity.</p></li><li><p>Added a &quot;Multivariate models&quot; documentation page with a runnable balanced two-trait example and the experimental / not-yet-R-wired boundary.</p></li><li><p>Expanded planned backend marker/control vocabulary to include threaded CPU, AMDGPU, Metal, and oneAPI markers alongside CPU, CUDA, and auto metadata.</p></li><li><p>Added <code>backend_info()</code> typed status diagnostics for planned backend rows with execution marked unavailable.</p></li><li><p>Added planned genomic/QTL model-term vocabulary reservations: <code>genomic()</code>, <code>single_step()</code>, <code>markers()</code>, <code>marker_scan()</code>, and <code>qtl_scan()</code>.</p></li><li><p>Added planned standard quantitative-genetic model-term vocabulary reservations: <code>permanent()</code>, <code>common_env()</code>, <code>maternal_genetic()</code>, <code>maternal_env()</code>, <code>paternal_genetic()</code>, <code>paternal_env()</code>, <code>cytoplasmic()</code>, <code>imprinting()</code>, <code>dominance()</code>, <code>epistasis()</code>, <code>relmat()</code>, and <code>HSquared.precision()</code> in direct Julia code.</p></li><li><p>Added a Documenter model-spec grammar page mirroring the R twin&#39;s status separation for parsed, reserved, and planned syntax.</p></li><li><p>Added <code>formula_status()</code> grammar diagnostics and a Documenter status table mirroring the R twin&#39;s parsed/reserved/planned grammar rows.</p></li><li><p>Added <code>validation_status()</code> diagnostics for covered, external, partial, and planned validation rows.</p></li><li><p>Added <code>max_dense_cells</code> guards for the temporary dense validation path.</p></li><li><p>Added experimental dense variance-component optimization for validated low-level animal-model specs.</p></li><li><p>Added experimental low-level variance-component, fixed-effect, EBV/BLUP, fitted-value, and heritability extractors for the dense spec path.</p></li><li><p>Switched <code>breeding_values(fit)</code> to the Henderson MME solve at the fit&#39;s variance components.</p></li><li><p>Switched <code>fitted_values(fit)</code> to the same Henderson MME solve at the fit&#39;s variance components.</p></li><li><p>Added experimental dense prediction-error-variance and reliability extractors for the dense spec path.</p></li><li><p>Extended validation-scale prediction-error-variance and reliability extractors to supplied-variance <code>HendersonMMEResult</code> objects.</p></li><li><p>Extended supplied-variance <code>variance_components()</code> and <code>heritability()</code> extractors to <code>HendersonMMEResult</code> objects.</p></li><li><p>Added <code>EBV()</code> and <code>BLUP()</code> aliases for <code>breeding_values()</code>, plus <code>accuracy()</code> as a checked square-root transformation of reliability.</p></li><li><p>Added <code>fit_diagnostics()</code> as metadata-only extraction for low-level <code>AnimalModelFit</code> and supplied-variance <code>HendersonMMEResult</code> objects.</p></li><li><p>Added experimental direct payload <code>fit_animal_model(y, X, Z, Ainv; ...)</code> target for bridge-shaped inputs.</p></li><li><p>Added explicit <code>fit_animal_model(...; target = :henderson_mme, variance_components = ...)</code> dispatch for supplied-variance Henderson MME solving.</p></li><li><p>Added <code>henderson_mme()</code> for sparse Henderson mixed-model-equation solving at supplied variance components.</p></li><li><p>Added a shared R/Julia Henderson mixed-model-equation validation fixture for the supplied-variance output path.</p></li><li><p>Added <code>result_payload()</code> with field names aligned to the R <code>hsquared_fit</code> extractor contract.</p></li><li><p>Added <code>HSData</code>, <code>HSDataIDMap</code>, and <code>id_map()</code> as an in-memory mirror of the R <code>hs_data()</code> input-container contract.</p></li><li><p>Added <code>HSData</code> marker-map metadata validation and genotype-marker alignment checks.</p></li><li><p>Added <code>data_status(::HSData)</code> diagnostics mirroring the R twin&#39;s <code>data_status()</code> surface for component presence, ID-overlap counts, pedigree status, genotype status, marker-alignment status, expression status, annotation-feature status, and environment-key status. Diagnostic only; no bridge payload, raw-pedigree Ainv construction, genotype parsing, relationship construction, annotation or environment covariate joins, automatic expression-feature joins, environmental model terms, marker scan, genomic fitting, eQTL/omics fitting, or QTL/eQTL claim.</p></li><li><p>Recorded the R twin&#39;s <code>hs_data()</code> genotype-status diagnostics from <code>hsquared</code> head <code>f067cd9</code> and mirrored them in Julia <code>HSData</code> as metadata diagnostics only.</p></li><li><p>Recorded the R twin&#39;s <code>hs_data()</code> expression-status diagnostics from <code>hsquared</code> head <code>06cdf59</code> and mirrored them in Julia <code>HSData</code> as metadata diagnostics only.</p></li><li><p>Recorded the R twin&#39;s <code>hs_data()</code> annotation-feature diagnostics from <code>hsquared</code> head <code>87888d9</code> and mirrored them in Julia <code>HSData</code> as metadata diagnostics only.</p></li><li><p>Recorded the R twin&#39;s <code>hs_data()</code> environment-key diagnostics from <code>hsquared</code> head <code>e7fbb31</code> and mirrored them in Julia <code>HSData</code> as metadata diagnostics only.</p></li><li><p>Added <code>sparse_csc_matrix()</code> for R <code>Matrix::dgCMatrix</code> slot marshalling.</p></li><li><p>Recorded the R twin&#39;s PEV/reliability bridge extractor contract while keeping Julia <code>result_payload()</code> fields unchanged.</p></li><li><p>Recorded the R twin&#39;s tiny/local bridge enrichment of PEV/reliability from exported Julia extractors, still without widening base <code>result_payload()</code>.</p></li><li><p>Recorded the R twin&#39;s supplied-variance <code>target = &quot;henderson_mme&quot;</code> enrichment from <code>prediction_error_variance(mme)</code> and <code>reliability(mme)</code>, still without widening base <code>result_payload()</code>.</p></li><li><p>Recorded the R twin&#39;s opt-in supplied-variance Henderson MME bridge target, with explicit no-log-likelihood/no-variance-estimation boundary.</p></li><li><p>Recorded the R twin&#39;s sparse <code>Z</code> bridge marshalling handoff.</p></li><li><p>Recorded the R twin&#39;s optional <code>nadiv::Mrode9</code> pedigree-Ainv comparator evidence.</p></li><li><p>Recorded the R twin&#39;s <code>model_spec()</code> preview surface for the v0.1 formula-to-bridge payload.</p></li><li><p>Recorded the R twin&#39;s <code>hs_data()</code> parser integration for the v0.1 formula-to-bridge payload without changing the Julia payload shape.</p></li><li><p>Recorded the R twin&#39;s <code>animal(1 | id)</code> shorthand for <code>data = hs_data(..., pedigree = ped)</code> as R-side formula ergonomics only; the explicit <code>animal(1 | id, pedigree = ped)</code> contract and Julia payload shape are unchanged.</p></li><li><p>Added DocumenterVitepress documentation scaffold.</p></li><li><p>Added audience and comparator programme notes.</p></li><li><p>Added genomics/QTL/eQTL/GLLVM/GPU/HPC strategic roadmap.</p></li><li><p>Added a backend and algorithm roadmap page for CPU, threads, CUDA, AMDGPU, Metal, oneAPI, AI-REML, Takahashi selected inversion, Woodbury paths, APY, and claim gates.</p></li><li><p>Kept high-level fitting entry points as honest placeholders.</p></li></ul>',3)])])}const h=o(t,[["render",n]]);export{u as __pageData,h as default};
