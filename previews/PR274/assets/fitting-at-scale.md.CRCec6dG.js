import{_ as t,o as i,c as a,ao as s}from"./chunks/framework.C-V19Ze5.js";const p=JSON.parse('{"title":"Fitting at scale: exact vs matrix-free","description":"","frontmatter":{},"headers":[],"relativePath":"fitting-at-scale.md","filePath":"fitting-at-scale.md","lastUpdated":null}'),r={name:"fitting-at-scale.md"};function o(n,e,l,d,h,c){return i(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="Fitting-at-scale:-exact-vs-matrix-free" tabindex="-1">Fitting at scale: exact vs matrix-free <a class="header-anchor" href="#Fitting-at-scale:-exact-vs-matrix-free" aria-label="Permalink to &quot;Fitting at scale: exact vs matrix-free {#Fitting-at-scale:-exact-vs-matrix-free}&quot;">​</a></h1><p><code>HSquared.jl</code> has <strong>two</strong> engines for the <code>K</code>-independent-random-effect mixed model, and <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_multi_effect"><code>fit_multi_effect</code></a> chooses between them by feasibility. This page explains the choice, the accuracy-vs-feasibility trade, and how to control it.</p><div class="warning custom-block"><p class="custom-block-title">Experimental</p><p>Everything on this page is experimental and validation-scale. The public default fit path is the univariate Gaussian animal model; these multi-effect engines are opt-in.</p></div><h2 id="The-two-engines" tabindex="-1">The two engines <a class="header-anchor" href="#The-two-engines" aria-label="Permalink to &quot;The two engines {#The-two-engines}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">exact</th><th style="text-align:right;">matrix-free</th></tr></thead><tbody><tr><td style="text-align:right;">function</td><td style="text-align:right;"><a href="/HSquared.jl/previews/PR274/api#HSquared.fit_sparse_multi_effect_aireml"><code>fit_sparse_multi_effect_aireml</code></a></td><td style="text-align:right;"><a href="/HSquared.jl/previews/PR274/api#HSquared.fit_multi_effect_mc_reml"><code>fit_multi_effect_mc_reml</code></a></td></tr><tr><td style="text-align:right;">method</td><td style="text-align:right;">sparse AI-REML (exact gradient from a Cholesky <strong>selected inverse</strong>)</td><td style="text-align:right;">Monte-Carlo EM-REML (matrix-free solves + a <strong>Hutchinson stochastic trace</strong>)</td></tr><tr><td style="text-align:right;">forms/factorizes <code>C</code>?</td><td style="text-align:right;"><strong>yes</strong> — a sparse Cholesky each iteration</td><td style="text-align:right;"><strong>no</strong> — only &quot;matrix × vector&quot;</td></tr><tr><td style="text-align:right;">accuracy</td><td style="text-align:right;">exact</td><td style="text-align:right;">approximate — the gradient carries a Monte-Carlo standard error</td></tr><tr><td style="text-align:right;">where it wins</td><td style="text-align:right;">small–moderate size (exact, few iterations)</td><td style="text-align:right;">very large size, where the factorization is infeasible</td></tr></tbody></table><p><strong>Why two?</strong> With <em>two or more</em> random effects (a contemporary-group, maternal, or permanent-environment term alongside the animal effect) the sparse Cholesky <strong>fills in</strong> — the factor becomes far denser than <code>C</code> — and past roughly <code>10⁵</code> individuals it becomes infeasible in memory and time (measured: the direct multi-effect path is already ~quadratic by <code>q ≈ 50 000</code>; a METIS reordering did <strong>not</strong> fix it). The matrix-free engine never forms or factorizes <code>C</code>, so it has no fill wall — it has stayed feasible fitting <code>K = 3</code> models to 200 000 individuals in testing (the matrix-free <em>solve</em> it is built on reaches a million) — but it pays with Monte-Carlo noise in the variance-component gradient.</p><p>A <em>single</em> random effect is usually the easy case — but <strong>not always</strong>, and the difference is <strong>fill-in, not size</strong>. See the next section.</p><p>This is the same accuracy-for-feasibility trade a variational approximation makes: you reach for the approximate engine <strong>only</strong> where the exact one cannot run.</p><h2 id="The-single-effect-animal-model:-size-is-not-the-problem,-fill-is" tabindex="-1">The single-effect animal model: size is not the problem, fill is <a class="header-anchor" href="#The-single-effect-animal-model:-size-is-not-the-problem,-fill-is" aria-label="Permalink to &quot;The single-effect animal model: size is not the problem, fill is {#The-single-effect-animal-model:-size-is-not-the-problem,-fill-is}&quot;">​</a></h2><p>For the univariate animal model the same two-engine choice exists — <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_ai_reml"><code>fit_ai_reml</code></a> (exact) versus <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_matrix_free_reml"><code>fit_matrix_free_reml</code></a> (matrix-free) — and <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_animal_model"><code>fit_animal_model</code></a> with <code>target = :auto</code> picks between them (plus the eigen-once path).</p><p>The intuition &quot;one random effect, so the factorization stays sparse&quot; is <strong>only true for well-structured pedigrees</strong>. What decides cost is the fill-in of the MME Cholesky, <code>nnz(L)/n</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">pedigree</th><th style="text-align:right;">fill <code>nnz(L)/n</code></th><th style="text-align:right;"><code>fit_ai_reml</code></th><th style="text-align:right;">machine</th></tr></thead><tbody><tr><td style="text-align:right;">half-sib, q = 300 000</td><td style="text-align:right;">~17–19</td><td style="text-align:right;">2.3 s</td><td style="text-align:right;">DRAC (<code>fir</code>)</td></tr><tr><td style="text-align:right;">random-mating, q = 10 000</td><td style="text-align:right;">262</td><td style="text-align:right;">132 s</td><td style="text-align:right;">Totoro</td></tr><tr><td style="text-align:right;">random-mating, q = 20 000</td><td style="text-align:right;">471</td><td style="text-align:right;">1 529 s (~25 min)</td><td style="text-align:right;">Totoro</td></tr></tbody></table><p>A high-fill pedigree at <strong>1/15th the size</strong> costs ~660× more. Field data from livestock and most managed populations is low-fill and needs none of this; densely interconnected pedigrees — small founder bases, random mating, deep overlapping generations — are where it bites.</p><div class="warning custom-block"><p class="custom-block-title">These rows are from different machines, and so is the table further down</p><p>The three rows above come from two clusters (<code>docs/dev-log/recovery-checkpoints/2026-07-24-f0-adversarial-highfill-decision.md</code>), and the crossover table later on this page is a Mac Studio. <strong>Do not read them as one series.</strong> The same fit — <code>fit_ai_reml</code> at q = 10 000, fill 262 — is 132 s on Totoro and 231 s on the Mac Studio. Each table is internally consistent; across tables only the <em>shape</em> carries meaning.</p></div><p>The mechanism is specific and worth knowing, because it is <em>not</em> the factorization. At q = 20 000 the sparse Cholesky takes 0.35 s; the <strong>Takahashi selected inverse</strong> that supplies AI-REML&#39;s exact score trace takes 381 s, and it is recomputed every iteration. That single term is the wall. <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_matrix_free_reml"><code>fit_matrix_free_reml</code></a> replaces it with a matrix-free Hutchinson stochastic trace and PCG solves, so <code>C</code> is never assembled or factorized during the fit:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fit_animal_model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(spec; target </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :matrix_free</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># or fit_matrix_free_reml(spec)</span></span></code></pre></div><p>Measured crossover on high-fill pedigrees — a measurement on one machine, not a performance claim. Timings are the <strong>median of 3 runs</strong> after a discarded warm-up, single-threaded on an Apple Mac Studio (julia 1.12.6). Reproduce with <code>sim/matrix_free_crossover_benchmark.jl</code>; the committed numbers, including per-cell min/max, are in <code>sim/matrix_free_crossover.tsv</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">q</th><th style="text-align:right;">fill</th><th style="text-align:right;">exact</th><th style="text-align:right;">matrix-free</th><th style="text-align:right;">speed-up</th><th style="text-align:right;">agreement with the exact optimum</th></tr></thead><tbody><tr><td style="text-align:right;">1 000</td><td style="text-align:right;">50</td><td style="text-align:right;">0.22 s</td><td style="text-align:right;">1.73 s</td><td style="text-align:right;">0.13× (exact wins)</td><td style="text-align:right;">1.7e-2</td></tr><tr><td style="text-align:right;">2 000</td><td style="text-align:right;">77</td><td style="text-align:right;">1.05 s</td><td style="text-align:right;">2.88 s</td><td style="text-align:right;">0.37× (exact wins)</td><td style="text-align:right;">3.0e-3</td></tr><tr><td style="text-align:right;">5 000</td><td style="text-align:right;">151</td><td style="text-align:right;">25.0 s</td><td style="text-align:right;">9.12 s</td><td style="text-align:right;"><strong>2.74×</strong></td><td style="text-align:right;">4.7e-3</td></tr><tr><td style="text-align:right;">10 000</td><td style="text-align:right;">262</td><td style="text-align:right;">231.4 s</td><td style="text-align:right;">13.9 s</td><td style="text-align:right;"><strong>16.6×</strong></td><td style="text-align:right;">4.7e-3</td></tr></tbody></table><p>The exact path grows super-linearly in fill; the matrix-free one roughly linearly. Note the agreement column: it is a Monte-Carlo <em>approximation</em>, not equality — see <a href="/HSquared.jl/previews/PR274/fitting-at-scale#Reading-the-Monte-Carlo-noise">Reading the Monte-Carlo noise</a> below.</p><div class="warning custom-block"><p class="custom-block-title">The matrix-free fitter is opt-in — <code>:auto</code> will not choose it for you</p><p><code>target = :auto</code> routes only between the two <strong>exact</strong> fitters. It never selects <code>fit_matrix_free_reml</code>, and you must ask for it explicitly.</p><p>That is deliberate, and it is a fence rather than an oversight. The regime a route would serve — high fill past the dense eigen cap — is exactly the regime in which this fitter has <strong>not</strong> been measured: the crossover above was measured at <code>n ≤ 10 000</code>, so routing on it at <code>n &gt; 20 000</code> would be extrapolation. Handing you a Monte-Carlo estimate you did not ask for, on the strength of a number measured somewhere else, is a worse failure than being slow.</p><p>Wiring it into <code>:auto</code> needs a pre-declared recovery gate in that tail first. Until then: <code>fit_animal_model(spec; target = :matrix_free)</code>.</p></div><h2 id="Choosing-automatically-multi-effect" tabindex="-1">Choosing automatically (multi-effect) <a class="header-anchor" href="#Choosing-automatically-multi-effect" aria-label="Permalink to &quot;Choosing automatically (multi-effect) {#Choosing-automatically-multi-effect}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fit_multi_effect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(y, X, effects)                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># method = :auto (the default)</span></span></code></pre></div><p><code>:auto</code> routes on feasibility:</p><ul><li><p>a <strong>single</strong> random effect (<code>K = 1</code>), or <code>N = p + Σqᵢ ≤ direct_max_n</code> → <strong>exact</strong>;</p></li><li><p>otherwise → <strong>matrix-free</strong>, printing a one-line <code>@info</code> that it switched and that the estimates carry a Monte-Carlo standard error.</p></li></ul><div class="warning custom-block"><p class="custom-block-title"><code>fit_multi_effect</code>&#39;s <code>K = 1</code> shortcut assumes low fill</p><p>That <code>K = 1</code> → exact rule is a <em>structural</em> assumption, and a high-fill single-effect pedigree breaks it (see the previous section). For the univariate animal model prefer <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_animal_model"><code>fit_animal_model</code></a> with <code>target = :auto</code>, which routes on measured fill rather than on <code>K</code>; or pass <code>method = :matrix_free</code> here explicitly.</p></div><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fit_multi_effect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(y, X, effects; method </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :exact</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># force exact (may run out of memory)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fit_multi_effect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(y, X, effects; method </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :matrix_free</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># force matrix-free (accept MC noise)</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">The <code>:auto</code> threshold is a heuristic</p><p><code>direct_max_n</code> (default <code>200_000</code>) is a <strong>coarse, machine-agnostic</strong> cutoff calibrated to the measured <code>K ≥ 2</code> factorization cost, deliberately conservative and always overridable. It is <strong>not</strong> a precise per-problem fill prediction — that is future work. When in doubt on a large problem, try <code>method = :exact</code> first; if it fits in memory, its answer is exact.</p></div><p>The returned <code>NamedTuple</code> carries a <code>dispatch</code> field (<code>:exact</code> or <code>:matrix_free</code>) recording which engine ran. The two result shapes differ: the matrix-free result adds a <code>trace_mcse</code>, and its <code>loglik</code> is <code>NaN</code> unless you ask for it with <code>compute_loglik = true</code> — the matrix-free REML log-likelihood needs a stochastic log-determinant (<a href="/HSquared.jl/previews/PR274/api#HSquared.matrix_free_reml_loglik"><code>matrix_free_reml_loglik</code></a>, estimated by stochastic Lanczos quadrature), so it carries its own Monte-Carlo error, reported as <code>loglik_mcse</code>.</p><p>The single-effect <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_matrix_free_reml"><code>fit_matrix_free_reml</code></a> differs here: it returns the standard <code>AnimalModelFit</code>, and its log-likelihood is <strong>exact</strong>, not stochastic. It can afford one <a href="/HSquared.jl/previews/PR274/api#HSquared.sparse_reml_loglik"><code>sparse_reml_loglik</code></a> call at the converged estimate because that costs two Choleskys and <strong>no</strong> selected inverse — and the selected inverse, not the factorization, was the wall it exists to avoid. Set <code>compute_loglik = false</code> to skip even that.</p><h2 id="Reading-the-Monte-Carlo-noise" tabindex="-1">Reading the Monte-Carlo noise <a class="header-anchor" href="#Reading-the-Monte-Carlo-noise" aria-label="Permalink to &quot;Reading the Monte-Carlo noise {#Reading-the-Monte-Carlo-noise}&quot;">​</a></h2><p>When the matrix-free engine runs, the fit reports <code>trace_mcse</code> — the Monte-Carlo standard error of the score-trace terms, i.e. the noise you traded for feasibility. It shrinks like <code>1/√nprobe</code> (more probe vectors ⇒ tighter). If you need a tighter answer, raise <code>nprobe</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fit_multi_effect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(y, X, effects; method </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :matrix_free</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, nprobe </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 256</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The matrix-free fit <strong>reproduces</strong> the exact AI-REML variance components as <code>nprobe</code> grows: in the recovery gate it tracks the exact estimator on identical data, with the Monte-Carlo perturbation controllable by <code>nprobe</code>.</p><h2 id="Honest-scope" tabindex="-1">Honest scope <a class="header-anchor" href="#Honest-scope" aria-label="Permalink to &quot;Honest scope {#Honest-scope}&quot;">​</a></h2><ul><li><p>These engines fit <strong>variance components + BLUPs</strong> for <code>K</code> independent Gaussian random effects. They are not the public default, not calibrated-interval methods, and the matrix-free engine is a supplied-machinery estimator whose scale evidence is machine-specific (see the validation-status page and the v0.8 recovery-checkpoints).</p></li><li><p>The exact engine&#39;s small-sample recovery is the covered <code>V3-NEFFECT-REML</code> result. The matrix-free engine&#39;s added claim is only that its Monte-Carlo approximation <strong>reproduces</strong> that exact fit.</p></li><li><p>The same fence applies to the single-effect <a href="/HSquared.jl/previews/PR274/api#HSquared.fit_matrix_free_reml"><code>fit_matrix_free_reml</code></a> (<code>V1-MATFREE-REML</code>, <strong>experimental</strong>). It is validated to recover the <code>fit_ai_reml</code> optimum within Monte-Carlo error on committed high-fill fixtures — and that is agreement with <em>another estimator</em>, not recovery-to-truth. A pre-declared known-truth recovery gate at tail scale and an external same-estimand comparator are <strong>owed</strong> before any covered claim. It is not wired into the R bridge and moves no public capability count.</p></li><li><p>Every timing on this page is a <strong>measurement on the machine that ran it</strong>, not a performance claim or a competitive benchmark, and none of it is a CI gate.</p></li><li><p>Both <code>:auto</code> fill thresholds (60 for eigen-once, 150 for matrix-free) are <strong>first-pass scalar heuristics</strong>. The true crossover is a joint fill × <code>n</code> surface; the 150 anchor was measured at <code>n = 5 000</code> and extrapolated into the <code>n &gt; 20 000</code> tail it serves. Mapping that surface properly is owed work.</p></li></ul>`,35)])])}const m=t(r,[["render",o]]);export{p as __pageData,m as default};
